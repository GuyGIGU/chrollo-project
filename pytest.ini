[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    slow: slow tests (deselect with -m "not slow")
    regression: byte-parity / shadow / seed-recall regression guards
    network: tests that touch the network (deselect with -m "not network")
