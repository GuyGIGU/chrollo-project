# Chrollo Frontend

React + Vite dashboard for the local Chrollo app.

```powershell
npm install
npm run build
npm run lint
```

The production UI is served by the FastAPI backend from `webapp/frontend/dist`.
