# Asks — every open question owed to the operator, one line each

**The landing rule (council 2026-08-22):** a session appends its ask in the SAME change that
creates it; a ruling received in chat lands its decisions.md row AND closes its ask row in the
same session. An unlanded ruling stays visible here instead of surviving only in an agent memory.
Closed asks move to the bottom with their resolution — never deleted.

| Asked | The question | Blocks | Look at |
|---|---|---|---|
| 2026-09-08 (A/B RAN same day) | **The RS/uptrend A/B is DONE — my recommendation is leave them at zero. Yours is the only thing owed.** You said go, all three legs ran, nothing was flipped. **(1) The reversal reproduces on their own cohort and variable** (2,078 pre-demotion episodes vs their cited 1,977): `score_rs_bonus` −0.141 vs `durable_win` but **+0.091** vs P(MFE≥25%); `score_uptrend_bonus` −0.091 and **+0.150**. **(2) But the tool's own noise floor there is 0.150, and −0.141/−0.091 sit INSIDE it** — under `signal_edge`'s own adequacy rule the honest verdict on the deduped cohort is INERT in both directions, so "actively harmful" does not survive dedup. **(3) The restored ordering ranks better on every cut** (top-decile P(≥25%) 9.06→10.80%, spread 2.04→2.63x; ρ vs mfe_20d +0.121→+0.147) **but a cluster bootstrap over scan days does not establish it: +1.84pp, 95% CI [−0.35, +4.35], P(improves) 92.2%.** Crosses zero. **(4) The cost is measured**: at the top decile median MAE goes −7.29% → −8.52% and MFE/\|MAE\| 1.01 → 0.96 — the same "tail bought with a wider stop" trade the ADR read found. **(5) CORRECTION to what I told you**: restoring does NOT roughly double S-tier. That 51%→29% figure is from the retired raw-sum ladder; tier now comes from `ta_grade`, whose divisor is computed live from the caps (171.0 now, **201.0** if restored), so a setup must earn ≥17.5% of its current points just to hold its grade — restoring at the current 62/52/42 cuts would SHRINK S-tier, and a real flip would have to re-choose the ladder. **What shipped instead is the measuring stick**: `EDGE_TARGETS` now carries a label-free magnitude target judged on its own noise floor, the primary deliberately unchanged, and a `verdict_disagrees` flag that fires live on `score_adr` (+0.269 beneficial on magnitude, invisible to the primary), `score_high_proximity` (−0.157 harmful) and `score_base_age` (the only term graded beneficial today — and it does not survive the cross-check). **Answer: accept "stays at zero", or tell me the bootstrap bar is too strict for your purposes and I will take it further.** | nothing — the weights are unchanged and the Tested-DEAD row stands | [rs_uptrend_reopen_2026-09-08.md](rs_uptrend_reopen_2026-09-08.md) "THE A/B RAN"; `core/archive/analyze.py` `MAGNITUDE_TARGET` |
| 2026-09-08 (superseded by the row above) | **Is this enough to spend the RS/uptrend A/B your own ruling requires?** Your 2026-08-06 ruling says they *"stay demoted at 0 … never re-inflated outside an operator A/B"*, and the Tested-DEAD row rests on the 2026-07-22 edge read ("actively harmful", 1977 episodes). **New evidence: that read scored a criterion you do not trade.** The Stage-1 classifier ranks sub-scores against `durable_win` / `barrier_win`, which resolve on whichever barrier is hit FIRST — target, or a mechanical stop 3% under support. You manage exits by eye, not on that stop. Measured on the same labelled rows, the two statistics disagree by RS quartile: win rate falls 47.3% → **37.6%** from RS-low to RS-high while P(MFE≥25%) rises 9.70% → **16.17%** and P(≥40%) 1.62% → **5.31%**. Among rows filed as `loss`, the share that nonetheless reached +25% is 2.00% at RS-low against **5.64%** at RS-high — **28 rows are recorded as losses and went on to +25%, and those 28 are 16.4% of every row in the archive that reached +25% at all**. Decomposing the sign flip, it is the TARGET not the missing rows: Spearman(RS, barrier_win) = −0.042 but Spearman(RS, P(MFE≥25%)) = **+0.081 on identical rows**, before any row is added back. `yearly_return` reads BENEFICIAL on every target including the label-gated ones. **What this does NOT show:** I did not reproduce the original −0.19/−0.22 (different cohort; they measured the ramped sub-score, I measured the raw signal), and nothing here says restoring the weights improves the ranking — only that the evidence for removing them scored the wrong criterion. **The cost is real**: the demotion moved S-tier share 51% → 29%, so restoring roughly doubles S-tier. And RS-high genuinely does get stopped out more — if you want the ordering to favour names that hold a tight stop, the original ruling stands untouched. Answer: spend the A/B, or let the Tested-DEAD row stand with this appended as the counter-evidence. | 30 points of scoring currently at zero; the Stage-1 classifier's primary target | [rs_uptrend_reopen_2026-09-08.md](rs_uptrend_reopen_2026-09-08.md); `config/scoring.py` `SCORE_RS_BONUS` / `SCORE_UPTREND_BONUS`; `core/archive/analyze.py` `EDGE_TARGETS` |
| 2026-09-08 | **Re-run the 2026-09-03 "no edge" verdict — both statistics it rested on are now known to be the wrong ones.** That verdict tested the gate against the outside universe at the MEDIAN and at +15%, and concluded no measurable edge over a random same-day pick (+0.1%, p=0.203). Two measurements since say it looked in the wrong places. **(a) The ordering was never tested at the tail.** Within-day score deciles over 7,025 matured rows: P(MFE≥40%) is **3.06% top decile against 0.15% bottom — 20.9x**, P(≥25%) 4.2x, while the median is flat and non-monotonic (7.63% → 6.31%). The median is tail-blind by construction, and the tail is the only region a VCP/Qullamaggie book is paid in. **(b) The win rate was computed on a moving denominator.** 41% of matured rows carry no `barrier_label` and the missing share runs 26% for tier S to 80% for tier C — it survives a control for scan age — so every win rate compared tiers on denominators differing 4x, which is how tier C came to read as the best tier. Both are now fixed in the reporting ([edge_denominator_2026-09-08.md](edge_denominator_2026-09-08.md)); the VERDICT itself has not been re-run. **What I could not settle:** the cohort old enough that every row is labelled is 1,072 rows over six scan days and 85% tier S — one market episode, too thin to rule on, and its own deciles point the other way. So this needs either more matured history or a re-run on the corrected statistics, and it is your call whether that is worth doing now or after the archive thickens. It also bears directly on the parked signal-edge program (row below, register row 6): re-landing 2,246 lines of rigor machinery to re-measure a median would be the wrong purchase. | Whether the engine's ranking is believed; the signal-edge live-or-die below | [edge_denominator_2026-09-08.md](edge_denominator_2026-09-08.md); `core/backtest/edge_report.py` `tail_rates`; [pattern_register.md](pattern_register.md) row 6 |
| 2026-09-07 | **Sign the two state names, and the repeat threshold.** Your cut is built: the headline is now one of two words, and the taxonomy sits underneath it. **(a) The names are MINE.** The pill reads **"Re-run the scan"** (amber) or **"Needs attention"** (red), each named for what you do, not for what broke — say the word if you'd rather read something else there (e.g. "Needs Claude", which is closer to how you put it). **(b) I picked the repeat threshold: THREE.** The same failure on three consecutive runs of one job stops being "just re-run it" and escalates to Needs attention. One night is noise; two can be two nights of your usual power-off; three means pressing the button is not fixing it. It is one constant, `REPEAT_ESCALATION` — say 2 or 4 and it moves. **(c) `stale_data` is on the Needs-attention side.** A stale-data stop is the engine refusing to scan prices that are behind, so re-running meets the same refusal — but if you'd rather see it as "download, then re-run", it moves to the re-runnable side with one row. | nothing (it ships either way) | `services/scan_diagnosis.py` verdict block; [scan_interruption_incident_2026-09.md](scan_interruption_incident_2026-09.md) "The operator cut the report to TWO states" |
| 2026-09-07 | **Move the nightly scan earlier, and sign the failure wording.** Two parts. **(a) The real fix for your lost nights is the SLOT, not better error text.** The scan starts 18:00 New York = 01:00 your time and needs ~17 minutes; your Start-menu shutdowns landed at 01:00:25, 00:59:30 and 01:42 on three consecutive nights, costing two scans. `SCAN_SCHEDULE_HOUR_ET` (`config/runtime.py:26`) is a one-line change — but the slot exists to run after the US close, so moving it is your call, not mine. **(b) The four failure sentences and their proposed solutions are MINE, not signed.** They are what the registry and the pill tooltip now say, e.g. *"The computer was shut down while the scan was still running, so the scan never finished."* **(c) One sentence asserts a negative** — *"The Chrollo dashboard restarted while the scan was running. The computer stayed on, as far as Windows recorded."* It is gated hard (Windows read successfully, no shutdown record in the run's window, and no reboot since the run began) but it is the only claim in the feature that could be wrong. Say the word and it collapses into *"the reason is not recorded"* — four lines. | nothing (the feature ships either way) | `services/scan_diagnosis.py` REASONS/SOLUTIONS; [scan_interruption_incident_2026-09.md](scan_interruption_incident_2026-09.md) |
| 2026-09-02 | **Is this the density you draw at?** The framing direction is ruled (your 2026-09-02 complaint); the NUMBERS are not. The card now shows **140 trading days at ~4.9 px per day in a 742px card**, two-up at 1536 — against your TradingView screenshot's ~190 days at ~7.0 px/day in a 1389px pane. The modal opens on **one trading year at 4.17 px/day**. Both sit inside the module's own 3.7–5.3 faithful band, but the band was never your ruling. Three knobs, one line each to move: `CHART_FRAMING.mini.referenceBars` (the card's span), `CHART_FRAMING.modal.referenceBars` (the close-up's), and `CARD_MIN_PX` (620 → two-up; 480 was three-up). | Sealing the constants; the ~7 monster bases below | MSEX / NP / SBUX side by side in the grid; [chart_framing_2026-09.md](chart_framing_2026-09.md) |
| 2026-09-02 | **Carry more daily history for the ~14 long-base setups?** You ruled *"for monster bases simply zoom out the base"* and that is built (the ceiling now yields; over-60% cards went 7 → 3). But **three cannot be fixed by framing** — the payload only carries 300 trading days and their rests need more: STRA (rest 344 days, wants an 872-day window, still fills 100% of the card), XRN (263 → 766, 87.7%), CRSP (189 → 555, 63.0%). A per-setup floor — carry `max(300, base_len + context)` days for the ~14 setups whose rest exceeds 120 days — costs well under 1 MB on a 17.8 MB artifact, against 6.3 MB for a global `DASHBOARD_CHART_DAYS` raise. It is a scan-pipeline change and takes effect on the next nightly scan, so it is yours to call. | STRA / XRN / CRSP reading honestly | `config/runtime.py` `DASHBOARD_CHART_DAYS`; `core/pipeline/market_data/candles.py` `chart_candles`; [chart_framing_2026-09.md](chart_framing_2026-09.md) monster-tail section |
| 2026-08-31 | **Three cited evidence sidecars are gone — rule the repair.** EC-53 landed today (cited evidence is tracked in the change that cites it); the audit that followed it found four `output/power_play_census_*.json` sidecars cited by decisions.md ruling rows, the flag ledger and the Power Play program record. `power_play_census_postfix_2026-08-22.json` survives ONLY in the main checkout — **8.9 MB**, too big to commit without your word given the documented bloat pattern. `power_play_census_relaxed.json`, `power_play_census_species.json` and `power_play_census_wall.json` are **unrecoverable** (searched the project tree, all 25 scratchpad roots, and the OneDrive mirror). Re-running them under the cited names would be WORSE than the gap: it would produce today's numbers wearing the names the rulings cite. Options: (a) commit the 8.9 MB survivor and mark the three dead in place; (b) mark all four dead-with-reason; (c) re-run under NEW dated names as fresh corroboration, leaving the original citations marked unrecoverable. | Reproducibility of the Power Play ruling evidence | decisions.md rows citing `output/power_play_census_*`; [flag_ledger.md](flag_ledger.md); [power_play_program_2026-08.md](power_play_program_2026-08.md) |
| 2026-08-28 | **Flip the contraction rescue?** Your six-miss ruling ("no reason at large for these to miss") is built as `CONTRACTION_RESCUE_ENABLED` (dark): when the whole walk elects NOTHING, it re-walks once with the resistance-contraction form you already ruled real on EGBN/PKE ("BOTH have setups on these days so yeah", 2026-08-19). Frozen-frame A/B: **EGBN fires 01-07 tier A on your rails, PKE 02-18 tier B; junk corpus 18/18 clean; existing elections untouchable by construction** (the WCC wider-box defect that refused the global flip is unreachable). Fleet census at the cache edge: **32 conversions out of 1,241 no-reads (2.6%; +10% on the 309 standing fires), every one a self-naming story fire — a 32-name eyeball sheet awaits you** (`research/evidence/miss_lane_census_2026-08-28.json`; KFY/TBI/ANGO/BIIB top it). The flip re-seals the ratchet (EGBN/PKE leave the expected-miss list) and wants the second-walk cost measured on one nightly scan first. | EGBN catching live (PKE left the miss list: the re-marked PKE@2026-04-07 fires at BASELINE — 2026-03-26 tier B, his rails) | [miss_program_2026-08.md](miss_program_2026-08.md); [flag_ledger.md](flag_ledger.md) row |
| 2026-08-28 | **The 50-day dip exception: rule the SHAPE — the census says as-shipped is wide.** `SMA50_DIP_EXCEPTION_ENABLED` (dark) converts BOTH sma50 misses (SKYT — whose drawn spring dragged price under the 50-day — **fires tier S on 2026-04-07**; ST **fires tier S on 2026-04-06**, the session before its drawn SOS); junk corpus clean; SMA200/YoY still gate. But the fleet census (cache edge 2026-08-27): **157 of 1,770 sma50 refusals FIRE through it — +51% on the 309 standing fires** — and they are mostly shallow 3-session wobbles hugging the 50-day (median gap 0.43 ATR), while SKYT sits at the DEEP end (20 sessions, 0.90 ATR), so **no knob setting drops the wobbler mass without dropping SKYT first** (the measured curve is in the program doc). Options: (a) flip as-is and eyeball the sheet (`research/evidence/miss_lane_census_2026-08-28.json`); (b) narrow to the STORY-coupled form first — admit the dip only when the elected box's own below-S excursion (the drawn-spring shape) explains it — a follow-up design on your go-ahead; (c) hold dark. | SKYT + ST catching live | [miss_program_2026-08.md](miss_program_2026-08.md); [flag_ledger.md](flag_ledger.md) row |
| 2026-08-29 | **Flip the bottoming-base lane?** `BOTTOMING_BASE_LANE_ENABLED` (dark) — your "I don't want to miss them" ruling built: the sma200 rule opens at BOTH layers (door + anchor seeding) when the 50-day is reclaimed; price/volume/YoY still gate, and the chained-door case is pinned (a both-smas-under chart can never enter). **MDT fires 2026-07-14 tier S at your drawn R 82.83.** Junk corpus clean. The census ran: **410 sma200 refusals, 194 admit under the reclaimed-50-day condition, 60 FIRE (+19% on 309 standing fires; 15 S / 32 A)** — a 60-name eyeball sheet in `research/evidence/miss_lane_census_2026-08-29.json`, between the rescue's 32 and the dip lane's 157. | MDT catching live | [miss_program_2026-08.md](miss_program_2026-08.md); [flag_ledger.md](flag_ledger.md) row |
| 2026-08-29 | **Flip the ceiling-rest LPS exception — and eyeball its razor.** `LPS_CEILING_REST_ENABLED` (dark): the launched-above-R straddle passes when the rest lands ON the ceiling (within 0.3 ATR under R). The bar is placed by YOUR drawn class — DSGN 0.010 / MATX 0.087 / MSGS 0.148 / NOK 0.241 vs junk ENIC at 0.314 — **a ~0.06-ATR razor each side** (at 0.5 ENIC fired tier A and the junk guard went red; at 0.3 all 18 reject). **NOK fires 2026-02-13 tier B at your R to the penny, ordinary strict pool; shadow panel zero drift.** The census ran: conversions are SURGICAL (**8 of 1,241 no-reads** — KFY/EQH/KNSL/PGR/IPI/IMO/VTOL/UGI) but the drift leg found **12 of 309 current fires move**: rails/box/tier hold everywhere — the ELECTED LPS WINDOW changes (a newly-legal later ceiling rest wins the elector), re-pointing the trigger (CSX 51.88→51.70, XOM 162.81→161.13, PH 1023.68→1012.16) and a few score points. Arguably the more terminal read, but moved buy levels on ~4% of live picks are yours to eyeball, with the razor. | NOK catching live; the corpus's most common terminal form | [miss_program_2026-08.md](miss_program_2026-08.md); [flag_ledger.md](flag_ledger.md) row |
| 2026-08-26 | **Last Supper as a TYPED EVENT — go / no-go on the program.** You asked for *"the algorithm to know it exists as an event and to not be afraid of it."* The second half is DONE (the chip left the Warning group; it never cost a point anyway). The first half is not: the engine measures over-extension GEOMETRY around the LPS and deliberately does not label the run-up as a dated event, so it cannot say *when* a Last Supper happened, cannot place it before vs after the LPS, and cannot tell a chart that already ate its Last Supper from one that has not. Typing it is a real program — `event_map` reserves the column family (BACKLOG Task 12) and the R-rail wave machinery already produces the raw waves. It is also the natural home for the *"stocks popped off AFTER it"* claim: once the event is dated, the archive can measure forward returns from it instead of us both believing it. Scope + go-ahead needed; your drawn Last Supper marks would be its ground truth. | Dating the event; measuring what follows it | decisions.md 2026-08-26 Last-Supper row; `engine_alpha/structure/events/event_map.py`, [BACKLOG_2026-07.md](BACKLOG_2026-07.md) Task 12 |
| 2026-08-26 | **Calibrate the SOS decisiveness bar from DRAWN marks** (method switched by the operator the same day — see the ground-truth ruling in decisions.md; the mined-tails eyeball below is now the FALLBACK, not the plan). Ruled: decisive is part of the SOS definition (quick + covers a lot of ground; multi-bar allowed), but the bar itself is unset. The path: ship the workbench's SOS / Last Supper / Mini-consolidation event marks, the operator draws real specimens, then the stat-card lane measures HIS marks to place the floor — never a threshold proposed off engine output. Fallback if he prefers the table: every current SOS wave is located with dates+prices (2026-08-26 session) — a shallow tail (PH 06-29→06-30 +2.7%, GCMG, MTN +2.8% story anchor, ITT, XRN), a grind tail (RMBI 0.38 box in 17d; XRN +10.9%/16d anchor), the decisive contrast set (LECO +18.7%/12d, SHW +23.6%/11d), and the boundary cases that decide whether SPEED counts at all: MAIN +17.0%/26d, RRC +14.8%/20d — big ground, slow climb, story anchors today. Either way the change lands via EC-48 (flip counts + ratchet + shadow on a branch). | The SOS decisiveness correction (sweep P2-7 continuation); strategy_alpha's recorded-not-implemented gap | decisions.md 2026-08-26 decisiveness + ground-truth rows; the located tables in the 2026-08-26 session |
| 2026-08-25 | **The 166.6 MB error log is now INERT — rule on deleting it**, and the elevated `nssm set ChrolloDashboard AppRotate…` block (now a BACKSTOP, no longer the remedy). Measured after both redeploys: 1 line since the last restart, the startup banner. Nothing is writing to it; it is 166.6 MB of history. NSSM holds the handle, so it needs the service stopped (or an online rotate) — an elevated action, yours. | Disk (166.6 MB) | decisions.md 2026-08-25 rows |
| 2026-08-23 | **Type names**: rule the operator-facing names for the two chain lanes (proposals ride the plan's Task 1, drawn from your own words — "base on base", the shakeout-recovery family, "tactical long"). Wire keys and archive columns freeze forever, so nothing serializes before this ruling. | Plan Tasks 8/13/14 (archive, profiles, wire) | [pattern_register.md](pattern_register.md) rows 8–9 |
| 2026-08-23 | **Chain specimens**: round 1 (40 candidates) **RULED ALL-JUNK 2026-08-25** — the crude screen adversely selected deal-pinned/buyout stocks (gap → near-zero spread and volume). The ask stays open: a round-2 sheet is owed once the miner grows a deal-pinned exclusion — or name 2–3 of your own charts per chain directly, which short-circuits the mining entirely. | The wall fixture (plan Task 3) + every threshold the program will propose | decisions.md 2026-08-25 row + **[story_chain_candidates_2026-08.md](story_chain_candidates_2026-08.md)** |
| 2026-08-22 | **Graduation sitting**: the live marks DB holds 34 marks (+1 beyond the sealed 33) and both fingerprints drifted past the pin ~2026-07-27 — review the delta, re-approve, re-pin. The Trigger-coverage seal gap (decisions.md 2026-08-20) can ONLY close inside this event, so the recipe widening rides it. | The engine's test standard ingesting your newest ground truth | `python -m tools.regression.marks_corpus --check` (the advisory line) + the workbench |
| 2026-08-22 | **Signal-edge program: live or die?** 2,246 lines of honest-edge measurement parked on `wip/signal-edge-backtest` since 2026-07-13. | Pattern Register row 6 | [pattern_register.md](pattern_register.md) row 6 |
| 2026-08-22 (re-measured 2026-08-31) | **Trend-terminal kill-by — the measurement now says DELETE; your word closes it.** Re-run on current arithmetic (payload 2026-08-27, 309 names, post-re-key): **291 identical / 17 lost / 1 moved** (was 249/21/1 on 271 names), split 12 class A / 5 class B. Three findings decide it. **(a)** The box-blindness fingerprint REPLICATED on a third disjoint cohort, after the only repair that has landed: **5 of 5 class-B losses still have their claimed climax above the box's own R, median +0.56 box heights** (was 9 of 9 at +0.45). **(b)** The loss cohort has turned over COMPLETELY for the third time running — **zero shared tickers between any two A/Bs** — so this evidence can never bank, and every future re-ruling needs a fresh sitting on names nobody has seen. **(c)** None of IRMD/IART/CYRX is still a loss (IRMD and IART are out of universe; CYRX now reads identical), so your 2026-07-28 KEEP ruling cannot be re-tested on this cohort at all. Deleting is cheap and runtime-neutral — the gate lives entirely inside `if terminal_floor is not None`, which narrative sets to None while the flag is off — and the expensive part **survives**: `market_structure.trend_terminal_floor` is unconditionally live feeding the Phase-A climax polarity re-key. What retires is a ~33-line filter, a ~35-line helper, one settings key, one manifest row, one instrument, one test module. **The honest counter:** 7 of today's 12 class-A losses have their climax essentially ON the box ceiling (≤ +0.20 box heights), which is what a genuine "the trend topped and the box is the reaction" read looks like — so the gate is probably right about most of what it removes. That argues the DOCTRINE is right, not that this code should be carried; the doctrine is safe either way in decisions.md (2026-07-27 / 2026-08-14, append-only). **Kill-by re-dated 2026-08-31 → 2026-09-15 with this measurement as the written reason** — the ledger's law satisfied today, not silently carried — and the blocker is now a tracked program ([pattern_register.md](pattern_register.md) row 10) instead of a phrase. Answer DELETE, or open the box-blindness program with a date. **Still owed separately:** your 2026-07-28 IRMD/IART/CYRX KEEP ruling has no decisions.md row — it survives only as prose in the ledger. | `TREND_TERMINAL_BOX_GATE_ENABLED` | [flag_ledger.md](flag_ledger.md) row + [pattern_register.md](pattern_register.md) row 10 |
| 2026-08-22 (re-measured 2026-08-31) | **AR flag — the owed re-measure is DONE, it settles the flip a second time, and what is left is disposal.** Against the re-keyed climax: **101 of 335 firing overlays re-anchor** (was 100 of 291), OFF span median 39 → 47, ON 7.5 → 5, **tighten median 19 → 35 bars**. An isolation run varying only `bricks._cause_is_up` on one cache attributes that drift to the re-key itself (legacy polarity 121/335 at tighten 22; re-keyed 101/335 at 35) — **the flag now touches fewer charts and disagrees by more on the ones it touches**. The DO-NOT-FLIP mechanism survives intact: the drawn OFF `ar_bar` still equals `box.start_bar` on 5 of 6 live reads, `segment_trends` still finds your trend end on only 2 of 9 marks, the climax is still early on 5 of 7 (median −18, was 6 of 6 at −50.5), and 27 of 101 movers still sit at the fixed 60-day lead-in ceiling — the re-key changed which END of that window is taken and left the window alone, exactly as the 2026-08-19 diagnosis predicted. On your own marks the flag is now a NO-OP on 3 of the 4 live reads and loses the 4th badly (EC: 6 bars off vs 65 on). So the question is no longer whether to flip: **delete it, or keep it dark purely as the anchor program's measuring instrument** — `tools.ar_first_reaction_diff` and half of `tools.research.operator_marks_diff` measure the anchor defect BY toggling this flag, and the climax-anchor program is still open. **No re-date: the kill-by stands at 2026-09-15 and DELETES if unruled** (the row already forbids a second hopeful carry). | `AR_FIRST_REACTION_ENABLED` disposal | [flag_ledger.md](flag_ledger.md) AR row |
| 2026-08-22 | **June forming-bar era** (2,299 rows, 2026-06): cohort-shift impossible, per-row evidence preserved — rule the row-grain pass in, or accept the era as-is. | Archive history hygiene | decisions.md 2026-08-11 row |

## Closed

| Asked | The question | Resolution |
|---|---|---|
| 2026-08-30 | **Promote the bar-state census + rail-area experiment to committed instruments (or rule the gap closed).** The two 2026-08-30 rail-area rulings cited session-scratchpad evidence that nothing committed could reproduce (EC-16 gap, caught at the pre-merge council review). | **CLOSED 2026-08-31 — both halves promoted, both reproduce.** [`tools/research/rail_area_census.py`](../tools/research/rail_area_census.py) + [rail_area_census_2026-08.md](rail_area_census_2026-08.md) reproduce **25 of 25** cited figures (the four distance-blind cuts at 0.44–0.53; 86% / 75% containment at ±0.5 ATR; the 1.19× vs 3.42× unit test; 7-of-22 and 11-of-16 sitting above the rail; 1.73 vs 3.05 ATR box heights; the 1.267/0.349 and 1.089/0.000 pierce maxima; 1 ATR = 0.403 box heights at n=3,407) in ~50 s, one command. [`tools/research/bar_state_census.py`](../tools/research/bar_state_census.py) + [bar_state_census_2026-08.md](bar_state_census_2026-08.md) reproduce every cited bar-state figure exactly in ~22 s (the six-rule harshness grid cell for cell; 8 left-up / 3 right-edge / ZERO crash-back; 28 of 48 semi crash-backs; 79 of 89 support reclaims; 16 of 20 true rests overlapping in_progress waves; the 17/35 vs 18/98 seller's tail; 8.8% vs 2.4% ceiling engagement). Both ran on the sealed marks fingerprint `92d6a529…` and both reproduce ACROSS a manifest rotation, so the evidence is not epoch-bound. Sidecars force-added past `output/*.json` per EC-53. |
| 2026-08-22 | **`claude/focused-ellis-499011`**: locate the cloud session carrying the lens-tweaks build, or declare it dead. | **CLOSED 2026-08-31 — never stranded: it landed on main three weeks ago.** The reflog is decisive — the branch was merged into main TWICE, `0d671f4` (2026-08-10) and `38f8e18` (2026-08-11, "the lens tweak list"), and both are ancestors of origin/main today. The lens merge carried `5f084a1` (W/M mini-charts), `00475fc` (grid 17→8 + sector-as-ETF) and `100846e` (rich chip tooltips restored) — 9 files, +353/−59. All four lens items verify LIVE in the current tree: `ScreenerStockLens.jsx:94-103` holds exactly 8 cells (17 at `00475fc^`), `TimeframeMiniRow.jsx` is mounted at `ScreenerModal.jsx:224`, `tagTooltips.js` is wired at `tagResolver.js:51`, and `sector_etf` renders at `ScreenerStockLens.jsx:65/101` and `ScreenerCard.jsx:148-152`. All 33 dangling commits were catalogued; none carries lens work, because nothing was lost. **Root cause of the false alarm:** the ask was raised on 2026-08-22, eleven days AFTER the merge, by inferring "stranded" from the missing worktree directory rather than from a git ancestry check — and a merged-then-deleted branch looks identical to a stranded one from the filesystem. The lesson: prove strandedness with `git merge-base --is-ancestor`, never with a missing directory. |
| 2026-08-30 | **Sign the LAST word: what do we call the departure wave (emitted type `markup`)?** Proposal was **departure**. | **CLOSED same day — markup SIGNED verbatim** once explained in plain words: *“ahh I see then yes we can use markup.”* The word table is signed in full; the departure proposal dropped. decisions.md 2026-08-30 closing row. |
| 2026-08-30 | **Sign the ceiling position label + rule the touching-both caveat.** Proposal “top of the base”; caveat = the ultra-tight base. | **CLOSED same day — “at resistance” signed** (*“the top of the base is the Resistance line”*) **and touching_both RULED a fourth value** (differentiate the correct upper-vicinity read from a base one bar’s worth of height). Serving landed the same sitting: wire + four engine-resolved position chips, labels from POSITION_LABELS; CVI is the one live flip. decisions.md 2026-08-30 closing row. |
| 2026-08-30 | **Sign the ONE-vocabulary word table** — touch_and_pivot, support_test + verdict, open-at-the-right-edge, held/gave axis. | **CLOSED 2026-08-30 — signed, with one amendment and one remainder.** touch_and_pivot ✓ (and widened: all three ceiling states that pivot later fold into it); the breach verdict re-worded **breached** (his official term, one word both rails — “gave” retired from the axis); support_test + verdict ✓; open-at-the-right-edge ✓. Remainder = the markup/departure word (successor row above). decisions.md 2026-08-30 word-table row. |
| 2026-08-30 | **Sign the position labels before they reach the wire** — “at the ceiling” / “mid-range” / “on support” defaults offered. | **CLOSED 2026-08-30 — two of three signed, the third re-posed.** `mid_range` = **“middle of the base”** (clear of both rails; his tight-parent touching-both caveat recorded), `on_support` = **“on support”** (*“pretty self explained”*). “At the ceiling” REFUSED as the area label — he defines it as a bar STATE (below R, touching/almost), so the area needs its own word (successor row above; proposal “top of the base”). decisions.md 2026-08-30 row. |
| 2026-08-28 | **MDT is a BOTTOMING BASE — rule the scope.** Catching it means Chrollo reads Stage-1 accumulation; with both sma200 layers held open the engine elects R 82.83 — the drawn resistance to the penny. | **CLOSED 2026-08-29 — ruled the catch direction: "I don't want to miss them."** Built dark as `BOTTOMING_BASE_LANE_ENABLED` (both sma200 layers open when the 50-day is reclaimed); MDT fires 2026-07-14 tier S. decisions.md 2026-08-29 row; successor ask = the flip (above). |
| 2026-08-28 | **The LPS ceiling-rest family — go/no-go on the program.** NOK elects boxes at the drawn rails and dies at the LPS; shelf harness blesses 10/36 drawn LPS at drawn basis. | **CLOSED 2026-08-29 — ruled go by "I don't want to miss them."** The minimal member shipped dark: `LPS_CEILING_REST_ENABLED` sanctions the launched-above straddle resting within 0.3 ATR of R (drawn-placed bar, ENIC razor pinned); NOK fires 02-13 tier B. The WIDER family (window-spans-box ×8, not-rest-on-low ×7 at drawn basis) stays open as future drawn-marks work. decisions.md 2026-08-29 row; successor ask = the flip (above). |
| 2026-08-26 | **"Last Supper" now names THREE different things — rule the vocabulary before the mark ships.** The drawn mark (his "version of BU"), the engine's over-extension warning chip, and his own 2026-08-23 "final-shakeout class" ruling all wore the name, pointing in different directions. | **CLOSED same day — the question dissolved.** He confirmed the engine's definition ("the final run-up that traps late buyers before the real pullback") is CORRECT and that it can sit before OR after the LPS: one concept, not three. The mark SHIPPED as `last_supper`; separately he ruled it is **not a bad thing** ("many stocks popped off after said event"), so the chip left the Warning group. decisions.md 2026-08-26. Successor ask: event-TYPING (see the row above). |
| 2026-08-26 | **SOS-without-a-breakout (function-sweep P2-7): a reading-model ruling.** The event reader types "SOS" on hold-quality alone; `breached` computed but never consulted; docs/story claimed "break above R held". Census: 58/96 live SOS events unbreached; 29/52 story anchors (18 below the rail, 11 inside the 0.5-ATR buffer). | **CLOSED same day — operator: "A."** The read stands: the held high test IS the strength, breakout not required. Words corrected everywhere they claimed a break (classifier docstring + story line, strategy_alpha, structure_legend); wyckoff_canon §5 gains the SOS false-friend row; decisions.md 2026-08-26 row carries the census. Requiring a breach (options B/C) is measured-and-declined. |
| 2026-08-26 | **Pivot-collapse correction (function-sweep P2-6): rule the flip.** The swing walk deleted a COMMITTED swing whenever a small counter-pivot followed (permanently at the window tail), against its own documented absorb rule. Fully measured on `fix/pivots-collapse-absorb`: same 32 fire, zero election flips, ONE number — GF 96.4→96.2 (rank 20→22). | **CLOSED same day — operator: "sure go ahead."** Merged to main as the EC-48 flip commit; shadow baseline recaptured with the GF drift declared; branch deleted. Ruling row: decisions.md 2026-08-26. The sweep's group-3 queue now proceeds one at a time (SOS-never-requires-breach next). |
| 2026-08-22 | **Redeploy + log rotation** — make the retirement, the log-flood fix and the dependency bump live. | **CLOSED 2026-08-25 — operator redeployed twice, and both rounds were VERIFIED IN PRODUCTION rather than assumed.** Round 1 proved the stderr half (5 lines in the error log since restart, zero `chrollo.request`) and FALSIFIED the rest: `uvicorn.access` was a twin with no quiet list, so the flood moved to stdout — 2,296 of the next 3,000 lines. Round 2 silenced it. Controlled test after: 3× `/health` (quiet) → **0 lines**, 1× `/screener-data/` (not quiet) → **exactly 1 line**, ours, with request id and duration; zero uvicorn twins; 100 bytes for four requests where the old path wrote five lines. CI green on main twice running. |
| 2026-08-23 | **Story-chain build: go / no-go** — the 15-task council plan for disjointed setups (base on base + after-shakeout recovery as typed lanes). | **CLOSED same day — operator: "go."** Build opened (council-implement run `2026-08-23-1420`); program record: [story_chain_program_2026-08.md](story_chain_program_2026-08.md) |
