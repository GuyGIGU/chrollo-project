# Archive edge read — 2026-07-22

**Reproduce:** `python -m core.archive.analyze` (read-only) against
`webapp/backend/trading_journal.db`.
**Provenance:** committed so the finding is reproducible instead of memory-only
(supersedes the un-committed 2026-07-15 read; see [[project_edge_read_2026-07-15]]).

## Sample & validity

- **1,977 episodes** (deduped from 6,491 raw rows — 4,514 continuation re-flags
  collapsed to the first-seen anchor, the same grouping /calibration uses).
- **Validity verdict: OK** — adequate for outcome + correlation analysis.
- 867 / 1,977 carry `fwd_return_20d`; **44** carry the full 60-bar window
  (only rows scanned ≳87 calendar days ago can — the 60d fill is healthy, not
  empty: 43/43 of the pre-April rows are fully matured).
- Tiers S 1084 / A 639 / B 241 / C 13. Sources screener 1937 / seed 39 / manual 1.
  Types LPS 1814 / REBOUND 163.
- **REGIME CAVEAT (load-bearing): 1,940 BULLISH vs 12 NEUTRAL, 0 bear.** Every
  number below is one bull regime. The archive alone cannot answer
  "survives a bear"; that needs the on-disk 5y replay cohort.
- Survivor-only cache: delisted / dead names are absent, so realized returns are
  a conservative-optimistic read (winners over-represented).

## Tier ranking still holds (S > A ≳ B)

| tier | n | med 20d | med 60d | win% | trig% | avgR |
|---|---|---|---|---|---|---|
| S | 1084 | **+5.3%** | +25.6% | 73.0% | 92.5% | **1.58** |
| A | 639 | +4.0% | +12.3% | 63.2% | 88.4% | 1.45 |
| B | 241 | +4.0% | — | 65.8% | 87.6% | 1.37 |
| C | 13 | (n too small) | — | — | 88.9% | — |

S is cleanly best on every column; A/B are close on 20d but S/A/B is monotonic on
avgR. The tier score is doing real ranking work.

## Prime-directive tightness test — MIXED (unchanged verdict)

Tighter-tertile vs looser-tertile mean `fwd_return_20d`:

- **Supports:** `tightness_ratio` +2.3%, `base_median_spread_atr` +2.2%,
  `atr_ratio` +1.2%, `bin_d_ascending_support_quality` +3.2%,
  `trav_n_full_traversals` +4.1%, `box_width` +0.1%.
- **Contradicts:** `base_tight_bar_pct` −2.8%, `trav_top_dead_space` −1.1%,
  `lps_descent_frac` −0.5%, `bin_d_vs_b_range_ratio` −0.4%,
  `contraction_quality` −0.3%.

Read: *worked-equilibrium* tightness (traversal count, spread-vs-ATR, dwell
quality) supports the thesis; raw bar-texture proxies do not. Tightness is a
structure-QUALITY / risk signal, not a clean reach predictor — consistent with
the 07-15 read.

## Signal edge — sub-score subtraction shortlist (MEASURE-ONLY)

Primary outcome `durable_win` (n=654, minority class 313, noise floor |r|<0.15;
341 durable wins vs 9 cash-grabs). Rank-association of each scoring sub-score with
the realized outcome:

| sub-score | rank_r | verdict |
|---|---|---|
| score_base_age | **+0.33** | beneficial |
| score_atr_squeeze | +0.17 | beneficial |
| score_high_proximity … score_box_tightness | +0.12 … −0.05 | inert (9 terms) |
| score_adr | −0.18 | **harmful** |
| score_uptrend_bonus | −0.19 | **harmful** |
| score_rs_bonus | −0.22 | **harmful** |

Extends 07-15 (rs_bonus harmful, base_age beneficial) on a larger, gate-passing
sample and adds `uptrend_bonus` and `adr` to the harmful list, `atr_squeeze` to
beneficial. Nine terms are inert (candidates to trim), incl. `box_tightness`.

> **DOCTRINE — no action taken here.** This is a subtraction *candidate* list, not
> a re-weighting. Per AGENTS.md, weights/thresholds do not change without being
> asked, and never on one regime. The rs_bonus/uptrend/adr "harm" is confounded
> with a single bull regime (momentum names lag in a broad rip), so it must be
> re-cut per `spy_trend` on a regime-spanning cohort before any weight discussion.

## Other observations

- **Top 20d correlates:** `htf_w_box_width` +0.291, `last_supper_reclaim_quality`
  −0.262 (higher reclaim quality → lower forward return — the 07-15 sign holds),
  `lps_swing_depth_pct` +0.209, `breadth_pct` +0.195.
- **By LPS form:** `buec_shelf` +62.3% (n=15 — the EGBN above-R/high-shelf family,
  tiny sample, promising), `terminal_valley` +16.7% (n=697, 95% win),
  `undercut_rebound` +13.5% (n=93, 100% win), `clean_downswing` +15% (n=99).
- **By breadth:** low +0.5% / mid +4.4% / high +7.1% — outcomes track market
  breadth strongly (another single-regime confound to control for).
- **Data-quality flag:** 11 episodes with `base_length` > 250 bars
  (CACC 325, MSCI 429/433, BMRN 296, …) — almost certainly anchor mis-detection
  picking an over-long window; excluded from nothing here, worth a look.

## What this read does NOT do

- No **base-rate null** ("better than darts at the same eligible universe?") —
  `analyze` is descriptive; the null lives in `core/backtest/null_model.py` and is
  a separate run.
- No **regime split** — the archive is 100% bull; the bear answer needs the replay
  cohort. These two are the next measurement step (see [[project_edge_read_2026-07-15]]).
