# Flag ledger — dark engine flags and the decision each is waiting on

Every flag-gated engine capability that shipped **default-off** is listed here
with the decision that blocks its flip and a **kill-by date**. A dark flag is
not free: it is a dead branch in the reader, a row in the freeze manifest, and
a standing "we'll validate later" IOU. By its kill-by date a flag must be
**flipped live**, **deleted**, or **re-dated with a written reason** — never
silently carried.

Maintenance rules:
- A new default-off engine flag lands with a row here **in the same change**.
- **It also declares its OFF-state acceptance in the same change**: a named entry in
  `tests/contracts/test_inert_contract.py`'s `DECLARED_ADDITIVE_FIELDS` — the exact result-row
  keys the lane may ADD, or the EMPTY set when its whole surface is the canonical
  fields the shadow guard and the ratchet already own. THIS list is the enrollment
  rule, stated here because the test's own "all declared" assertion compares a dict to
  a literal set in the same file and so cannot catch a lane that never enrolled
  (council review 2026-09-01, finding 11b). A dark lane with no declared additive set
  has no inert contract to re-scope at its dissolution.
- Flips/deletions move the row to *Retired* (keep the evidence trail).
- Live flags (`TIGHTNESS_ADR_AWARE`, `HTF_CONTEXT_ENABLED`, ...) are
  deliberately NOT here — they are engine identity (frozen in
  `engine_alpha/freeze/manifest.py`), not pending decisions. A live flag whose
  validation window has closed gets **FOLDED** (the conditional is removed and
  the behavior becomes unconditional engine code; the settings key survives
  untouched until the next manifest rotation retires it) — the weeks-validated
  2026-06/07 flips (`TRAVERSAL_GATE_ENABLED` first, 2026-07-18) fold this way
  under the Purity Pass.

## Dark flags (default-off, decision pending)

| Flag | Built | Blocking decision | Evidence | Kill-by |
|---|---|---|---|---|
| `FUNDAMENTALS_ENABLED` (Lane C) | 2026-06-29 (`b8753a1`) | The scoring/enrichment wire-up wave (fundamentals feeding score/archive) — substrate is built and unit-tested, consumption wave not yet scheduled | `core/fundamentals/` docstrings | 2026-09-30 |
| `RS_LINE_ENABLED` (Lane C) | 2026-06-29 (`b8753a1`) | Same wire-up wave (RS-line new-high chip / score input) | `core/regime/rs_line.py` docstring | 2026-09-30 |
| `SECTOR_RANKING_ENABLED` (Lane C) | 2026-06-29 (`b8753a1`) | Same wire-up wave (sector-RS context for the health board / regime label) | `core/regime/sector_ranking.py` docstring | 2026-09-30 |
| `ELECTION_TRACE_EXPORT_ENABLED` | 2026-08-04 (Surface the Read Task 4) | **The walk's own narration, published:** `read_structure`'s trace captured during the SAME election that fired (never a re-run) and summarized engine-side (`structure/box/trace_export.py`) to a compact date-anchored story — per-root climax/AR dates + outcome + per-stage refusal counts + how far the best candidate got (`terminal_verdict`, the ONE summarizer) + the elected framing's provenance; gate-leg sentences render from the structured leg records through the one operator-language vocabulary (`leg_sentence`), never internal `detail` prose. The RAW trace never leaves the engine — measured 2026-08-04 over 40 real fires: full-trace JSON median ~106 KB / p90 3.6 MB / max 5.8 MB per ticker. Archived raw per fire (`election_trace` TEXT, model-only column; NULL = never captured, never backfilled) and served in the payload narrative block (parsed). Changes NO election/gate/score — capture cost only: **measured +20.3 ms per evaluated ticker ≈ +111 s per full US-Stocks scan** (trace-off 477.1 vs trace-on 497.4 ms/t, n=40 live prep). **Blocking decision:** operator flip after the bound re-measures in the scan-metrics evaluation phase on a real nightly scan. **That is the identical circular precondition the consolidation-method flags carry, and EC-49 already rules it — this row is the standing casualty EC-49 was written about (dark since 2026-08-04 on an unmeetable gate).** The choreography now exists and applies here unchanged: [consolidation_method_2026-09.md](consolidation_method_2026-09.md) §Task 14, "The trial scan" — one armed night, the bound named before it, a one-word revert, the trial night's rows kept and disclosed. The read is `market_context._scan_metrics.phases_s.evaluation` in `output/screener_data.json` (the persisted key is `phases_s`, never `phases`) against the preceding US-Stocks lines of `output/scan_metrics.jsonl`; note that the measured +111 s/scan claim above is ~1.5× the ~70 s night-to-night spread those dark nights already show, so this flag's bound is the one case here where a single night genuinely resolves the number | `settings.ELECTION_TRACE_EXPORT_ENABLED`; [engine_reference.md](engine_reference.md) "The narrative fact block (Surface the Read)" | 2026-09-30 |
| `STRATEGY_READ_ENABLED` | 2026-08-04 (Surface the Read Task 13) | **Held-through-correction, measure-first:** two RAW campaign-context measures per fire (`structure/context/strategy_read.py`, family pattern): `strategy_correction_depth_pct` (how deep the elected base's floor sits below the resolved climax bar's high, fraction of that high — SIGNED: negative = the floor holds ABOVE the climax high, the selling-climax recovery shape; ALB is the live example) and `strategy_floor_above_ar` (0/1: the floor held at/above the automatic reaction's low). Reads ONLY facts the walk already resolved (climax/AR bars + elected S) — no new detection, two array lookups per FIRE, expected evaluation delta ≈ 0 s. Never gates, never scores, NULL = never measured; **a ruled judgment over these is a LATER archive calibration, deliberately NOT an add-time threshold** (measure-first doctrine). EC-17 happy path committed on the story-pool-guard template (real cascade, only this flag forced on, flag-off leg byte-identical per identity). **Blocking decision:** operator flip after the ScanTimer re-measure on a real nightly scan — the same circular precondition, so the same choreography applies: `conventions.md` EC-49 as executed in [consolidation_method_2026-09.md](consolidation_method_2026-09.md) §Task 14, "The trial scan", read at `market_context._scan_metrics.phases_s.evaluation` (the persisted key is `phases_s`, never `phases`). Honest caveat for THIS flag specifically: its expected delta is ≈ 0 s (two array lookups per fire), which is an order of magnitude under the ~70 s night-to-night spread the dark `evaluation` phase already shows, so a trial night can only prove "nothing exploded" — it can never measure this cost, and the bound named before the night should say so rather than pretend to a number | `settings.STRATEGY_READ_ENABLED`; [engine_reference.md](engine_reference.md) "The narrative fact block (Surface the Read)" | 2026-09-30 |
| `ELECTION_STABILITY_ENABLED` (+ `ELECTION_STABILITY_LOOKBACK`) | 2026-07-11 (Calibration at Scale Task 14) | Measure-only persistence probe on FIRING setups: re-run the eval-twin prep + the structure election alone at D−1..D−k (backward ONLY — no lookahead can enter the archive) and grade "same reading?" via the one cross-frame identity predicate (`engine_alpha/election_identity.same_election`: box-start DATE + rails within 0.10 box-height). Emits `_stability_same_frac`/`_streak`/`_probes`/`_refused` (underscore diagnostics; archive family is a later stage; `_refused` counts prep-refused shifts — universe-gate flicker — apart from election flicker, though both grade not-same in same_frac). Evidence: junk flickers (BODI's flag-ON band pair exists 04-15, dies 04-16) while real boxes persist. **Measured cost (the flip evidence, k=3, corpus fixture fires): mean ≈2.75s per firing ticker** (each probe = full twin prep + election; ~+4.5 min per 100-fire scan) — a flip likely wants k=1..2 or an election-only fast path measured first. HONEST SEMANTICS NOTE (corrected per Council Review 2026-07-11 finding 9): the probe measures backward persistence of the BOX election, not the fire — a box can elect well before its LPS completes, so same_frac at a first fire can be anywhere in 0..1 (corpus fires measured mostly 0 = the election churned into those fires; AVT 0.33 = pre-fire persistence exists). Interpret at calibration time, never as a same-day quality gate. Inert battery: flag-off probe NEVER consulted (raise-if-touched), deterministic raw values, ledger+manifest registered. **FLIP PRECONDITION ADDED 2026-09-01 (council review round two) — it only binds if a rescue lane is live too.** The probe's k backward `read_structure` calls run INSIDE the one evaluation (the `ELECTION_STABILITY_ENABLED` branch of `engine_alpha/evaluation.py`'s `_score_eval_context`), and that is exactly where `evaluate_ticker_with_rescue_stats` arms the per-process rescue sink. With this flag and either rescue flag both live, a backward shift that fully refuses would escalate and book into the SAME sink — k extra walks summed into `rescue_ms`, `escalated_forms`/`elected` overwritten last-write-wins — which is finding 1's pathology with a different second reader, on the very night a flip's cost number is read. Not a live defect (both dark); the fix is the species lane's, one scoped `narrative.rescue_sink(None)` around the probe's walks. Tracked as [pattern_register.md](pattern_register.md) row 14 (EC-50) | `engine_alpha/stability.py` (identity predicate: `engine_alpha/election_identity.same_election`); committed battery `tests/engine/test_election_stability.py`. *(Evidence pointer repaired 2026-08-20: this row cited `../PLAN-calibration-at-scale.md`, a gitignored root plan that resolves nowhere — EC-16 requires committed pointers, and the module paths still read `core/pipeline/` from before the Engine-α extraction.)* | 2026-09-15 |
| `POWER_PLAY_STORY_FORM_ENABLED` | 2026-08-17 (species program Task 6) | **The resistance contraction as the SECOND named ruled form inside the ONE story admission** (`event_map.resistance_contraction_admission`, EC-18 — never a species-local predicate; renamed from "holding shelf" by operator naming ruling 2026-08-18, decisions.md — the record says the behavior, and note the untangled collision with the LPS layer's own live `holding_shelf` swing type): floor never FAILED + no terminal S-drift + the right edge ENGAGED at the ceiling (an open R episode; a terminal `R^` bar produces the same open episode, so the post-breakout stance is covered). The story pool's O(1) prefilter grows the form's exact necessary condition (`last_high >= R − tol`) behind this flag; an admission NAMES the measured behavior in `story_admission_profile` — "contracting above resistance \| <profile>" (last close above the rail) or "contracting at resistance \| <profile>" (pressing from below), via `resistance_contraction_label` — while the S-test form's record stays byte-identical. **A separate gate from the lane chooser BY DESIGN:** the species lane toggles this under the ONE scoped override (`htf.window_override`, riding the preset dict) around its OWN election only, so the paying read can never consult the shelf form even after the lane flag flips (the build-time row said `flag_capture` — corrected 2026-08-17 review; `flag_capture` now DELEGATES to `window_override`, one save/restore core). **The form is PROVISIONAL** — its legs are the operator's eye model + the MAN diagnosis, and the ruling sheets (program Task 4, **generated 2026-08-18**: S3 probes what the form admits, S4 the best book-valid poles it refuses — at clock 8's first legal look the form admits 83 and refuses 1,591) calibrate or re-rule it. **Its EC-17 acceptance did NOT land in Task 10** (the frozen pair pins only `refused_clock`/pending — the 2026-08-17 review, finding 3, caught this row promising a safety net that did not exist) **and LANDED POST-REVIEW**: `tests/engine/test_power_play_lane.py` drives the real cascade to a contraction-form `admitted_dark` with input-tied assertions + a mutation probe, and every `pp_state` has a real-producer case (EC-22). **Blocking decision — SHARPENED 2026-08-19, and it is now a single named ruling.** The preset half FLIPPED LIVE this day (see Retired); this half did NOT, deviating knowingly from "the two flags flip together or not at all", because **with it ON the sealed Guided-List ratchet BREAKS: `EGBN` (2026-01-15 → fires 01-07, tier A) and `PKE` (2026-02-24 → fires 02-18, tier B), both recorded expected-misses of stage `rail-placement`, now FIRE.** Good news is still a ratchet break (the guard's own words: "verify the stage's acceptance evidence, then re-freeze"), and re-freezing the operator's sealed standard is HIS ruling, never an agent's. Isolation measured cleanly the same day: polarity-fix only → 28/33 PASS; + preset ON → 28/33 PASS; + this flag ON → FAIL on exactly those two. Worth putting to him WITH the conversion: **EGBN already carries an open condition-D ruling**, diagnosed ~85% "decisive high-shelf LPS" (price holding above the rail) — which is precisely what *contracting above resistance* names, so the conversion is plausibly the form doing its job rather than a leak. **ASKED AND ANSWERED — AND THE FLIP WAS STILL REFUSED, 2026-08-19.** He ruled both real: *"BOTH have setups on these days so yeah"*. That ruling STANDS as acceptance evidence for those two names, the ratchet WAS resealed at 30/33 — and then rolled back, because converting them is not all the flag does. **`tools.regression.shadow_diff` found canonical drift on a name nobody ever marked: WCC. The species form re-elects it into a box 2.2× WIDER (`Box Width` 0.0608 → 0.1346; rails R 368.90 → 374.00, S 347.77 → 329.62; `Base Len` 21 → 16), costing `Score` 110.8 → 96.9, `Tier` **S → A**, and 16 places of rank (3rd → 19th).** A looser framing winning election inverts the prime directive. Two unruled side-effects with it: three already-pinned hits fire EARLIER (NKTR 2026-04-09 → **2026-03-27**, MS 05-18 → 05-15, YPF 05-06 → 05-05), and the flip **breaks the very invariant this flag was designed around** — `test_paying_read_never_consults_the_species_form`, the settings comment's *"a passenger never touches the paying read"*. 12 guards failed in all. **Everything was restored byte-identical** (baseline, `STAGE_TAGS`, ratchet 28/33 PASS, shadow guard no-drift). **METHOD NOTE WORTH MORE THAN THE VERDICT: the Guided-List ratchet alone said PASS at 30/33 and would have shipped this. The shadow-output guard is what caught WCC — a ratchet only sees names the operator marked.** Run both, always. **The blocker is now a PROGRAM, not a decision:** graduating the resistance contraction into the paying read is a re-election change to live picks and needs its own A/B over the firing cohort — is the wider WCC framing a one-off or a class, and is it ever the better read? — with the EGBN/PKE ruling banked as its first acceptance evidence. The lane loses nothing meanwhile: the species twin enables the form inside its own scoped override regardless; this flag only ever governed the PAYING read | `settings.POWER_PLAY_STORY_FORM_ENABLED`; [power_play_program_2026-08.md](power_play_program_2026-08.md) Tasks 1/6 + "Council review + the fix program"; `tests/engine/test_resistance_contraction.py` + `tests/engine/test_power_play_lane.py` | 2026-10-31 |

| `CONTRACTION_RESCUE_ENABLED` | 2026-08-28 (miss program) | **The resistance-contraction form at FULL-REFUSAL scope** — the program the 2026-08-19 `POWER_PLAY_STORY_FORM_ENABLED` refusal named ("graduating it into the paying read needs its own program"), built as its answer: when the WHOLE root walk elects nothing (never after a cause-before-effect abstention, which is doctrinal and final), `read_structure` re-walks ONCE with the contraction form armed inside the story pool. Scoped to full refusals BY CONSTRUCTION, so the three 2026-08-19 refusal grounds are unreachable: an existing election can never be displaced (WCC keeps its strict box — the rescue never runs on a ticker that reads), pinned hits keep their fire dates on every session where they currently elect, and the species passenger invariant stands (`test_paying_read_never_consults_the_species_form` pins the species FLAG; this lane is its own named flag and its own scope). Frozen-frame A/B (docs/miss_program_2026-08.md): **converts exactly the two operator-ruled conversions — EGBN@2026-01-15 fires 01-07 tier A on his rails, PKE@2026-02-24 fires 02-18 tier B** ("BOTH have setups on these days so yeah", 2026-08-19); NOK does NOT convert (its wall is the LPS ceiling-rest family, measured in the program doc; ST converts through the door lane); negative corpus stays zero-fire; flag-off byte-identical (shadow `--check` green). Fleet census (`tools.research.miss_lane_census`, cache edge 2026-08-27): **32 conversions of 1,241 no-reads (2.6%; +10% on 309 standing fires), all self-naming story fires** — sidecar `research/evidence/miss_lane_census_2026-08-28.json`. **Blocking decision: the operator's eyeball of the 32-name sheet + flip vs the program A/B — THIS flag's own reseal is 28/33 → 30/33 with EGBN/PKE pinned by name (SKYT converts through the dip lane: 31/33 with both wave-1 lanes armed, 32/33 with all four — the program doc's isolated shapes; corrected at the 2026-08-30 review, EC-15), and the second-walk eval cost on refusing tickers must be measured on a nightly scan first — the instrument is BUILT (consolidation-method Task 10, 2026-09-01): the `rescue_lane_worker_s` ScanTimer pseudo-phase + the `market_context["rescue_lane"]` attempt block, present only on nights the lane runs (`tests/pipeline/test_rescue_transport.py`). That cost precondition is circular by construction — the pseudo-phase only records on a night the lane RAN, i.e. flag-ON — so the night itself is a declared TRIAL SCAN **under `conventions.md` EC-49** (*"a live-only cost bound is measured by ONE named trial scan with pre-agreed revert"*, codified 2026-08-22 and cited nowhere until this review): who arms the flag, the bound named before the night (proposed: ≤ 5 min of summed `rescue_lane_worker_s`), where it is read the next morning, and the one-word revert are written down in [consolidation_method_2026-09.md](consolidation_method_2026-09.md) §Task 14, "The trial scan" (council review 2026-09-01, finding 7). **The morning-after read, exactly** — the persisted key is `phases_s`, never `phases`: `output/screener_data.json` → `market_context._scan_metrics.phases_s.rescue_lane_worker_s` (the US-Stocks payload, so nothing has to be disambiguated), with `output/scan_metrics.jsonl` for the preceding nights — a MIXED-UNIVERSE log whose last line is the 29-ticker ETF scan, so pick the US-Stocks record by `counts.universe_tickers`. Read the number as SUMMED IN-WORKER seconds, not wall clock: on the live 2026-08-31 scan the sibling `power_play_lane_worker_s` read 231.72 inside a 145.93 s `evaluation` phase. An ABSENT key means the lane never ran. The instrument was made honest for the planned flag combination in the same review: the sink no longer books the species lane's scoped DARK read alongside the paying walk (finding 1)** | `settings.CONTRACTION_RESCUE_ENABLED`; [miss_program_2026-08.md](miss_program_2026-08.md); [consolidation_method_2026-09.md](consolidation_method_2026-09.md) §Task 10; `engine_alpha/structure/narrative/reader.py` read_structure escalation; `tests/engine/test_miss_program_lanes.py` | 2026-10-31 |
| `BAR_POSTURE_RESCUE_ENABLED` | 2026-09-01 (consolidation-method Task 7) | **The S-test form's ceiling leg in the operator's unit, at the same full-refusal scope as the contraction rescue** — the measured bar-as-unit fix landed as a VERSIONED second form (`event_map.story_admission_bar_posture`: `terminal_r_engagement` — the bar's HIGH engages the rail zone — instead of `terminal_r_posture`, the close-basis leg), never an in-place edit of `frame_terminal_posture`, the ONE close-basis predicate the S-test prefilter and the episode reader both resolve through (its promotion counts are pinned; the wholesale basis swap is Tested-DEAD — the July dwell-gate form broke 10/26 identities and fired DGII/FLG). On a full refusal the escalation walk arms this form beside (or instead of) the contraction form — ONE re-walk, the explicit armed-form roster, no settings mutation. **Banked A/B (chair-verified 2026-08-31; `research/evidence/consolidation_evidence_2026-08-31/battery.log`): control all-green; candidate junk corpus 18/18 silent, shadow panel byte-identical on all 32 fixture fires, sealed ratchet breaks on EXACTLY EGBN (fires 2026-01-07 tier A) + PKE (2026-02-18 tier B) — both dates/tiers the operator ruled real 2026-08-19.** EGBN's whole miss is ONE close-basis leg (lower_dwell 0.118 vs the 0.15 floor; bar-basis 0.235 PASS; the floor defended 5×; `research/evidence/consolidation_evidence_2026-08-31/probes/`). **The full-refusal scope is what the fleet census dictated** (`bar_posture_census.json`, 5,532 tickers, cache edge 2026-08-27, wholesale in-memory swap): 308/309 standing fires byte-identical, 0 elections moved, **12 new fires (KFY BIIB VTR RCUS ICLR GEO VRTS CCEP BMY MSGS CARS AMCR — all full refusals at baseline ON THAT EDGE, so all were reachable through this scope THEN) and 1 lost — QTTB** (root-steal: an older root story-admits a wider frame, first-complete-wins ends the walk, the descent-tail gate kills the wide frame downstream) — **unreachable at this scope by construction** (QTTB reads at baseline; the escalation never runs on a ticker that reads). Registering the flag rotates the engine epoch (the declared vocabulary seam). **THE FLEET COLUMN PERISHED IN FOUR DAYS — RE-DERIVED 2026-09-01 at the 2026-08-31 cache edge** (`research/evidence/consolidation_evidence_2026-09-01/`: probe `probes/bar_posture_reachability.py`, sidecar `bar_posture_reachability.json`, log `reachability.log`; population fingerprint `1bd50279…`. **The morning-of command, repo root, dashboard venv interpreter — this machine's bare `python` is the documented two-installs trap:** `.\.venv\Scripts\python.exe research\evidence\consolidation_evidence_2026-09-01\probes\bar_posture_reachability.py`, ~60 s. **No engine stamp is quoted on this row deliberately** — the probe computes it inside its own run and writes it to the sidecar, and the epoch rotated twice on 2026-09-01 alone, so read it out of the re-run sidecar. It now writes **two**: `engine_manifest` (the machine, read fresh at run time) and `engine_manifest_at_measurement_pin` (the identity the printed numbers were actually measured under), with the boolean `measurement_identity_matches_machine` beside them. On the morning they agree, quote either; on a morning they do NOT, the one to quote beside the counts is `engine_manifest_at_measurement_pin`, because that is the engine the counts describe — and a difference with NO flag named in the advisory means a THRESHOLD moved, which no flag pin can see. **The whole sheet is measured at the PLAIN baseline** — and that is now pinned by the probe rather than inherited from the machine, across **every boolean the engine says can decide a reading, not just the lane flags**: the pinned set is derived from the engine's own identity roster (`engine_alpha.freeze.manifest.ENGINE_SETTINGS_KEYS`, whose completeness over the eval path is proved by `tests/engine/test_invariants.py::test_every_scoring_settings_symbol_is_in_manifest`), 29 flags held at this changeset's shipped values, so both rescue lanes are dark and the species preset is off as before but nothing else is left to the machine either. Classification runs under that pin, the conversion leg re-reads each reachable name with only this lane armed (through the one scoped override, self-restoring), the sheet asserts that the set it STAMPS is the set it COMPARES, and a machine whose ambient differs earns a loud advisory naming every moved flag by name (`measurement_flags.ambient_deltas`). A roster flag with no declared pin value aborts the sheet at import rather than printing an unpinned count. Why that pin matters at the sitting, measured 2026-09-01 on this cache and this population — **three flags move these counts, not one**: with `LPS_CEILING_REST_ENABLED` live (its own flip sits on the same asks page, so it is the likeliest to be on) KFY stops being a full refusal and elects at BASELINE through THAT lane, taking the headline to 3 reachable / 3 converting; with `STORY_POOL_ENABLED` off the sheet inverts to 6 reachable / 3 electing / **zero** conversions; with `SMA50_DIP_EXCEPTION_ENABLED` live the universe gate softens and all three sma50 names (ICLR, MSGS, VTR) enter the read instead, emptying that bucket and re-splitting the twelve across the other two — a UNIVERSE flag, which is exactly why a hand-list of lane names was never going to be complete. Two more were already pinned and so could not leak: with `CONTRACTION_RESCUE_ENABLED` live instead, ALL FOUR reachable names elect through the SIBLING lane at baseline with "contracting at resistance" records — so on a machine where that lane is already live this flip buys nothing new, and an unpinned probe would have printed `full_refusal_reachable 0` or credited the sibling's conversions to this lane. A 29-flag ambient sweep the same day found the remaining 21 inert HERE, on THESE twelve names, at THIS cache edge — recorded in the sidecar as a null result on one population and deliberately not as a property of those flags, which is why they stay pinned). The same twelve names now classify FOUR ways — the retired probe could only say reachable/not, because the public `read_structure` returns `None` for both a full refusal and a doctrinally-final cause-before-effect abstention, so it would have silently mislabelled a veto as reachable: **4 full refusals the lane reaches, and ALL FOUR convert through the shipped scoped lane** (AMCR tier B 93.3 · CARS A 100.9 · KFY A 107.3 · VRTS A 103.5, each electing through the story pool with the "engaged at resistance \| …" record); **5 now elect at baseline** where the lane cannot touch them (BIIB elects without firing; BMY, CCEP, GEO, RCUS **already fire tonight with no flip at all**); **3 fail the universe filters** at the sma50 leg (ICLR 164.99 vs 167.06, MSGS 384.73 vs 394.03, VTR 91.17 vs 92.36); **0 cause-vetoed** — zero today, but the property is now tested rather than assumed, and the TEST is what changed at the round-two review: finality is the `narrative._CAUSE_VETOED` sentinel (`read_structure` step 2 returns on it before arming a single form), NOT `doctrine_audit._VETO_OUTCOMES`, which answers a different question (which non-elections the coverage census excuses) and also holds `lps_before_spring`. An LPS chronology floor ends one root's attempt and the walk moves on, so such a name is an ordinary full refusal the armed lane DOES re-walk — the first predicate would have printed "never reachable" over a chart this flip converts and skipped its conversion leg. Zero names in this population hit that floor (`reachable_with_chronology_floor: 0`, now disclosed on the sheet), so the error cost nothing here by luck rather than by design, and the probe now proves both halves of the rule against the engine before it prints a count. The survivors' grades moved too (KFY S/119.9 → A/107.3, VRTS A/107.5 → A/103.5, CARS A/101.3 → A/100.9, AMCR B/86.2 → B/93.3): it is not only the count that perishes, it is every cell. **So the honest fleet exposure of this flip is 4 names, not 12** — a ~3× overstatement if the census row were read as standing fact, and 8 of the 12 charts an eyeball sheet would have offered are charts the lane can no longer reach. **What does NOT churn is the acceptance evidence:** EGBN 2026-01-07 tier A is reproduced end-to-end against the SEALED fixture by `tests/engine/test_bar_posture_rescue.py`, unaffected by cache drift (merge note 2026-09-24: the banked A/B's second conversion, PKE 2026-02-18 tier B, is no longer pinned — PKE:2026-02-24 left the corpus on 2026-09-08 when he deleted that drawing and redrew the setup at 2026-04-07, which fires at BASELINE, so the guard's PKE half retired with it, the same loss the contraction rescue recorded; his 2026-08-19 ruling on both dates stands) — rule the flip from the calibrated list, re-derive fleet color the day of the sitting (~60 s, the probe rewrites its own sidecar and prints the four-way table). **Blocking decision: operator flip vs the fleet sheet RE-DERIVED ON THE MORNING OF THE SITTING — the W3 record-label signing is SATISFIED ("engaged at resistance" signed 2026-09-01, decisions.md signing row; the string freezes at this flip) — the reseal SHARES EGBN/PKE with the contraction rescue's (whichever lane flips first re-seals 28/33 → 30/33 with both pinned by name — arithmetic of the 33-mark corpus; on the 35-mark corpus that follows his current drawings the shared name is EGBN alone, and with only this lane armed the five sealed misses replay to EGBN converting 2026-01-07 tier A while MDT, NOK, SKYT and ST stay silent: 30/35 → 31/35, measured on the sealed fixture 2026-09-24), and the second-walk cost bound rides the same ScanTimer instrument as the rescue lane (Task 10), read on ONE armed trial night whose choreography is proposed in [consolidation_method_2026-09.md](consolidation_method_2026-09.md) §Task 14 ("The trial scan") — the precondition is otherwise circular, since `rescue_lane_worker_s` only records on a night the lane ran, which is exactly the shape `conventions.md` EC-49 already rules. The read is `market_context._scan_metrics.phases_s.rescue_lane_worker_s` in `output/screener_data.json` — the persisted key is `phases_s`, never `phases` — and it is summed in-worker seconds, not wall clock. That instrument was also made honest for exactly this combination: the sink no longer books the species lane's scoped DARK read, which would have summed its wall-time into `rescue_ms` and overwritten the attempt outcome on the very night the numbers are read (council review 2026-09-01, finding 1)** | `settings.BAR_POSTURE_RESCUE_ENABLED`; [consolidation_method_2026-09.md](consolidation_method_2026-09.md) §Task 7; `engine_alpha/structure/events/event_map.py` `story_admission_bar_posture`; `tests/engine/test_bar_posture_rescue.py` | 2026-10-31 |
| `SMA50_DIP_EXCEPTION_ENABLED` (+ `SMA50_DIP_MAX_SESSIONS`/`SMA50_DIP_MAX_ATR`) | 2026-08-28 (miss program) | **The 50-day dip exception at the universe door** — the deep-spring/recovery lane the miss program's universe-door ruling asked to be designed instead of removing the trend template: an sma50 refusal enters chart reading (geometry stays the only veto; SMA_200 + YoY legs still gate behind it) when the dip under the 50-day began within `SMA50_DIP_MAX_SESSIONS` (25) and the close has recovered to within `SMA50_DIP_MAX_ATR` (1.0) ATR_10 of it. Frozen-frame evidence (docs/miss_program_2026-08.md): **SKYT's drawn spring dragged price under the 50-day for ~4 weeks; with the door held open the engine elects a COMPLETE strict-pool structure on 2026-04-07 and fires tier S** — the door was that day's ONLY wall — and **ST fires tier S on 2026-04-06** through its own two door sessions (margins −1.7%/−1.3%, 0.38/0.31 ATR deep; the pre-SOS shelf completes an LPS at that edge), so this lane converts BOTH sma50 misses. Deliberately does NOT reach MDT: its wall is the sma200 rule at BOTH the door and anchor seeding (`collect_root_anchors` `below_trend_sma`) — a bottoming-base species ruling for the operator, not a dip. **The census answered its own precondition and the answer is WIDE**: at the cache edge 2026-08-27, 291 of 1,770 sma50 refusals admit and **157 FIRE (+51% on 309 standing fires)** — mostly shallow 3-session wobbles at median 0.43 ATR under the rail, while SKYT (20 sessions, 0.90 ATR) sits at the deep end, so **no knob tightening separates the wobbler mass from the drawn class without losing SKYT first** (measured curve in the program doc). **Blocking decision: rule the shape — flip as-is (+157, eyeball sheet `research/evidence/miss_lane_census_2026-08-28.json`), narrow to the story-coupled form first (admit the dip only when the elected box's own below-S excursion explains it — a follow-up design), or hold dark; plus the ratchet reseal for SKYT (28/33 → 31/33 with the rescue's two)** | `settings.SMA50_DIP_EXCEPTION_ENABLED`; [miss_program_2026-08.md](miss_program_2026-08.md); `engine_alpha/evaluation.py` `_sma50_dip_admits`; `tests/engine/test_miss_program_lanes.py` | 2026-10-31 |
| `BOTTOMING_BASE_LANE_ENABLED` | 2026-08-29 (miss program, operator ruling) | **The sma200 rule opened at BOTH its layers when the 50-day is reclaimed** — the operator re-marked the misses and ruled MDT a wanted catch (*"I don't want to miss them"*), resolving the bottoming-base scope ask in the catch direction. MDT's wall was the sma200 rule twice: the universe door AND `collect_root_anchors`' `below_trend_sma` seeding refusal — while the engine, held open, elects his drawn box to the penny. One flag opens both layers under ONE condition (close ≥ SMA_50: a bottoming base is read only once its intermediate trend has turned); price/volume/YoY still gate, and the chained-door guard is pinned (a frame under BOTH smas that dip-excepts past the sma50 leg is refused at sma200 — the two door lanes can never chain). Frozen-frame A/B: **MDT fires 2026-07-14 tier S at his drawn R 82.83**; junk corpus clean with the lane armed. **The census answered (cache edge 2026-08-27): 410 sma200 refusals → 194 admit (47%) → 60 FIRE (+19% on 309 standing fires; 15 S / 32 A / 11 B / 2 C)** — sidecar `research/evidence/miss_lane_census_2026-08-29.json`. **Blocking decision: operator flip vs that 60-name eyeball sheet + the ratchet reseal** | `settings.BOTTOMING_BASE_LANE_ENABLED`; [miss_program_2026-08.md](miss_program_2026-08.md); `engine_alpha/evaluation.py` sma200 leg + `engine_alpha/structure/box/box_primitives.py` seeding gate; `tests/engine/test_miss_program_lanes.py` | 2026-10-31 |
| `LPS_CEILING_REST_ENABLED` (+ `LPS_CEILING_REST_MAX_BELOW_R_ATR`) | 2026-08-29 (miss program, operator ruling) | **The launched-above-R straddle sanctioned when the rest lands ON the ceiling** — NOK elects his box at his exact rails every session and dies at ONE LPS leg ("window launched above resistance"); his re-drawn 2-bar rail rest sits 0.241 ATR under R with the launch being the SOS top's own giveback. The exception passes the INSIDE-zone launch gate when `support_low ≥ R − 0.3·ATR` — the bar placed by the DRAWN class (DSGN 0.010 / MATX 0.087 / MSGS 0.148 / NOK 0.241; YPF 0.772 is a mid-box shape, not this form) against the nearest junk (ENIC, must-not-fire, would-be rest 0.314): **a stated ~0.06-ATR razor each side** — at 0.5 ENIC fired tier A and the negative-corpus flag-on leg went red; at 0.3 all 18 junk cases reject. **NOK fires 2026-02-13 tier B, ordinary strict pool**; shadow panel NO DRIFT flag-on (no existing election or LPS window moves on the 32-ticker fixture). **The census answered (cache edge 2026-08-27): conversions surgical — 8 of 1,241 no-reads — but 12 of 309 current fires DRIFT with the flag armed** (BSTZ CSX EC EOG LCUT MMI MTDR OVV PH WHWK XOM ZD): rails/box/tier hold on every sampled name; the ELECTED LPS WINDOW moves (the elector prefers the latest legal window, and a newly-legal ceiling rest wins), re-pointing triggers (CSX 51.88→51.70, XOM 162.81→161.13) and a few score points. **Blocking decision: operator flip vs the 8-name conversion sheet + the 12-name drift sheet + his eyeball of the 0.3 razor (drawn-evidence-shaped but n=4 vs n=1)** | `settings.LPS_CEILING_REST_ENABLED`; [miss_program_2026-08.md](miss_program_2026-08.md); `engine_alpha/structure/lps/detection.py` launch gate; `tests/engine/test_miss_program_lanes.py` + `tests/regression/test_negative_corpus.py` (the ENIC pin) | 2026-10-31 |

## THE 2026-07-18 rotation (behavior-identical; a named archive seam)

The Purity Pass Task 11 rotation retired the seven folded flag keys in ONE
deliberate `engine_config_version` event:
`TRAVERSAL_GATE_ENABLED`, `CANDLE_SPREAD_AWARE`, `PUZZLE_SCORE_ENABLED`,
`PIP_MACRO_PHASE_A_ENABLED`, `SOS_TRIM_ENABLED`, `BOX_BACKEXT_ENABLED`,
`DESCENT_TAIL_GATE_ENABLED` — each already unconditional engine code (task-7
folds, every one parity-proven). The before/after manifest dump proved a **pure
7-key deletion bijection**: zero keys added, zero values or JSON types moved.

- Hash equivalence: `91c70bdbd6a6686099ca2173250e602fd89120cc6df9e8631e62390a125640c6`
  → `5a4d2282474457ec70cde167a78bf6c80f83d15971d7f4df3bdb18f76bc1134d` is a
  **behavior-identical rotation** — the reading did not move; only the key set did.
- Archive seam: analyses that partition by `engine_config_version` should treat
  these two hashes as the same engine (like the 06-26 data seam, this is a
  bookkeeping cut, not a behavior cut).
- Hash-consumer stances, ruled deliberately: the calibration harness `--prev`
  comparison will report "ENGINE moved" across this commit — a false alarm to
  caveat in the next report; every mark's staleness chip flips (expected —
  marks keep their birth hash, no re-stamping); future seam analyses cut here.

## Retired

| Flag | Built | Outcome | Evidence |
|---|---|---|---|
| `TREND_TERMINAL_BOX_GATE_ENABLED` | 2026-07-27 (anchor-polarity ruling) | **DELETED 2026-09-08 on the operator's ruling** — verbatim: *"Lets DELETE the first two."* The kill-by fell due 2026-09-15 and this was its second and last re-date, so the row's own standing instruction was DELETE-if-unruled; he ruled it early and explicitly. The measured recommendation it was ruled on is [`trend_terminal_killby_2026-08-31.md`](trend_terminal_killby_2026-08-31.md). **What retired:** the post-collection filter in `bricks.validate_equilibrium` (and that function's `terminal_floor` parameter), `box_primitives.trend_terminal_legal_open`, the settings key, the manifest row, `tools/trend_terminal_ab.py`, and `tests/test_trend_terminal_gate.py`. **What SURVIVED, deliberately:** `market_structure.trend_terminal_floor` is untouched and unconditionally live — it feeds Phase A's climax polarity re-key (`bricks._cause_is_up`, 2026-08-19), which is the expensive half. Its Layer-3b guards MIGRATED rather than retired, to `tests/engine/test_trend_terminal_floor.py` (6 tests); the one test that asserted the deleted `terminal_floor=` parameter did not migrate, and the new module says so. **Byte-identical by construction** — the flag was already off, so the only runtime change is that the floor is now always computed lazily inside the Phase-A guard, which is exactly what the dark path already did. Gates at deletion: reader pin zero-diff, marks ratchet 28/33, doctrine audit clean, full suite green. The DOCTRINE is untouched and safe in `decisions.md` (2026-07-27, 2026-08-14 — append-only, and they rule on the CHART, not on this code); re-implementing the gate later is a fresh A/B, not a revert. PRIOR BLOCKING RECORD, kept verbatim: **A box may not OPEN before the trend running into it printed its climax — and if that climax lands INSIDE the box, at least `MIN_BASE_DAYS` (20) bars must have printed since.** Operator ruling 2026-07-27 (LIVN): *"We can't start the anchor from the opposite direction of the trend if we are still inside that trend"* + *"way too young to be considered anything but some false breach of the range."* Judged on each candidate's BACK-EXTENDED start against `market_structure.trend_terminal_floor` — per-bar terminal of the covering **confirmed** trend segment (first-write-wins so the CAUSE trend owns the handover bar; segments still RUNNING never veto, their extreme being unconfirmed). Filters the elected candidate list in `validate_equilibrium`. **Overshoot magnitude was falsified 3x and is NOT used:** the operator accepts 47.9% (PXS), 82% (VIK), 101% (MATX) of a box height past R and rejects LIVN at 20.57% — a `TREND_TERMINAL_OVERSHOOT_BOX` knob was built, measured and removed; do not re-propose. **Evidence:** engine's own `segment_trends` put LIVN's uptrend terminal at 2026-07-06 while the box opened 2026-06-18, anchoring the climax on an `HH/up` bar and the AR on an `HL/up` bar. Operator marks obey the rule 31/33 (93.9%). **Gates:** sealed marks ratchet **28/33 PASS flag-ON**; full pytest green flag-OFF; doctrine gate unchanged flag-OFF. Live A/B **re-measured 2026-08-13** on the 2026-08-12 payload (271 names, committed instrument `tools.trend_terminal_ab`): **249 identical / 21 lost / 1 MOVED**. Two corrections to the 2026-07-27 run: the loss set turned over COMPLETELY (not one of the original 39 still loses — this is a maturity rule, so a refused box only has to wait, and 14 sessions printed), and *"the gate never re-frames a box"* is **falsified** (APH re-elects an earlier framing; by construction — filtering candidates lets a later legal framing win the root). The 21 split cleanly: **12 class A** (the trend topped inside the box's first 10 bars — the LIVN case the ruling was written for) and **9 class B** (an upthrust inside a range that had already worked 12–27 bars — the contested class, and the open question). 6 of 21 sit within 2 bars of the `MIN_BASE_DAYS` floor. **Blocking decision — CORRECTED 2026-08-13.** This cell used to read *"operator eyeball of the 39 lost boxes"*. **That eyeball already happened** (2026-07-28, the session that committed `e5fec02`): the operator read the three cheapest class-B falsifiers — IRMD, IART, CYRX — and ruled all three **KEEP**, i.e. the gate was wrong to remove them, with the diagnosis that **`segment_trends` is box-blind**: every in-base rally above R prints another HH, the uptrend never "ends", the terminal lands inside the base, and the maturity clock starts on the wrong bar. Verdict recorded as *"It's not wrong; it's early."* **The unblock is therefore NOT another eyeball — it is 15–20 dated trend-end labels from the operator** (the IRMD sentence form: *"a downtrend ended on 05-01, then a new uptrend started and ended 06-10"*), to fix `segment_trends`, then re-run the A/B. ⚠ **This ruling has NO `decisions.md` row and no trace in the repo** — it survived only in an agent session memory, which is why the 2026-08-13 what's-next board mis-reported this flag as "blocked purely on your eye". Needs an operator-confirmed decisions.md row. **Fresh evidence for the box-blind diagnosis, 2026-08-13:** of the 9 class-B losses (climax deep inside a range that had already worked 12–27 bars), **9 of 9 have their claimed trend terminal ABOVE the box's own R** — median +0.45 box heights, range +0.06..+0.91. A trend that ends outside the base prints its extreme outside the base; these do not. The class-A losses by contrast include terminals a full box height BELOW S (KN −1.01, NPO −1.00) — real trend ends. That separation is the defect's fingerprint, measured on a cohort with no overlap with the one it was diagnosed on. (Reported as a DIAGNOSTIC of the reader's input, explicitly not as a candidate gate — overshoot magnitude as a rejection rule stays Tested-DEAD.) **Re-measure before any re-ruling; the sheet decays by design**. **CORROBORATED 2026-08-14 — the 2026-07-28 ruling is faithful.** The operator re-marked IRMD by hand, independently, three weeks later, and put the trend end at **2026-06-10** — the same date the agent memory recorded. He supplied IART (2026-06-15) and CYRX (2026-06-04) with it, plus 6 more dated marks off the AR renders; all 9 are recorded append-only in [`trend_end_marks_2026-08.json`](trend_end_marks_2026-08.json). **The box-blind diagnosis is confirmed from the other side and SPLIT IN TWO — this matters, because only one half is a trend-reader problem.** (a) *The pivot isn't found:* `segment_trends` holds a terminal within 3 bars of his trend end on only **2 of 9** marks; on IRMD/IART/CYRX/CATO/CNI/EC/HTH the peak he marks is not in the pivot set at all, because a later upthrust to the box's own R is a higher high and swallows the true trend end. (b) *The pivot IS found and Phase A cannot reach it:* on **BCPC** `segment_trends` holds his EXACT pair (2026-02-17 up terminal, 2026-02-20 down terminal) and on **LZB** his trend end to the bar (2026-06-17) — and Phase A anchors months earlier regardless, because the elected box opens BEFORE his trend end and the chronological invariant `ar_bar <= box.start_bar` forbids Phase A from reaching forward into it. No amount of work on the trend reader fixes (b); there the BOX is the binding constraint. ⚠ **9 marks is a direction of travel, NOT an acceptance set** — the operator explicitly declined to certify them (*"there isn't one concrete answer Im 100% sure is correct, their just very ugly structures (most of them) so i cant be sure"*). Do not seal this gate against that file; the 15–20 labels the unblock asks for are still owed, and marks on CLEAN structures are worth more than more marks on this cohort. **THE MARKS ASK IS WITHDRAWN 2026-08-19 — [`climax_anchor_diagnosis_2026-08-19.md`](climax_anchor_diagnosis_2026-08-19.md).** Half (b) is now fully mechanised without any new labels, and it is larger than (a) was thought to be: the drawn anchor is reconstructed backward from the elected box over a fixed 60-trading-day window (`bricks._enforce_climax_terminality`, off the module-level `_SEG_LEAD_IN = 60`), so **where the box opens determines the anchor error entirely** — on the six measurable marks the window term is pinned at −60 on five, and the spread comes only from box placement. Failure (b) is therefore not a residue, it is the mechanism. Polarity was separately keyed on `root.kind`, the keying already Tested-DEAD 2026-07-27; on CNI that branch overwrote the operator's exact pair after the raw resolver had found it — **RE-KEYED 2026-08-19** (`e4471e0`; decisions.md 2026-08-19 row) onto the confirmed covering segment (`trend_terminal_floor.direction`, cause-wins): CNI resolves to his date exactly, BCPC −82 → −39, earlier-than-his-end 5 of 6 → 3 of 6 (median −36 → −9), HTH regresses −6 → +54; ratchet 28/33 held. **Fixing `segment_trends`' box-blindness (half (a)) would NOT by itself move the drawn anchor**, because the drawn anchor never consults `segment_trends`' terminal — the diff tool reports that separately (2 of 9). Whether (a) and (b) are one program or two is still unruled, but they are now known to be different code paths. The 2026-08-19 re-key repairs the climax POLARITY only — the fixed 60-day window (term A) and box placement (term B) still stand, and `segment_trends`' box-blindness (half (a)) is untouched; this flag's re-ruling still waits on that fix + a fresh A/B, and the 2026-07-28 IRMD/IART/CYRX ruling still owes its operator-confirmed decisions.md row **RE-MEASURED 2026-08-31 — the kill-by fell due, and this is the written reason it is RE-DATED to 2026-09-15 rather than silently carried; the full record is [`trend_terminal_killby_2026-08-31.md`](trend_terminal_killby_2026-08-31.md).** `tools.trend_terminal_ab` on the 2026-08-27 payload (309 names, HEAD carrying `e4471e0`): **291 identical / 17 lost / 1 moved (CCK)**, split 12 class A / 5 class B. Proportionally the gate is quieter than 2026-08-13's 249/21/1 (7.7% → 5.5% of names removed), but the re-key may NOT be credited for that — the population changed and, decisively, **the loss cohort turned over COMPLETELY for the third consecutive A/B: the intersection of any two of the three loss sets is EMPTY** (2026-07-27: 39 names; 2026-08-13: 21; today: 17). That turnover is the property that governs this flag: a maturity rule's evidence can never bank, so every future re-ruling needs a fresh sitting on names nobody has ever seen — and **the 2026-07-28 IRMD/IART/CYRX KEEP ruling can no longer be re-tested at all** (IRMD and IART are out of the payload universe; CYRX is in it and now reads identical). What DID survive the re-key is the defect fingerprint: class-B losses carried their claimed climax above the box's own R on 9 of 9 at median +0.45 box heights in August, and on **5 of 5 at median +0.558** today — two disjoint cohorts, one polarity repair between them, the same tell. The honest counter is recorded too: 7 of today's 12 class-A losses sit with their climax essentially ON the box ceiling (≤ +0.20 box heights), which is what a genuine "the trend topped and this box is the reaction" read looks like — so the gate is probably RIGHT about most of what it removes. That argues the DOCTRINE is right, not that this code should be carried; the doctrine is safe either way in `decisions.md` (2026-07-27, 2026-08-14 — append-only, and they rule on the CHART, not on code). One more mark against carrying it: the single re-framing (CCK) trades a tighter box for one **26% wider** opening 6 sessions earlier, which cuts against the prime directive. **MEASURED RECOMMENDATION: DELETE.** The retiring surface is a ~33-line filter in `bricks.validate_equilibrium`, `box_primitives.trend_terminal_legal_open`, one settings key, one manifest row, `tools/trend_terminal_ab.py` and `tests/test_trend_terminal_gate.py` — while **`market_structure.trend_terminal_floor` SURVIVES untouched**, unconditionally live feeding the Phase-A climax polarity re-key (`bricks._cause_is_up`), so the hard-won segment machinery is not what would be discarded; with the flag off the reader is byte-identical after removal, the only runtime consequence being the manifest rotation. Layer 3b of the gate's test module tests the FLOOR and must migrate rather than retire. **The re-date exists to give the operator the ruling**: DELETE, or open the `segment_trends` box-blindness program with a date ([`pattern_register.md`](pattern_register.md) row 10, opened today so the blocker is a tracked thing rather than a phrase). **If still unruled on 2026-09-15, DELETE — this is the flag's second and last re-date.** | `settings.TREND_TERMINAL_BOX_GATE_ENABLED`; [engine_reference.md](engine_reference.md) "Phase B — trend-terminal box gate"; **eyeball sheet (current): [`research/fidelity/trend_terminal_2026-08-13/LOSSES.md`](../research/fidelity/trend_terminal_2026-08-13/LOSSES.md)** + `index.html`; the 2026-07-27 sheet is marked SUPERSEDED in place; **operator marks + measurement 2026-08-14: [`anchor_marks_ruling_2026-08-14.md`](anchor_marks_ruling_2026-08-14.md)**, [`trend_end_marks_2026-08.json`](trend_end_marks_2026-08.json) |
| `AR_FIRST_REACTION_ENABLED` | 2026-07-04 (raw-bar); trend-derived 2026-07-05; **big-bounce retarget 2026-07-05** | **DELETED 2026-09-08 on the operator's ruling** — verbatim: *"Lets DELETE the first two."* Ruled a week before the 2026-09-15 kill-by this row had already declared non-re-datable ("DELETES if unruled"). Dark from 2026-07-04 to deletion, so **nothing the engine reads changed**. He was given the row's own two options — delete it, or keep it dark purely as the anchor program's measuring instrument — and chose delete; **the consequence is real and is recorded here**: the flip half of `tools/research/operator_marks_diff.py` retired with it and the climax-anchor program lost a toggle it used to measure by. The ANCHOR half of that tool survives and is now its whole purpose. **What retired:** `bricks._first_impulse_ar_end`, `market_structure.first_reaction_after`, the flag + its four `AR_*` tuning constants, their five manifest rows, `tools/ar_first_reaction_diff.py`, and the two guard tests in `tests/engine/test_bricks.py` + six in `tests/engine/test_market_structure.py`. **What SURVIVED, deliberately:** `market_structure.elected_trend_leg_base` (drawn by `tools/research/full_package_render.py`, whose off/on AR pair collapsed to one dot and whose `AR_UP_LEG_LOOKBACK` became a local render constant); `_prep_live` moved into `operator_marks_diff` rather than being copied (EC-3); and `analyze.py`'s `PHASE_A_ANCHOR_FEATURES` seam partition, which was BUILT as this flag's flip precondition but stands on its own — the climax-terminality re-key is itself an AR-mode seam. Gates at deletion: reader pin zero-diff, marks ratchet 28/33, doctrine audit clean, full suite green. PRIOR BLOCKING RECORD, kept verbatim: Re-anchors the drawn Phase-A automatic reaction to **the low of the first *continuous* reaction after the terminal swing** (`market_structure.first_reaction_after`): retrace basis = the trend's **FULL leg** (elected segment start, `elected_trend_leg_base`), closed at the first **big confirmed bounce** off that low (no stall). Retargeted from the terminal-sub-leg+stall read after the operator's dated BC/AR marks on **PH/TOL/AVNT/AAP/AGCO/TFX** (2026-07-05) showed it over-tightened the clean reactions — the retarget is exact on TOL/AVNT and surgically fixes only the AAP-class second-leg overshoots. Tighten-only + overlay-only. Gates green: full pytest, **shadow `--check` flag-OFF byte-identical**, hermetic seed-recall; `CANONICAL_FIELDS` exclude the AR bars so a flip is byte-identical on scoring/tier/canonical outputs. **NOT fully byte-identical downstream:** flipping shifts the ARCHIVED `bin_a_*` measurement columns (`structure.ar_bar` → `measure_phases`, consumed by `analyze.py`), which no freeze gate covers — a live flip needs a `bin_a_*` guard / `engine_config_version` partition in `analyze.py` FIRST or it silently mixes two AR-mode populations into the fingerprint pool. Universe scan: **19/140 overlays re-anchor** (median 8, max 53 bars; AEBI −53 / TAC −47 / AES −38 = the "smear" cases; AAP −12) — more conservative than the raw-bar 37/140. **Council-reviewed for engine-α (2026-07-06): stays default-OFF in α** (the archive seam is a β-flip prerequisite); the retarget rides into α as measure-only substrate, flag off. **RE-MEASURED 2026-08-13** on the live cache (`tools.ar_first_reaction_diff --scan`): **100 of 291 firing overlays re-anchor** — far more than the 19/140 recorded above, because the raw fallback paints a long synthetic reaction. OFF span median **39 bars** (24/100 at ≥58); ON span median **7.5** (50/100 collapse to ≤7); tighten median 19, max 59. The drawn climax→AR stripe is smeared across a third of live reads. **BLOCKER CLEARED 2026-08-13:** the archive-seam precondition named in this row — partition `bin_a_*` before a flip mixes two AR-mode populations — is now taken in `core/archive/analyze.py` (`PHASE_A_ANCHOR_FEATURES`: withheld from the pooled fingerprint + outcome correlations across an `engine_config_version` seam, reported scoped to the current epoch; gate `tests/archive/test_analyze_anchor_seam.py`). **What remains is the operator's eyeball only** — renders at `research/fidelity/ar_first_reaction_2026-08-13/`. **RE-DATED 2026-08-15 → 2026-09-15 on 2026-08-13; the written reason this file's own rule demands:** the engineering blocker is cleared and the evidence is refreshed, so what is left is a judgment only the operator can make — and it had never been put in front of him with current numbers. Not a silent carry: **if this is still dark on 2026-09-15 with the renders unread, DELETE the flag rather than re-date it a second time**. **RULED 2026-08-14 — DO NOT FLIP.** The renders were read; the operator wrote a verdict on 9 of the 12 and supplied dated trend-end/AR marks with them (recorded append-only in [`trend_end_marks_2026-08.json`](trend_end_marks_2026-08.json)). Measured against his own marks on the 6 names where both reads compute: **flag-OFF is closer on 4 of 6 — |off| = 133 bars total vs |on| = 266** (median 18.5 vs 48), on the cohort deliberately selected to be where the flag moves MOST (these 12 renders are the 12 largest tighteners in the universe), i.e. a cohort chosen to flatter the proposal and it still loses. **Mechanism, which matters more than the verdict — this is NOT "the retarget is badly built":** the raw fallback pins `ar_bar` to `box.start_bar` (measured exactly equal on 9/9 firing names), and the operator's AR **is** the box open by his own repeated definition (*"AR at 17/06 which also serves as the root swing for the consolidation"*, stated on IRMD, IART and CYRX) — so flag-OFF satisfies his identity BY CONSTRUCTION, right for a reason unrelated to reading a reaction. The retarget breaks that identity: it re-attaches the AR to a short reaction off `climax_bar`, the correct Wyckoff move, which would be the right answer if `climax_bar` were the trend end. It is not — the engine's climax is EARLIER than his trend end on **6 of 6** measurable names, never once later, median −50.5 bars. The retarget's SHAPE is vindicated (its spans are 1–7 bars against his 1–5; on HTH its AR lands on 2026-04-09, his trend end, to the bar — exactly what he said: *"the green circle is correct but not for the AR"*). Its span is right and its anchor is wrong, so a flip trades an accidentally-correct drawing for a principled incorrect one. **Blocking decision is therefore no longer the eyeball — it is the CLIMAX ANCHOR:** this flag cannot be ruled on its own merits until `climax_bar` lands on a trend end. Kill-by NOT re-dated (2026-09-15 stands); the delete-trigger above is discharged (the renders were read) and replaced by: **if the climax anchor is still unaddressed on 2026-09-15, delete the flag**. **ANCHOR MECHANISED 2026-08-19 — [`climax_anchor_diagnosis_2026-08-19.md`](climax_anchor_diagnosis_2026-08-19.md).** The blocking condition named above ("cannot be ruled until `climax_bar` lands on a trend end") is now explained, and the explanation is that `climax_bar` **is not a chart measurement at all**: `_enforce_climax_terminality` re-anchors to the extreme of a FIXED 60-trading-day window ending at the box open (`bricks._enforce_climax_terminality`, whose repair window is built from the module-level `_SEG_LEAD_IN = 60`) and returns `(window_extreme, pbs)` — which is also why the drawn AR equals `box.start_bar` by assignment, not by reading a reaction. The error decomposes exactly as `dCx = A + B`, `A = climax − box.start ∈ [−60, 0]` (mechanically bounded; measured min exactly −60 across the live fleet), `B = box.start − his trend end` (exogenous). **`A` is pinned at −60 on five of the six marked names, so the whole spread lives in `B`** — LZB is exact because its box opened right after his peak and the terms cancel; HTH (−60+54) and CATO (−60+42) are two large errors nearly cancelling, NOT near-misses. Polarity WAS keyed on `root.kind`, a seed label a median ~394 trading days away, contradicting the drawn pair 4/4 on live specimens — **the same keying `decisions.md` ruled Tested-DEAD 2026-07-27**, whose row already prescribes the remedy ("derive the cause from the segment covering the bar"). **THAT RE-KEY EXECUTED 2026-08-19** (`e4471e0`; decisions.md 2026-08-19 row): `_cause_is_up` (`bricks.py`) now reads the direction from the confirmed segment covering the box open (`trend_terminal_floor.direction`, cause-wins), `root.kind` surviving only as the fallback where no confirmed segment covers the bar. Measured after, against the operator's marks: **CNI resolves to his date exactly; BCPC −82 → −39; earlier-than-his-end 5 of 6 → 3 of 6, median −36 → −9; HTH REGRESSES −6 → +54** (its former closeness was two large errors cancelling — recorded, not hidden); Guided-List ratchet 28/33 held, same fingerprint. On **CNI** the raw resolver returned his exact pair (2026-07-17 → 2026-07-21) and the mis-polarised branch overwrote it. ⚠ **The 2026-08-14 cohort statistic must not be quoted fleet-wide**: those 12 renders were selected as the largest AR pull-ins, which IS the lookback term, so the cohort sits at the 60-day ceiling by construction. ⚠ **MAN's "exact pair at clock 10" recorded in `decisions.md` 2026-08-14 could NOT be reproduced** (`read_structure` returns None under the supported override) — do not lean on it un-re-derived. **The 15–20 marks are WITHDRAWN as the unblock** — none were needed, and the re-key half of the old either/or is DISCHARGED (executed 2026-08-19, above). What remains open for THIS flag: the retarget can now be RE-MEASURED against the repaired anchor — re-run `tools.ar_first_reaction_diff` on the re-keyed climax and put that comparison to the operator; his ruling on it is what the 2026-09-15 kill-by gates on (still dark; expires with that document as the written reason if unruled) **RE-MEASURED 2026-08-31 — the owed re-measure is DONE; record: [`ar_remeasure_2026-08-31.md`](ar_remeasure_2026-08-31.md).** Against the re-keyed climax the scan reads **101 of 335 firing overlays re-anchor** (was 100 of 291), OFF span median 39 → 47 (27 of 101 at ≥58), ON span median 7.5 → 5 (62 of 101 at ≤7), **tighten median 19 → 35 bars**, tighten total 2,365 → 3,149. A one-cache isolation varying ONLY `bricks._cause_is_up` (`research/fidelity/ar_first_reaction_2026-08-31/rekey_isolation.py`) attributes that widening to the re-key itself rather than to the cache: legacy polarity 121 of 335 movers at tighten median 22, re-keyed 101 at 35 — so **the flag now touches FEWER charts (−16.5%) and disagrees by MORE on the ones it still touches**, and only 69 movers are shared between the two polarities. The 2026-08-14 DO-NOT-FLIP mechanism survives, weakened but not overturned: the drawn OFF `ar_bar` still equals `box.start_bar` on 5 of 6 live reads (was 9 of 9), `segment_trends` still holds a terminal within 3 bars of his trend end on only 2 of 9 marks (unchanged), the resolved climax is still earlier than his trend end on 5 of 7 (median −18, was 6 of 6 at −50.5 — the anchor genuinely improved, the flag did not), and 14 of 101 movers sit at `old_span == 60` EXACTLY with 27 at ≥58: the drawn climax is still the fixed `_SEG_LEAD_IN` window artifact, because the re-key changed which END of that window is taken and left the window alone, exactly as the 2026-08-19 diagnosis predicted. On the operator's own marks the flag is now a **no-op on 3 of the 4 live reads** (CYRX/BCPC/CNI tie) and loses the fourth badly (EC: 6 bars off vs 65 on); note the marks cohort has thinned to 7 of 15 in universe, and 3 of the 4 moving names fall back to PRE-re-key recorded dates, so the fleet scan is the clean current-arithmetic read. **So the flip question is settled a second time and only DISPOSAL is left: delete the flag, or keep it dark purely as the anchor program's measuring instrument** — `tools.ar_first_reaction_diff` and the flip half of `tools.research.operator_marks_diff` measure the anchor defect BY toggling it, and the climax-anchor program (fixed 60-day window, box placement, `segment_trends` box-blindness) is still open. **NOT re-dated: 2026-09-15 stands and DELETES if unruled**, per this row's own standing forbiddance of a second hopeful carry. Fixed in the same change, because the tool was overstating this flag's case: `tools/research/operator_marks_diff.py` counted TIES as flag-ON wins and printed "ON closer on 5 of 7" for a 2–2 head-to-head; it now reports OFF wins, ON wins and ties as three separate counts. | `settings.AR_FIRST_REACTION_ENABLED`; [strategy_alpha.md](strategy_alpha.md) "Trend & Change of Character" + [engine_reference.md](engine_reference.md) "Phase A — First-reaction AR anchor"; **renders 2026-08-13**: `research/fidelity/ar_first_reaction_2026-08-13/`; **ruling + measurement 2026-08-14: [`anchor_marks_ruling_2026-08-14.md`](anchor_marks_ruling_2026-08-14.md)**, marks [`trend_end_marks_2026-08.json`](trend_end_marks_2026-08.json) |
| `SENTENCE_ARCHIVE_ENABLED` | 2026-09-01 (consolidation-method Tasks 8/9) | **FLIPPED LIVE 2026-09-02** — operator ruling in the asks sweep (decisions.md): *flip it, with one armed trial night*. The archive is **measure-only**, and the flip battery is what proves it rather than the docstring: the full suite went green at **1,929 passing with only two bookkeeping guards red** — the settings quick-reference and this ledger's own Dark-table invariant — and **not one score, tier, election, ranking or shadow test moved**. Reader pin zero-diff, shadow no-drift, junk corpus 18/18, ratchet 28/33 with every expected miss still missing, doctrine audit PASS on all live setups. **What the flip actually turns on:** every fire now archives the chart translated into his signed words — the folded token tape (spring / support_test / sos / upthrust / markup / touch_and_pivot / mini_consolidation with held / breached / open / unreadable verdicts, date-anchored, closed-set-asserted at mint) in the `sentence_*` columns, measured ONCE in the shared eval chain so live/seed/manual rows cannot disagree. **Pre-flip rows stay NULL forever — there is no backfill**, and the epoch rotation (`93655c1a…`) is what keeps the two eras separable. **THE COST BOUND IS STILL OWED AND IT IS THE ONE THING A FLIP CANNOT SELF-MEASURE:** the fold only runs on a night it is armed, so the number arrives on the operator's FIRST nightly scan after `update_dashboard.bat`. Bound named before the night per EC-49: **≤ 60 s added to the evaluation phase against an expectation of 1–3 s**. Read it the next morning from `market_context._scan_metrics.phases_s.evaluation` in `output/screener_data.json` (the persisted key is `phases_s`, never `phases`; that file is the US-Stocks payload, so nothing needs picking). To compare against preceding nights use `output/scan_metrics.jsonl` — **a mixed-universe log with no universe field, so never read the last line**: pick the full-universe record by `counts.universe_tickers` (~5,900 against the 15-ticker Sectors+Market and 29-ticker Commodities+ETFs scans). **If it breaks the bound the revert is one word** — this flag back to False — and **the trial night's rows KEEP**: they carry their own engine stamp, so nothing ever needs deleting | `settings.SENTENCE_ARCHIVE_ENABLED`; [consolidation_method_2026-09.md](consolidation_method_2026-09.md) §Tasks 8/9; `engine_alpha/structure/events/event_vocabulary.py` (family + serializer + producer); `tests/engine/test_sentence_family.py`; PRIOR BLOCKING RECORD, kept verbatim: **The archived sentence — the ONE language's folded token tape, measure-first.** Per fire, every reader's output (the puzzle waves + their role stamps, the rail episodes, the mini-consolidation) folds through the signed vocabulary (`event_vocabulary.unify_events` — the operator-signed word table, closed sets asserted at mint per EC-55) and serializes DATE-ANCHORED into the `sentence_*` column family (`sentence_tokens`/`sentence_n_tokens`/`sentence_nan_bars`; owning module `event_vocabulary.py`, model-only columns, producer splat on ALL THREE writers per EC-30). Measured in the ONE shared eval chain once per elected box — live, seed and manual rows carry identical sentences by construction, and replay-at-T equals live-at-T. Measure-only: never gates, scores, or sorts; flag-off spreads {} and the family archives NULL; a refused read (unreadable geometry) NULLs the whole family (the three-state law, Task 3); the readability companion rides beside the counts. Rows written under a different reader vocabulary are a different epoch — the flag is manifest-listed from day one, no backfill EVER. **Blocking decision: operator flip after the evaluation-phase cost is read on one nightly scan** (the compute reuses the live event-map readers; the additive cost is one chokepoint pass + the fold + serialization per fire — expected low single-digit ms/fire, measured via scan metrics at the trial). **That night is a TRIAL SCAN under `conventions.md` EC-49, and the choreography is now written down instead of improvised** — who arms the flag, the bound named before the night, where it is read the next morning, and what a revert looks like: [consolidation_method_2026-09.md](consolidation_method_2026-09.md) §Task 14, "The trial scan". The precondition was otherwise circular: the evaluation-phase delta only exists on a night the fold ran, which only happens flag-ON (council review 2026-09-01, finding 7). **The read, and its honest limit:** `market_context._scan_metrics.phases_s.evaluation` in `output/screener_data.json` (the persisted key is `phases_s`, never `phases`), compared against the preceding US-Stocks lines of `output/scan_metrics.jsonl` — and those four most recent dark nights read 146.82 / 215.63 / 176.97 / 145.93 s, a ~70 s spread with no flag change at all, so the proposed ≤ 60 s bound is an explosion tripwire and NOT a measurement of a ~1–3 s fold. The fold's own evidence on the trial night is the per-fire `sentence_*` archive cells plus the `[sentence drop <date>]` stderr lines. Two pre-flip hardenings landed with that review and both belong on this row: (a) **an error inside the fold can no longer delete the fire from the night** — the family raises BY DESIGN (the mint assert, the vocabulary-miss refusal) and those are exactly the types the eval chain's skip-guard catches, so a lagging word table after a re-wording would have turned every setup carrying the new event into `EVAL_ERROR`; the measurement now sits in its own guard degrading to the family's declared NULL state plus a counted stderr drop (`evaluation.SENTENCE_DROPS`, per worker process — it does NOT aggregate into scan metrics, so the trial reads it as stderr lines, finding 5); (b) **the signed word/verdict sets are now engine identity, not just this flag** — the freeze manifest carries `SENTENCE_VOCABULARY` derived from the one declaration (`event_vocabulary.vocabulary_manifest()`, EC-33), so a post-flip re-wording rotates the epoch instead of minting new-worded rows under the old stamp with backfill forbidden (finding 6) — **and at the round-two review that guarantee was widened from the value SETS to the MAPPING**, because a re-ruling that stays inside the closed sets (moving `spring` from *held* to *breached*, swapping a word between two event types) left the hash unmoved: three such re-rulings were driven through the shipped code with zero rotation. The projection now carries the four declared tables plus the mini-consolidation constant, and each of those re-rulings moves the hash; a pure REORDERING of a table deliberately still rotates nothing (those tables are lookups nothing reads positionally, and a spurious epoch permanently splits a poolable cohort — the same split the TA-grade block makes between chapter ORDER and the chapter MAP). Both directions pinned in `tests/engine/test_event_vocabulary.py`. **The same hole one level up was closed at the round-three completeness pass (2026-09-01):** the projection is no longer a hand-typed list in the function body — it is DERIVED from a declaration registry (`_MANIFEST_SOURCES`), and a guard pins that registry against the module's own declarations, so every table is either projected into the identity or excluded from it on the record with its reason; a future table can no longer sit outside both by omission, and a registry row naming a vanished declaration refuses too. That pass also added the block the hand-typed list had already swallowed — the channel BASIS declarations, which say which substrate each channel's words stand on and the `span_origin` every archived date is measured from — taking the projection to eight keys. Each of those closings rotated the engine epoch again, and **no hash literal is written down on this row on purpose**: the value may rotate once more before the seam, so read it from the engine (`.\.venv\Scripts\python.exe -m engine_alpha.freeze.manifest --hash`) or from [engine_reference.md](engine_reference.md)'s generated Settings Quick-Reference block, which `tests/tooling/test_docs_sync.py` fails the suite over whenever it drifts. The reader-pin baseline and the marks-corpus seal still carry older stamps and both gates still PASS — the pin RECORDS its engine stamp without asserting it (what it asserts, the reader vocabulary, is current and zero-diff), and the seal only ADVISES on an epoch difference — so what is stale is the stamp, never the reading. **Re-stamping is the chair's act at the seam commit under EC-29/EC-51 (a baseline recaptures only at a flip/seam commit, in the same commit as the rotation it answers), never an agent's mid-build diff** — this is stated identically in [consolidation_method_2026-09.md](consolidation_method_2026-09.md) §Tasks 8+9, after a draft of that record claimed the stamps had already moved and this row said the opposite in the same changeset (round-three completeness critic, 2026-09-01). **Two flip-night expectations, and both now have a test behind them** — the first was written on this row a round before anything pinned it, which is precisely the defect the round-four critic proved by deleting the whole `_log.warning` call and watching the suite stay green. Both lines come out of `event_vocabulary.serialize_sentence` on the logger `chrollo.engine.event_vocabulary`, at WARNING, and both NAME the channel: **(1)** a **span** that cannot reach the elected window's ruler is reported instead of nulling in silence; **(2)** a **knowable causality stamp** — the reader's own "the day this became knowable" — that EXISTED and then failed to convert onto that ruler gets the span line's twin, same logger, same shape. The two are independent (deleting either leaves the other firing) and the second fires only on a LOST stamp: a record honestly still in progress, with no knowable day to convert, is never reported. Pinned in `tests/engine/test_event_vocabulary.py` by `test_the_off_ruler_span_null_is_named_on_the_log_never_silent`, `test_the_off_ruler_knowable_stamp_is_named_on_the_log_never_silent` and the anti-always-warn control `test_a_knowable_stamp_on_the_ruler_is_serialized_and_never_reported`. So a trial night's stderr may carry either family beside the `[sentence drop <date>]` lines, and it can be LOUD — both are per-RECORD, so a whole channel losing its stamps prints a line for every record in that channel, not one line for the night. **The archive contract does not widen in either case**: the cell still serializes an honest null. One record off the ruler is an edge read; a whole channel off it is a broken ruler, which is the only reason these lines exist. |
| `POWER_PLAY_PRESET_ENABLED` | 2026-08-17 (species program Task 5) | **FLIPPED LIVE 2026-08-19** — operator: *"sure lets take the fix, flip the power play thing, as long is everything works like I asked and is working by the design that i set out to work (Calibrated List, Things I listed as reading correctly, Strategy documentaion...) I believe we havent regressed"*. **Only the preset half flipped, and that is a DELIBERATE deviation from this ledger's own "the two flags flip together or not at all"**: with `POWER_PLAY_STORY_FORM_ENABLED` also ON the sealed Guided-List ratchet BREAKS — **EGBN and PKE, both recorded expected-misses of stage `rail-placement`, now FIRE** (tier A / tier B). Good news is still a ratchet break, and re-freezing the operator's sealed standard is his ruling, not an agent's; the story-form half stays dark pending it. The lane is fully functional on the preset alone — the twin enables the contraction form inside its OWN scoped override, so the module flag only ever reached the PAYING read. Gates at flip: Guided-List ratchet **28/33 held, same marks fingerprint**; full suite green. **The ScanTimer cost bound this row named as the gate could not be measured while the lane was dark** — the cost instrument only reports with this flag on — so the bound arrives with the first live nightly scan; if it is too dear the flag is a one-word revert. PRIOR BLOCKING RECORD, kept verbatim: **The species reading clock as a declared PRESET through the ONE cascade** (operator ruling `c029555`: Power Plays are WANTED setups): `htf.window_override(settings.POWER_PLAY_WINDOWS)` moves exactly two names together — `MIN_BASE_DAYS` and its import-time copy `PIP_MACRO_MIN_BASE_BARS` (a bare clock patch silently leaves the macro-bridge overlay at the default clock — program Task 1 §D). **The scan lane IS wired and consults this flag** (program Task 8: `evaluation.evaluate_ticker_with_power_play` — the composed twin; flag-off byte-identity proven by its committed twin test, and the election half proven end-to-end through the real cascade in `tests/engine/test_power_play_lane.py`, post-review). **The dict's clock value is RULED: 8 (operator, 2026-08-18 — decisions.md row; species lane only, everything else unchanged).** History: built provisional at 10; the executed Task-10 capture corrected the 2026-08-14 diagnosis (at 10, MAN's first look lands ON breakout day; 8 is the first pre-breakout value); the ruling followed the census + his 40 sheet rulings. **The ratchet baseline is recaptured at the ruled clock in the same declared change (EC-29)** — and the new frozen MAN fact IS the next lever: at clock 8 MAN is watched and refused by the BREAKOUT WALL (`refused_clock`: its close above the pole peak printed 07-29, before the 08-11 look), so the wall definition, not the clock, now blocks the drift-up half of the species. The RULED value comes from the census (`tools.research.power_play_census`, as-of-consistent; **the species re-run EXECUTED 2026-08-18** — 48 min wall clock, tripwire OK, sidecar `output/power_play_census_species.json`, superseding the 2026-08-17 clock-only run; headline: **clock 8's elected cohort is the only one with positive forward returns** — median fwd_20 +2.8%, 54% winners, n=79, vs negative at 10/15/20) + the operator's keep/junk rulings on the generated sheets — **all 40 RULED 2026-08-18** (sealed: `docs/power_play_verdicts_2026-08-18.json`; S2 8/10 keep on the 8-day marginal catch; S1 6/10 keep incl. every named anchor — the breakout wall, first close above the POLE PEAK, misfiles a majority of its book-valid fast-resolution cohort; the wall calibration is the recorded next lever). **The species-scoped occupancy/traversal relaxation is operator-approved as a MEASURED experiment** (the ABVX autopsy: the refusal wall is the EQ occupancy floors + traversal, the exact wall that killed MAN) — swept via the census `--override` lever, sidecar-stamped, no floor moves live. **Blocking decision (clock RULED 8 + breakout wall RULED departure-form + occupancy relaxation Tested-DEAD — all 2026-08-18, decisions.md):** the Task-8 ScanTimer evaluation-phase bound measured at clock 8, then his both-flags flip. The recaptured frozen fact: MAN @ 2026-08-12 is `admitted_dark` — the lane reads the specimen end-to-end, dark | `settings.POWER_PLAY_PRESET_ENABLED`; [power_play_program_2026-08.md](power_play_program_2026-08.md) Tasks 1/3/5/8/**10** + "Council review + the fix program"; [minervini_oneil_canon.md](minervini_oneil_canon.md) §2; batteries `tests/tooling/test_power_play_census.py` + `tests/engine/test_power_play_preset.py` + `tests/engine/test_power_play_lane.py` + `tests/regression/test_power_play_acceptance.py` |
| `TA_SCORE_V2` | 2026-07-01 (`d4c90bd`); BUILD COMPLETE 2026-08-08 (14 commits `9085e93..68540c0`, reweight-neutral) + COUNCIL-REVIEWED 2026-08-08 (15 findings, 4 P1, all fixed in-branch) | **FLIPPED LIVE 2026-08-09** — operator's A/B eyeball of the 2026-08-09 scan, verbatim *"Sure lets do 62 /52 /42"* then *"okay the lets do it"*. `Tier` now derives from `ta_grade` against the NEW `TIER_S_STRUCT/A/B/C_STRUCT = 62/52/42/32` ladder (manifest-registered), with the `S_MAX_BOX_WIDTH` cap unchanged on top — it is what holds 22 of 111 A-tier names out of S, AAP included at 0.151. **The A/B basis mattered:** the 2026-08-08 read was REJECTED (its rows predate the grade columns, so setup_quality graded absence-neutral 0 while the stored score kept its points — the entire ±31 movers list decomposed to exactly that differential, 6/6 vs the wire); the 2026-08-09 scan is the first with the term measured on 256/256 rows (`setup_quality_absent_rows: 0`, identity OK). **Weights deliberately NOT set:** `SCORE_SPRING` + the four `SCORE_STORY_*` stay 0 and both `TA_WARN_*` stay 1.0 — those sub-scores are flag-gated so the archive holds nothing to calibrate against; the flip STARTS that archive (measure-first, costs set later). Consequence stated so it is not read as a null result: the flip re-expresses the read on a 0-100 scale with chapters and re-bases the ladder, it does not change the judgment — within-date movement is median 0 / max ±2. **Drift: exactly two fields, `FOF.Tier` and `HVT.Tier`, both B→A** — no score, rail or ranking movement across all five flag-on shadow guards. Re-bless: shadow re-captured + PASS, seed recall re-captured at **66.2%** (the old baseline was already stale and FAILING with recall *improved* 56.9→66.2, +7 recovered / −1 KALV) + PASS, **marks ratchet held 28/33**, fold-parity captured, Quick-Reference regenerated. Cost: **median 0.45 ms / p90 0.62 / max 1.02** per fire (better than the 0.64/1.02/1.37 post-review baseline); payload +1,019 B median per setup (~255 KB at 256 fires vs a 25.3 MB artifact). **RETIREMENT EXECUTED 2026-08-23** (operator delegated 2026-08-22: "Take the lead on this one, if you think its good then push and flip"; the council's P1 showed the rollback had already stopped being faithful — the frozen JS cap mirror fired the demoted RS/Uptrend chips on every legacy-epoch row, so the insurance the path was kept for had lapsed): `calculate_tier` + `TIER_S/A/B/C` + the `TA_SCORE_V2` flag deleted (manifest rotation = the declared seam), ranking re-pointed to `ta_grade` (raw-sum tiebreak), the seed scan-back election re-keyed to the same basis, `setupScoreMath.js`/`setupTagsData.js` fire rules/`ScoreBreakdown.jsx`/`tagFlagsFromWire`/the dual-epoch switches deleted, the presentational catalog extracted to `tagCatalog.js`, the lens LPS grade serialized engine-side (`lps_grade_fraction`, EC-28), and the live `calculate_structure_tier` ladder got its own direct band tests before the twin carrying its coverage retired. Remaining open from the flip row: the grade-verdict UI (named deferral) | **committed evidence: [ta_grade_flip_2026-08-09.md](ta_grade_flip_2026-08-09.md)**; [ta_grade_flip_checklist_2026-08.md](ta_grade_flip_checklist_2026-08.md), [archive/specs/ta-score-hybrid-design.md](archive/specs/ta-score-hybrid-design.md) |
| `NEAR_MISS_LANE_ENABLED` | 2026-07-26 (near-miss lane Tasks 7–11; council-reviewed 2026-07-26, 15 findings fixed) | **FLIPPED LIVE 2026-07-27** — operator decision against the §6b re-measured A/B, verbatim "3% aint a biggie lets flip it": ALL THREE pre-registered bounds FAILED (p50 +7.10 ms vs ≤+1; p95 +57.97 ms vs ≤+10; corpus wall +2.96% vs ±2%) — the overrun is ACCEPTED, the budget is NOT retro-widened (EC-15). The one-leg box-gate refusal collector + bounded deferred completion + cohort archive + report CLI; a near-miss NEVER scores/fires/enters the picks — flag-on adds archive rows and the report cohort ONLY (picks byte-untouched by design, guard-pinned). **The flip HARDENS ruleset `2026-07-26.A` + the per-pool dedup grain (strict > rescued > band)** — a future re-rule is now an archive-seam event (EVERY distribution re-bases). Flip mechanics: census re-pin with the axis named (engine `28498359…` → `c1a5cca0…`, manifest-ONLY drift — fingerprint/junk/one-leg/ruled all held), full suite green flag-ON. Cost levers stay on the shelf if the live wall hurts: `NEAR_MISS_TOP_K` cut; early-exit completion (sized 335-of-431 not_ruled, §6 lever 2). First live cohort: the next scheduled scan; review via `python -m tools.research.near_miss_report` (`--outcomes` after maturation). Standing gate: `python -m tools.research.near_miss_census --check` | `engine_alpha/structure/box/near_miss.py`; [near_miss_lane_2026-07.md](near_miss_lane_2026-07.md) §6b/§8; guards `tests/engine/test_near_miss_guards.py` |
| `STORY_POOL_ENABLED` | 2026-07-25 (Event Map program Task 8) | **FLIPPED LIVE 2026-07-26** — operator per-fire chart eyeball passed (the final gate): both A/B conversions confirmed real pre-breakout structure at the ENGINE's elected rails ("the Support and Resistance on both is correct"); YPF ruled early-but-right (post-window it broke out, LPS'd 05-12..05-15, then broke out truly — exactly what an inside pre-breakout surfacing should look like); NKTR's pre-base gap ruled irrelevant to the box's traits. Story-rescue last-resort pool on the RULED admission form (census Option A, operator-ruled 2026-07-25: >=2 completed support tests + terminal resistance posture + no drift, `event_map.story_admission`), consulted only when strict/rescued/band pools are ALL empty. **Fire A/B executed 2026-07-25: 26→28/33** — NKTR@2026-04-09 (tier A, `R+ S+ R+ S+ R+ S+~ R^`) + YPF@2026-05-06 (tier S, `S+ R+ S+ R^`); all 26 prior hit elections per-identity byte-identical, shadow panel identical, negative bench fully refused incl. RLGT, zero junk fires. **The pre-registered "EGBN converts" clause FAILED — recorded honestly:** admission-certain at the drawn rails but its framing is never PROPOSED at a story-passing shape (rail PLACEMENT, upstream of any pool). Flip mechanics: deliberate ratchet reseal 26→28 (`marks_corpus --build-fixture`; STAGE_TAGS entries removed), election-stability guard re-pinned with the two story-caused hits named as the ONLY legally off-silent identities, floors 26→28. **Cost (re-measured 2026-07-26 post council-review reorder):** median +3.3 ms, mean +12.6 ms, tail +140 ms per consultation; ~+5–15 s scan-level (inside the BAND precedent); EVENT_MAP substrate side median +2.2 ms. The flip rotates `engine_config_version` (archive seam). Remaining safe perf lever: per-root dedup; the survivor cap is NOT a perf knob (election-changing, needs the full A/B) | `engine_alpha/structure/box/box_primitives.py` pool seam; [strategy_alpha.md](strategy_alpha.md) "The rail-episode read"; **committed evidence: [event_map_program_2026-07.md](event_map_program_2026-07.md)** (census pins + fire A/B + cost + eyeball verdict; standing gate `python -m tools.research.event_map_census --check`) |
| `EVENT_MAP_ENABLED` | 2026-07-10 (Event Map Task 6; rail-episode substrate 2026-07-25) | **FLIPPED LIVE 2026-07-25** — operator grant at the Event Map program scope gate (PLAN-event-map Task 13). Fires-only measure-only compute: the stamped swing map + role labels + the rail-episode substrate (as-of sentence over the ELECTED window/rails — the same READER the story pool consults but a DIFFERENT basis than its admission read, so the two may legally disagree (YPF class); a story fire's admitting evidence is archived separately in `story_admission_profile`), archived as the `event_map_*` column family (typed scalars + readability companion + one compact date-anchored tape). Map-ON parity contract held as an executable assertion with a demonstrated red step: fires/elections/scores byte-identical, ONLY archive columns gain values. Cost (Task 12 A/B): median +2.2 ms per firing ticker, max +10.6 (the tape precedent's order). The flip rotates `engine_config_version` — the family's FIRST archive seam; pre-flip NULL rows are never backfilled | `engine_alpha/structure/events/event_map.py`; [strategy_alpha.md](strategy_alpha.md) "The Event Map" + "The rail-episode read"; committed evidence: [event_map_program_2026-07.md](event_map_program_2026-07.md) |
| `EQ_DWELL_BAR_BASIS` | 2026-07-25 (`3a78f09`) | **TESTED-DEAD 2026-07-25, flag removed same day** — the sealed fire A/B: EGBN converts AT the drawn rails (first_fire 2026-01-02, the operator's named support-test bar) but 10/26 hit identities break (MATX lost entirely, FOSL/NGL/NTCT/ROIV/WTS displaced winners, BWA/EWTX/MS/VIK re-dated) AND junk DGII+FLG fire — close-residence dwell is load-bearing for election stability ("rails do not drift" measured). `_dwell_bar_basis` survives as a MEASURE-only helper; never re-wire it into the gate | [bar_dwell_protocol_2026-07.md](bar_dwell_protocol_2026-07.md) §8 |
| `CAUSE_BEFORE_EFFECT_VETO_ENABLED` (+ `CAUSE_LPS_LOOSE_MAX`) | 2026-07-20 (PLAN-cause-before-effect, council-implement) | **FLIPPED LIVE 2026-07-20** — operator grant after the live A/B eyeball. Cause-before-effect election precondition: a box may not be elected over a live trend that never matured a cause (the MIDD FP class). `bricks.cause_maturity` abstains the whole Structure (`return None`) ONLY when ALL THREE reads agree the cause is absent: `macro_bridge_zigzag` (box-INDEPENDENT) abstains AND `read_swing_map` reads a live up-staircase (`pre_box`/`box` both `'up'`) AND the elected LPS's final shelf never tightened (`lps.tightness_ratio > CAUSE_LPS_LOOSE_MAX` = 0.90). The shelf leg (separator hunt 2026-07-20, wf_8a6fdb42) is the AND-narrowing third operand: MIDD's shelf is the census-loosest (0.957) while all 43 seeded winners tightened more (nearest 0.842), so it rescues the tight-shelf winners (BP/LECO/MEOH/NTCT/VLO, `tightness_ratio < 0.71`) the first two reads alone dropped. Depth-free (CTOS keeps its bridge — NOT the census-killed above-R cap) and fail-OPEN (degenerate frame / non-'up' / bad ATR / missing shelf never vetoes — recall is the gate). **Recall proof:** hermetic seed recall 43/43 (0 dropped), operator-marks ratchet held, MIDD rejects. **Live A/B eyeball (fired set 2026-07-17, 234 names):** removes 7 / keeps 227 — MIDD + FLG (operator-ruled "trend + dips, not a base") blessed, and BNS/CVCO/HWM/NTCT(current base, NOT the seed winner)/TPL all operator-confirmed skips; zero important names touched. Council-reviewed 2026-07-20 (10 seats, adversarially verified): no correctness/security/recall defect; 2 P2 fixed. Flip battery green. **EC-8 eval-phase cost bound (500-frame universe sample):** +0.4% (~+2.7 ms/ticker, ~15s per full-universe nightly scan) — one extra `macro_bridge_zigzag` per elected box + `read_swing_map` on abstain. The flip rotates `engine_config_version` (the archive seam — the only boundary where a MIDD-class name abstains). | `engine_alpha/structure/narrative/bricks.cause_maturity`; `engine_alpha/structure/narrative/reader.read_structure`; [strategy_alpha.md](strategy_alpha.md) "Cause before effect"; [archive/PLAN-cause-before-effect.md](archive/PLAN-cause-before-effect.md) |
| `ELECTION_DETHRONE_ENABLED` (+ `ELECTION_DETHRONE_SESSIONS`) | 2026-07-16 (PLAN-solve-the-engine task 13) | **FLIPPED LIVE 2026-07-16** — operator grant (flip checklist #4). Stale-frame dethronement: a rescue-propped (SOS-trim) framing whose buffered R the tape has left FULLY behind for the trailing 10 sessions loses the election **in favor of a later valid framing** (never into an emptier read); dethroned pairs narrate as `stage=dethroned` in the cascade trace. Blast radius pinned hermetically: flag-on over every sealed-corpus pinned hit at its first-fire session, zero elections change hands (`tests/engine/test_election_surgery.py`). Flip battery green; harness: MATX fires in-window tier S. **The sibling rescued-pool arbitration lever was built and REJECTED** — it killed VIK's pinned corpus hit; counterexample recorded at the `box_primitives` pool seam; do not re-attempt without new corpus evidence | `engine_alpha/structure/box/box_primitives.py` pool seam; [archive/PLAN-solve-the-engine.md](archive/PLAN-solve-the-engine.md) task 13 |
| `LPS_OVERSHOOT_WINDOW_ATR_ENABLED` (+ `LPS_OVERSHOOT_WINDOW_ATR_MULT`) | 2026-07-16 (PLAN-solve-the-engine task 10) | **FLIPPED LIVE 2026-07-16** — operator grant (flip checklist #3). For **OVERSHOOT_R windows only** (breakout throwbacks resting ABOVE R), the window-localization gate's denominator becomes `max(box_height, 2.0 × ATR)` — above a NARROW box, box height is the wrong yardstick (CTOS's marked shelf: 1.06 box-heights but only 1.42 ATR; k ≥ 1.67 admits it, 2.0 = margin). Gate-only: the archived `window_range_pct_box` measure is unchanged; wide boxes and INSIDE/UNDERCUT_S zones untouched (unit-pinned); non-finite ATR refuses the rescoped path. Flip battery green; harness: CTOS fires 2026-07-15 tier S at rails within tolerance, span overlap 1.0. **HARDENED 2026-07-17** after the operator's BBVA eyeball ("just incomplete"): the rescope only engages on a **matured cause** (`base_len ≥ 2 × MIN_BASE_DAYS`, the terminal-shakeout floor applied to the opposite rail) — immature causes fall back to the raw gate. BBVA's fixture frame frozen as negative case `BBVA@2026-06-05`; shadow drop was exactly BBVA, CTOS byte-identical, 18/18 negatives, ratchet 9/11 held | `engine_alpha/structure/lps/detection.py` window gate; [archive/PLAN-solve-the-engine.md](archive/PLAN-solve-the-engine.md) task 10 |
| `BAND_RAILS_ENABLED` (+ `BAND_MAX_BOX_WIDTH`, `BAND_EVENT_MIN_BARS`, `BAND_EVENT_MAX_DEPTH_ATR`, `BAND_EVENT_MAX_BARS`) | 2026-07-10 (Event Map Task 11); chronological pairs per the operator's rail ruling 2026-07-11; sequence-aware HOLD chain + depth(5 ATR)/duration(20 bar) caps 2026-07-16 (solve-the-engine task 9) | **FLIPPED LIVE 2026-07-16** — operator grant (flip checklist #2), after the A/B rail eyeball landed FAVORABLE 2026-07-11 (all Phase C rulings correct; BODI's flag-ON election acceptable to his eye). Last-resort pair-election pool (consulted only when strict AND rescued pools are empty; outer Phase B only); a qualified DEEP below-rail event may measure up to `BAND_MAX_BOX_WIDTH = 0.23` wick-to-wick and types as `bin_c_type = TERMINAL_SHAKEOUT`; the HOLD chain reads progressive step-down spring sequences; the two caps kill the EGBN breakdown class (7.68–9.98 ATR / 40-bar "events"). **The flip battery caught two negative-corpus regressions and both were closed with knob-free bounds before the reseal:** DBD (dead-space tier-S election riding a 15-bar 4.1-ATR rally ABOVE R typed as a "poke") → above-rail spans are now bounded by the respect gate's own forgiveness horizon (`MAX_CONSECUTIVE_OUTSIDE_DAYS`); SPCB (a 34-bar high-flag whose left half was the +35% rally leg, tier-A on 30 judged churn bars) → the judged window must be a matured cause, ≥ 2 × `MIN_BASE_DAYS` bars after excision (a terminal shakeout ends a LONG Phase B). Flip battery thereafter: full pytest, shadow, marks ratchet resealed with the stage-matched BODI `band-vs-excursion` conversion (fires at the operator's exact 12.33/10.18 rails), EGBN stayed dead, all 17 negatives reject, hermetic recall green. EC-8 universe cost bound measured hermetically at flip (full 5,487-ticker cache, three passes): flag-ON adds ~+8–19% evaluation-phase cost (1041–1144s OFF vs 1240s ON; ~+2–3 min per nightly scan) and moves universe fires 317 → 331 (+14, +0.26% of universe). Frontend needed no change (Phase-C region draws type-agnostic off `bin_c_present`) | `engine_alpha/structure/box/rail_qualification.py` (formerly `band_rails.py`) `qualify_pair_events`; [archive/PLAN-event-tape.md](archive/PLAN-event-tape.md) Task 11; [archive/PLAN-solve-the-engine.md](archive/PLAN-solve-the-engine.md) task 9 |
| `LPS_HOLDING_SHELF_ENABLED` (+ `LPS_SHELF_LENGTH_MIN`, `LPS_SHELF_MIN_LOW_POS_BOX`) | 2026-07-10 (Event Map Task 8) | **FLIPPED LIVE 2026-07-16** — operator grant (solve-the-engine flip checklist #1). The GEOMETRY-ONLY second LPS completion form (two-form doctrine, [lps_final_structure_canon_2026-07-10.md](lps_final_structure_canon_2026-07-10.md)): a monotone-non-rising, upper-half holding shelf accepted where the pullback form rejects at its depth / volume-dry-up judgments. Flip battery: full pytest, shadow baseline re-captured, marks ratchet re-frozen with the stage-matched WTS + PBT `holding-shelf-lps` conversions (DRTS stays red on its frozen frame — box election fails pre-LPS, a data-drift case that already fires as a fresh-data seed hit), negative corpus + hermetic seed-recall green, harness delta names the ENGINE as the moved axis | `engine_alpha/structure/lps/detection.py` `_holding_shelf_verdict`; [archive/PLAN-event-tape.md](archive/PLAN-event-tape.md) Task 8 |
| `PIP_PIVOTS_ENABLED` (+ `PIP_PIVOTS_DIST_MIN`) | 2026-06-25 (`c08e61f`) | **DELETED 2026-07-03** — eyeball-rejected as a wash (fixes some inverted climax→AR overlays, creates others: GBTG/PLSE/CGNX, `d43e7fd`); dead branches removed from `segment_swings` / `read_market_structure`, keys removed from the freeze manifest. the module itself stays (`macro_bridge_zigzag` refines the same substrate; now `phase_a.py`, formerly `pip.py`) | [pip_macro_phase_a.md](pip_macro_phase_a.md) §history; byte-parity gates green at deletion |
| `CANDLE_SPREAD_AWARE` | 2026-06-30 (`ed6b82e`) | **FLIPPED LIVE 2026-07-04** — operator A/B eyeball (`tools/candle_ab.py` — tool retired 2026-07-18, 44f8293): 61/128 choppy bases docked (mean −0.39, min −3.0), 0 tier flips, clean bases preserved at grade 1.0; shadow baseline re-captured. **FOLDED 2026-07-18** (Purity task 7): the conditional is gone from `score_setup`; key retained until the Task 11 manifest rotation; `candle_ab.py` A/B is inert pending its Task 9 kill | `settings.CANDLE_SPREAD_AWARE`; E1 Track B A/B |
| `PUZZLE_SCORE_ENABLED` | 2026-06-30 (`d2db80c`) | **FLIPPED LIVE 2026-07-04** — operator A/B eyeball (`tools/puzzle_ab.py` — tool retired 2026-07-18, 44f8293): 128/128 fires lifted (mean +4.5, max +8.0), 22 tier flips; shadow baseline re-captured. Forward-return validation still due ~2026-07-15+ (a revisit, not a blocker). **FOLDED 2026-07-18** (Purity task 7): term + narrative compute unconditional (`score_setup`, `taxonomy`, evaluation); key retained until the Task 11 manifest rotation; `puzzle_ab.py` A/B is inert pending its Task 9 kill | `settings.PUZZLE_SCORE_ENABLED`; [engine_pass/](engine_pass/) E2/E3 |
| `PIP_MACRO_PHASE_A_ENABLED` | 2026-07-02 (`25b98cb`) | **FLIPPED LIVE 2026-07-04** — operator eyeball on the overlay A/B (`tools/phase_a_pip_diff.py` — tool retired 2026-07-18, 44f8293): 19/140 overlays re-anchor to the genuine trend-top→reaction, 0 stolen climaxes in EITHER mode, 0 score/fire/tier (Phase-A overlay only). The phase-ordering hold was a REAL overlay defect (macro AR painted inside the box): the box-overrunning macro bridge now abstains (`142bf50`, `bridge_end_max=phase_b_start_bar`); the sibling always-on order-N hole was closed (`5882226`); `test_segment_swings_finds_root_bridge` pinned flag-off (`b514f49`). **FOLDED 2026-07-18** (Purity task 7): the macro read is unconditional in `segment_swings`; the order-N unit test re-pinned to a macro-abstention monkeypatch (same fallback seam); key retained until the Task 11 manifest rotation; `phase_a_pip_diff.py` A/B is inert pending its Task 9 kill | [pip_macro_phase_a.md](pip_macro_phase_a.md); `test_invariants::test_phase_ordering_monotone` |
| `LPS_REQUIRE_PEAK_DOWN` (+ `LPS_PEAK_DOWN_TOL_BOX`) | 2026-06-24 (`dba8005`) | **DELETED 2026-07-06 (engine-α)** — the census found NO peak-down threshold that separates winners, and the OHI run-up class it targeted is killed by the live markup gate (`LPS_RESCUE_MAX_ADVANCE_BOX = 0.21`); the ledger row already recommended deletion. Removed the `find_lps` gate branch + both settings keys + manifest entries + the 3 flag-on tests; config-hash regenerated, shadow byte-identical (the flag was off, so no reading output moved) | council α-audit (2026-07-06) flag reconciliation; byte-parity gates green at deletion |
| `LPS_RESCUE_MAX_ADVANCE_BOX` | 2026-06-26 | **BLESSED LIVE for engine-α 2026-07-06** — the "reaction-not-markup" gate on the rising-support-shelf rescue (net advance/box > 0.21 → reject). Enabled + committed `232afd0` ("Enable LPS calibration: rescue-markup gate 0.21 + hold tolerance 0.95"); validated 2026-06-26 (seed-recall 0 curated winners dropped; drops OHI/AEF/NVT/SPCB run-ups, keeps NMAI's gradual shelf). The council α-audit flagged a stale "HELD uncommitted, revert to None" settings comment with no ledger row; comment corrected + this row recorded so the frozen base does not carry an un-blessed live gate. `None` = disabled (safe default) | `settings.LPS_RESCUE_MAX_ADVANCE_BOX`; `232afd0`; [project_lps_peak_down] |
