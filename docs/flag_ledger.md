# Flag ledger — dark engine flags and the decision each is waiting on

Every flag-gated engine capability that shipped **default-off** is listed here
with the decision that blocks its flip and a **kill-by date**. A dark flag is
not free: it is a dead branch in the reader, a row in the freeze manifest, and
a standing "we'll validate later" IOU. By its kill-by date a flag must be
**flipped live**, **deleted**, or **re-dated with a written reason** — never
silently carried.

Maintenance rules:
- A new default-off engine flag lands with a row here **in the same change**.
- Flips/deletions move the row to *Retired* (keep the evidence trail).
- Live flags (`TIGHTNESS_ADR_AWARE`, `HTF_CONTEXT_ENABLED`, ...) are
  deliberately NOT here — they are engine identity (frozen in
  `core/freeze/manifest.py`), not pending decisions. A live flag whose
  validation window has closed gets **FOLDED** (the conditional is removed and
  the behavior becomes unconditional engine code; the settings key survives
  untouched until the next manifest rotation retires it) — the weeks-validated
  2026-06/07 flips (`TRAVERSAL_GATE_ENABLED` first, 2026-07-18) fold this way
  under the Purity Pass.

## Dark flags (default-off, decision pending)

| Flag | Built | Blocking decision | Evidence | Kill-by |
|---|---|---|---|---|
| `AR_FIRST_REACTION_ENABLED` | 2026-07-04 (raw-bar); trend-derived 2026-07-05; **big-bounce retarget 2026-07-05** | Re-anchors the drawn Phase-A automatic reaction to **the low of the first *continuous* reaction after the terminal swing** (`market_structure.first_reaction_after`): retrace basis = the trend's **FULL leg** (elected segment start, `elected_trend_leg_base`), closed at the first **big confirmed bounce** off that low (no stall). Retargeted from the terminal-sub-leg+stall read after the operator's dated BC/AR marks on **PH/TOL/AVNT/AAP/AGCO/TFX** (2026-07-05) showed it over-tightened the clean reactions — the retarget is exact on TOL/AVNT and surgically fixes only the AAP-class second-leg overshoots. Tighten-only + overlay-only. Gates green: full pytest, **shadow `--check` flag-OFF byte-identical**, hermetic seed-recall; `CANONICAL_FIELDS` exclude the AR bars so a flip is byte-identical on scoring/tier/canonical outputs. **NOT fully byte-identical downstream:** flipping shifts the ARCHIVED `bin_a_*` measurement columns (`structure.ar_bar` → `measure_phases`, consumed by `analyze.py`), which no freeze gate covers — a live flip needs a `bin_a_*` guard / `engine_config_version` partition in `analyze.py` FIRST or it silently mixes two AR-mode populations into the fingerprint pool. Universe scan: **19/140 overlays re-anchor** (median 8, max 53 bars; AEBI −53 / TAC −47 / AES −38 = the "smear" cases; AAP −12) — more conservative than the raw-bar 37/140. **Council-reviewed for engine-α (2026-07-06): stays default-OFF in α** (the archive seam is a β-flip prerequisite); the retarget rides into α as measure-only substrate, flag off | `settings.AR_FIRST_REACTION_ENABLED`; [strategy_alpha.md](strategy_alpha.md) "Trend & Change of Character" + [engine_reference.md](engine_reference.md) "Phase A — First-reaction AR anchor" | 2026-08-15 |
| `TREND_TERMINAL_BOX_GATE_ENABLED` | 2026-07-27 (anchor-polarity ruling) | **A box may not OPEN before the trend running into it printed its climax — and if that climax lands INSIDE the box, at least `MIN_BASE_DAYS` (20) bars must have printed since.** Operator ruling 2026-07-27 (LIVN): *"We can't start the anchor from the opposite direction of the trend if we are still inside that trend"* + *"way too young to be considered anything but some false breach of the range."* Judged on each candidate's BACK-EXTENDED start against `market_structure.trend_terminal_floor` — per-bar terminal of the covering **confirmed** trend segment (first-write-wins so the CAUSE trend owns the handover bar; segments still RUNNING never veto, their extreme being unconfirmed). Filters the elected candidate list in `validate_equilibrium`. **Overshoot magnitude was falsified 3x and is NOT used:** the operator accepts 47.9% (PXS), 82% (VIK), 101% (MATX) of a box height past R and rejects LIVN at 20.57% — a `TREND_TERMINAL_OVERSHOOT_BOX` knob was built, measured and removed; do not re-propose. **Evidence:** engine's own `segment_trends` put LIVN's uptrend terminal at 2026-07-06 while the box opened 2026-06-18, anchoring the climax on an `HH/up` bar and the AR on an `HL/up` bar. Operator marks obey the rule 31/33 (93.9%). **Gates:** sealed marks ratchet **28/33 PASS flag-ON**; full pytest green flag-OFF; doctrine gate unchanged flag-OFF. Live A/B: 293 identical / 39 lost / **0 moved**, 35 of 39 losses opening Jun-Jul 2026. **Blocking decision:** operator eyeball of the 39 lost boxes — this is the first anchor fix that removes setups rather than repainting an overlay | `settings.TREND_TERMINAL_BOX_GATE_ENABLED`; [engine_reference.md](engine_reference.md) "Phase B — trend-terminal box gate" | 2026-08-31 |
| `TA_SCORE_V2` | 2026-07-01 (`d4c90bd`) | BUILD COMPLETE 2026-08-08 (14 build commits `9085e93..68540c0`, reweight-neutral) + COUNCIL-REVIEWED 2026-08-08: 15 findings (4 P1) ALL FIXED in-branch — manual-writer family splat, A/B identity on the pre-warning operand, EC-17 input-tied cascade, vocabulary-derived leak guard; chapter vocabulary re-ruled to the operator's phase-overlay words (Cause → Phase B → Phase C → Phase D → Trend): chapter composite + story terms + charter measurements + fired_tags + lens + A/B instrument all dark and EC-17-covered. REMAINING = the operator's A/B eyeball (`python -m tools.ta_grade_ab`) → the flip commit per [ta_grade_flip_checklist_2026-08.md](ta_grade_flip_checklist_2026-08.md) (weights + TIER_*_STRUCT cuts + the atomic re-bless are flip-commit work) | [ta_grade_flip_checklist_2026-08.md](ta_grade_flip_checklist_2026-08.md), [../specs/ta-score-hybrid-design.md](../specs/ta-score-hybrid-design.md) | 2026-08-31 |
| `FUNDAMENTALS_ENABLED` (Lane C) | 2026-06-29 (`b8753a1`) | The scoring/enrichment wire-up wave (fundamentals feeding score/archive) — substrate is built and unit-tested, consumption wave not yet scheduled | `core/fundamentals/` docstrings | 2026-09-30 |
| `RS_LINE_ENABLED` (Lane C) | 2026-06-29 (`b8753a1`) | Same wire-up wave (RS-line new-high chip / score input) | `core/regime/rs_line.py` docstring | 2026-09-30 |
| `SECTOR_RANKING_ENABLED` (Lane C) | 2026-06-29 (`b8753a1`) | Same wire-up wave (sector-RS context for the health board / regime label) | `core/regime/sector_ranking.py` docstring | 2026-09-30 |
| `ELECTION_TRACE_EXPORT_ENABLED` | 2026-08-04 (Surface the Read Task 4) | **The walk's own narration, published:** `read_structure`'s trace captured during the SAME election that fired (never a re-run) and summarized engine-side (`structure/trace_export.py`) to a compact date-anchored story — per-root climax/AR dates + outcome + per-stage refusal counts + how far the best candidate got (`terminal_verdict`, the ONE summarizer) + the elected framing's provenance; gate-leg sentences render from the structured leg records through the one operator-language vocabulary (`leg_sentence`), never internal `detail` prose. The RAW trace never leaves the engine — measured 2026-08-04 over 40 real fires: full-trace JSON median ~106 KB / p90 3.6 MB / max 5.8 MB per ticker. Archived raw per fire (`election_trace` TEXT, model-only column; NULL = never captured, never backfilled) and served in the payload narrative block (parsed). Changes NO election/gate/score — capture cost only: **measured +20.3 ms per evaluated ticker ≈ +111 s per full US-Stocks scan** (trace-off 477.1 vs trace-on 497.4 ms/t, n=40 live prep). **Blocking decision:** operator flip after the bound re-measures in the scan-metrics evaluation phase on a real nightly scan | `settings.ELECTION_TRACE_EXPORT_ENABLED`; [engine_reference.md](engine_reference.md) "The narrative fact block (Surface the Read)" | 2026-09-30 |
| `STRATEGY_READ_ENABLED` | 2026-08-04 (Surface the Read Task 13) | **Held-through-correction, measure-first:** two RAW campaign-context measures per fire (`structure/strategy_read.py`, family pattern): `strategy_correction_depth_pct` (how deep the elected base's floor sits below the resolved climax bar's high, fraction of that high — SIGNED: negative = the floor holds ABOVE the climax high, the selling-climax recovery shape; ALB is the live example) and `strategy_floor_above_ar` (0/1: the floor held at/above the automatic reaction's low). Reads ONLY facts the walk already resolved (climax/AR bars + elected S) — no new detection, two array lookups per FIRE, expected evaluation delta ≈ 0 s. Never gates, never scores, NULL = never measured; **a ruled judgment over these is a LATER archive calibration, deliberately NOT an add-time threshold** (measure-first doctrine). EC-17 happy path committed on the story-pool-guard template (real cascade, only this flag forced on, flag-off leg byte-identical per identity). **Blocking decision:** operator flip after the ScanTimer re-measure on a real nightly scan | `settings.STRATEGY_READ_ENABLED`; [engine_reference.md](engine_reference.md) "The narrative fact block (Surface the Read)" | 2026-09-30 |
| `ELECTION_STABILITY_ENABLED` (+ `ELECTION_STABILITY_LOOKBACK`) | 2026-07-11 (Calibration at Scale Task 14) | Measure-only persistence probe on FIRING setups: re-run the eval-twin prep + the structure election alone at D−1..D−k (backward ONLY — no lookahead can enter the archive) and grade "same reading?" via the one cross-frame identity predicate (`core/pipeline/election_identity.same_election`: box-start DATE + rails within 0.10 box-height). Emits `_stability_same_frac`/`_streak`/`_probes`/`_refused` (underscore diagnostics; archive family is a later stage; `_refused` counts prep-refused shifts — universe-gate flicker — apart from election flicker, though both grade not-same in same_frac). Evidence: junk flickers (BODI's flag-ON band pair exists 04-15, dies 04-16) while real boxes persist. **Measured cost (the flip evidence, k=3, corpus fixture fires): mean ≈2.75s per firing ticker** (each probe = full twin prep + election; ~+4.5 min per 100-fire scan) — a flip likely wants k=1..2 or an election-only fast path measured first. HONEST SEMANTICS NOTE (corrected per Council Review 2026-07-11 finding 9): the probe measures backward persistence of the BOX election, not the fire — a box can elect well before its LPS completes, so same_frac at a first fire can be anywhere in 0..1 (corpus fires measured mostly 0 = the election churned into those fires; AVT 0.33 = pre-fire persistence exists). Interpret at calibration time, never as a same-day quality gate. Inert battery: flag-off probe NEVER consulted (raise-if-touched), deterministic raw values, ledger+manifest registered | `core/pipeline/stability.py`; [../PLAN-calibration-at-scale.md](../PLAN-calibration-at-scale.md) Task 14 | 2026-09-15 |



## THE 2026-07-18 rotation (behavior-identical; a named archive seam)

The Purity Pass Task 11 rotation retired the seven folded flag keys in ONE
deliberate `engine_config_version` event:
`TRAVERSAL_GATE_ENABLED`, `CANDLE_SPREAD_AWARE`, `PUZZLE_SCORE_ENABLED`,
`PIP_MACRO_PHASE_A_ENABLED`, `SOS_TRIM_ENABLED`, `BOX_BACKEXT_ENABLED`,
`DESCENT_TAIL_GATE_ENABLED` — each already unconditional engine code (task-7
folds, every one parity-proven). The before/after manifest dump proved a **pure
7-key deletion bijection**: zero keys added, zero values or JSON types moved.

- Hash equivalence: `91c70bdbd6a6686099ca2173250e602fd89120cc6df9e8631e62390a125640c6`
  → `5a4d2282474457ec70cde167a78bf6c80f83d15971d7f4df3bdb18f76bc1134d` is a
  **behavior-identical rotation** — the reading did not move; only the key set did.
- Archive seam: analyses that partition by `engine_config_version` should treat
  these two hashes as the same engine (like the 06-26 data seam, this is a
  bookkeeping cut, not a behavior cut).
- Hash-consumer stances, ruled deliberately: the calibration harness `--prev`
  comparison will report "ENGINE moved" across this commit — a false alarm to
  caveat in the next report; every mark's staleness chip flips (expected —
  marks keep their birth hash, no re-stamping); future seam analyses cut here.

## Retired

| Flag | Built | Outcome | Evidence |
|---|---|---|---|
| `NEAR_MISS_LANE_ENABLED` | 2026-07-26 (near-miss lane Tasks 7–11; council-reviewed 2026-07-26, 15 findings fixed) | **FLIPPED LIVE 2026-07-27** — operator decision against the §6b re-measured A/B, verbatim "3% aint a biggie lets flip it": ALL THREE pre-registered bounds FAILED (p50 +7.10 ms vs ≤+1; p95 +57.97 ms vs ≤+10; corpus wall +2.96% vs ±2%) — the overrun is ACCEPTED, the budget is NOT retro-widened (EC-15). The one-leg box-gate refusal collector + bounded deferred completion + cohort archive + report CLI; a near-miss NEVER scores/fires/enters the picks — flag-on adds archive rows and the report cohort ONLY (picks byte-untouched by design, guard-pinned). **The flip HARDENS ruleset `2026-07-26.A` + the per-pool dedup grain (strict > rescued > band)** — a future re-rule is now an archive-seam event (EVERY distribution re-bases). Flip mechanics: census re-pin with the axis named (engine `28498359…` → `c1a5cca0…`, manifest-ONLY drift — fingerprint/junk/one-leg/ruled all held), full suite green flag-ON. Cost levers stay on the shelf if the live wall hurts: `NEAR_MISS_TOP_K` cut; early-exit completion (sized 335-of-431 not_ruled, §6 lever 2). First live cohort: the next scheduled scan; review via `python -m tools.near_miss_report` (`--outcomes` after maturation). Standing gate: `python -m tools.near_miss_census --check` | `engine_alpha/structure/near_miss.py`; [near_miss_lane_2026-07.md](near_miss_lane_2026-07.md) §6b/§8; guards `tests/test_near_miss_guards.py` |
| `STORY_POOL_ENABLED` | 2026-07-25 (Event Map program Task 8) | **FLIPPED LIVE 2026-07-26** — operator per-fire chart eyeball passed (the final gate): both A/B conversions confirmed real pre-breakout structure at the ENGINE's elected rails ("the Support and Resistance on both is correct"); YPF ruled early-but-right (post-window it broke out, LPS'd 05-12..05-15, then broke out truly — exactly what an inside pre-breakout surfacing should look like); NKTR's pre-base gap ruled irrelevant to the box's traits. Story-rescue last-resort pool on the RULED admission form (census Option A, operator-ruled 2026-07-25: >=2 completed support tests + terminal resistance posture + no drift, `event_map.story_admission`), consulted only when strict/rescued/band pools are ALL empty. **Fire A/B executed 2026-07-25: 26→28/33** — NKTR@2026-04-09 (tier A, `R+ S+ R+ S+ R+ S+~ R^`) + YPF@2026-05-06 (tier S, `S+ R+ S+ R^`); all 26 prior hit elections per-identity byte-identical, shadow panel identical, negative bench fully refused incl. RLGT, zero junk fires. **The pre-registered "EGBN converts" clause FAILED — recorded honestly:** admission-certain at the drawn rails but its framing is never PROPOSED at a story-passing shape (rail PLACEMENT, upstream of any pool). Flip mechanics: deliberate ratchet reseal 26→28 (`marks_corpus --build-fixture`; STAGE_TAGS entries removed), election-stability guard re-pinned with the two story-caused hits named as the ONLY legally off-silent identities, floors 26→28. **Cost (re-measured 2026-07-26 post council-review reorder):** median +3.3 ms, mean +12.6 ms, tail +140 ms per consultation; ~+5–15 s scan-level (inside the BAND precedent); EVENT_MAP substrate side median +2.2 ms. The flip rotates `engine_config_version` (archive seam). Remaining safe perf lever: per-root dedup; the survivor cap is NOT a perf knob (election-changing, needs the full A/B) | `engine_alpha/structure/box_primitives.py` pool seam; [strategy_alpha.md](strategy_alpha.md) "The rail-episode read"; **committed evidence: [event_map_program_2026-07.md](event_map_program_2026-07.md)** (census pins + fire A/B + cost + eyeball verdict; standing gate `python -m tools.event_map_census --check`) |
| `EVENT_MAP_ENABLED` | 2026-07-10 (Event Map Task 6; rail-episode substrate 2026-07-25) | **FLIPPED LIVE 2026-07-25** — operator grant at the Event Map program scope gate (PLAN-event-map Task 13). Fires-only measure-only compute: the stamped swing map + role labels + the rail-episode substrate (as-of sentence over the ELECTED window/rails — the same READER the story pool consults but a DIFFERENT basis than its admission read, so the two may legally disagree (YPF class); a story fire's admitting evidence is archived separately in `story_admission_profile`), archived as the `event_map_*` column family (typed scalars + readability companion + one compact date-anchored tape). Map-ON parity contract held as an executable assertion with a demonstrated red step: fires/elections/scores byte-identical, ONLY archive columns gain values. Cost (Task 12 A/B): median +2.2 ms per firing ticker, max +10.6 (the tape precedent's order). The flip rotates `engine_config_version` — the family's FIRST archive seam; pre-flip NULL rows are never backfilled | `engine_alpha/structure/event_map.py`; [strategy_alpha.md](strategy_alpha.md) "The Event Map" + "The rail-episode read"; committed evidence: [event_map_program_2026-07.md](event_map_program_2026-07.md) |
| `EQ_DWELL_BAR_BASIS` | 2026-07-25 (`3a78f09`) | **TESTED-DEAD 2026-07-25, flag removed same day** — the sealed fire A/B: EGBN converts AT the drawn rails (first_fire 2026-01-02, the operator's named support-test bar) but 10/26 hit identities break (MATX lost entirely, FOSL/NGL/NTCT/ROIV/WTS displaced winners, BWA/EWTX/MS/VIK re-dated) AND junk DGII+FLG fire — close-residence dwell is load-bearing for election stability ("rails do not drift" measured). `_dwell_bar_basis` survives as a MEASURE-only helper; never re-wire it into the gate | [bar_dwell_protocol_2026-07.md](bar_dwell_protocol_2026-07.md) §8 |
| `CAUSE_BEFORE_EFFECT_VETO_ENABLED` (+ `CAUSE_LPS_LOOSE_MAX`) | 2026-07-20 (PLAN-cause-before-effect, council-implement) | **FLIPPED LIVE 2026-07-20** — operator grant after the live A/B eyeball. Cause-before-effect election precondition: a box may not be elected over a live trend that never matured a cause (the MIDD FP class). `bricks.cause_maturity` abstains the whole Structure (`return None`) ONLY when ALL THREE reads agree the cause is absent: `macro_bridge_zigzag` (box-INDEPENDENT) abstains AND `read_swing_map` reads a live up-staircase (`pre_box`/`box` both `'up'`) AND the elected LPS's final shelf never tightened (`lps.tightness_ratio > CAUSE_LPS_LOOSE_MAX` = 0.90). The shelf leg (separator hunt 2026-07-20, wf_8a6fdb42) is the AND-narrowing third operand: MIDD's shelf is the census-loosest (0.957) while all 43 seeded winners tightened more (nearest 0.842), so it rescues the tight-shelf winners (BP/LECO/MEOH/NTCT/VLO, `tightness_ratio < 0.71`) the first two reads alone dropped. Depth-free (CTOS keeps its bridge — NOT the census-killed above-R cap) and fail-OPEN (degenerate frame / non-'up' / bad ATR / missing shelf never vetoes — recall is the gate). **Recall proof:** hermetic seed recall 43/43 (0 dropped), operator-marks ratchet held, MIDD rejects. **Live A/B eyeball (fired set 2026-07-17, 234 names):** removes 7 / keeps 227 — MIDD + FLG (operator-ruled "trend + dips, not a base") blessed, and BNS/CVCO/HWM/NTCT(current base, NOT the seed winner)/TPL all operator-confirmed skips; zero important names touched. Council-reviewed 2026-07-20 (10 seats, adversarially verified): no correctness/security/recall defect; 2 P2 fixed. Flip battery green. **EC-8 eval-phase cost bound (500-frame universe sample):** +0.4% (~+2.7 ms/ticker, ~15s per full-universe nightly scan) — one extra `macro_bridge_zigzag` per elected box + `read_swing_map` on abstain. The flip rotates `engine_config_version` (the archive seam — the only boundary where a MIDD-class name abstains). | `engine_alpha/structure/bricks.cause_maturity`; `engine_alpha/structure/narrative.read_structure`; [strategy_alpha.md](strategy_alpha.md) "Cause before effect"; [../PLAN-cause-before-effect.md](../PLAN-cause-before-effect.md) |
| `ELECTION_DETHRONE_ENABLED` (+ `ELECTION_DETHRONE_SESSIONS`) | 2026-07-16 (PLAN-solve-the-engine task 13) | **FLIPPED LIVE 2026-07-16** — operator grant (flip checklist #4). Stale-frame dethronement: a rescue-propped (SOS-trim) framing whose buffered R the tape has left FULLY behind for the trailing 10 sessions loses the election **in favor of a later valid framing** (never into an emptier read); dethroned pairs narrate as `stage=dethroned` in the cascade trace. Blast radius pinned hermetically: flag-on over every sealed-corpus pinned hit at its first-fire session, zero elections change hands (`tests/test_election_surgery.py`). Flip battery green; harness: MATX fires in-window tier S. **The sibling rescued-pool arbitration lever was built and REJECTED** — it killed VIK's pinned corpus hit; counterexample recorded at the `box_primitives` pool seam; do not re-attempt without new corpus evidence | `engine_alpha/structure/box_primitives.py` pool seam; [../PLAN-solve-the-engine.md](../PLAN-solve-the-engine.md) task 13 |
| `LPS_OVERSHOOT_WINDOW_ATR_ENABLED` (+ `LPS_OVERSHOOT_WINDOW_ATR_MULT`) | 2026-07-16 (PLAN-solve-the-engine task 10) | **FLIPPED LIVE 2026-07-16** — operator grant (flip checklist #3). For **OVERSHOOT_R windows only** (breakout throwbacks resting ABOVE R), the window-localization gate's denominator becomes `max(box_height, 2.0 × ATR)` — above a NARROW box, box height is the wrong yardstick (CTOS's marked shelf: 1.06 box-heights but only 1.42 ATR; k ≥ 1.67 admits it, 2.0 = margin). Gate-only: the archived `window_range_pct_box` measure is unchanged; wide boxes and INSIDE/UNDERCUT_S zones untouched (unit-pinned); non-finite ATR refuses the rescoped path. Flip battery green; harness: CTOS fires 2026-07-15 tier S at rails within tolerance, span overlap 1.0. **HARDENED 2026-07-17** after the operator's BBVA eyeball ("just incomplete"): the rescope only engages on a **matured cause** (`base_len ≥ 2 × MIN_BASE_DAYS`, the terminal-shakeout floor applied to the opposite rail) — immature causes fall back to the raw gate. BBVA's fixture frame frozen as negative case `BBVA@2026-06-05`; shadow drop was exactly BBVA, CTOS byte-identical, 18/18 negatives, ratchet 9/11 held | `engine_alpha/structure/lps.py` window gate; [../PLAN-solve-the-engine.md](../PLAN-solve-the-engine.md) task 10 |
| `BAND_RAILS_ENABLED` (+ `BAND_MAX_BOX_WIDTH`, `BAND_EVENT_MIN_BARS`, `BAND_EVENT_MAX_DEPTH_ATR`, `BAND_EVENT_MAX_BARS`) | 2026-07-10 (Event Map Task 11); chronological pairs per the operator's rail ruling 2026-07-11; sequence-aware HOLD chain + depth(5 ATR)/duration(20 bar) caps 2026-07-16 (solve-the-engine task 9) | **FLIPPED LIVE 2026-07-16** — operator grant (flip checklist #2), after the A/B rail eyeball landed FAVORABLE 2026-07-11 (all Phase C rulings correct; BODI's flag-ON election acceptable to his eye). Last-resort pair-election pool (consulted only when strict AND rescued pools are empty; outer Phase B only); a qualified DEEP below-rail event may measure up to `BAND_MAX_BOX_WIDTH = 0.23` wick-to-wick and types as `bin_c_type = TERMINAL_SHAKEOUT`; the HOLD chain reads progressive step-down spring sequences; the two caps kill the EGBN breakdown class (7.68–9.98 ATR / 40-bar "events"). **The flip battery caught two negative-corpus regressions and both were closed with knob-free bounds before the reseal:** DBD (dead-space tier-S election riding a 15-bar 4.1-ATR rally ABOVE R typed as a "poke") → above-rail spans are now bounded by the respect gate's own forgiveness horizon (`MAX_CONSECUTIVE_OUTSIDE_DAYS`); SPCB (a 34-bar high-flag whose left half was the +35% rally leg, tier-A on 30 judged churn bars) → the judged window must be a matured cause, ≥ 2 × `MIN_BASE_DAYS` bars after excision (a terminal shakeout ends a LONG Phase B). Flip battery thereafter: full pytest, shadow, marks ratchet resealed with the stage-matched BODI `band-vs-excursion` conversion (fires at the operator's exact 12.33/10.18 rails), EGBN stayed dead, all 17 negatives reject, hermetic recall green. EC-8 universe cost bound measured hermetically at flip (full 5,487-ticker cache, three passes): flag-ON adds ~+8–19% evaluation-phase cost (1041–1144s OFF vs 1240s ON; ~+2–3 min per nightly scan) and moves universe fires 317 → 331 (+14, +0.26% of universe). Frontend needed no change (Phase-C region draws type-agnostic off `bin_c_present`) | `engine_alpha/structure/rail_qualification.py` (formerly `band_rails.py`) `qualify_pair_events`; [../PLAN-event-tape.md](../PLAN-event-tape.md) Task 11; [../PLAN-solve-the-engine.md](../PLAN-solve-the-engine.md) task 9 |
| `LPS_HOLDING_SHELF_ENABLED` (+ `LPS_SHELF_LENGTH_MIN`, `LPS_SHELF_MIN_LOW_POS_BOX`) | 2026-07-10 (Event Map Task 8) | **FLIPPED LIVE 2026-07-16** — operator grant (solve-the-engine flip checklist #1). The GEOMETRY-ONLY second LPS completion form (two-form doctrine, [lps_final_structure_canon_2026-07-10.md](lps_final_structure_canon_2026-07-10.md)): a monotone-non-rising, upper-half holding shelf accepted where the pullback form rejects at its depth / volume-dry-up judgments. Flip battery: full pytest, shadow baseline re-captured, marks ratchet re-frozen with the stage-matched WTS + PBT `holding-shelf-lps` conversions (DRTS stays red on its frozen frame — box election fails pre-LPS, a data-drift case that already fires as a fresh-data seed hit), negative corpus + hermetic seed-recall green, harness delta names the ENGINE as the moved axis | `engine_alpha/structure/lps.py` `_holding_shelf_verdict`; [../PLAN-event-tape.md](../PLAN-event-tape.md) Task 8 |
| `PIP_PIVOTS_ENABLED` (+ `PIP_PIVOTS_DIST_MIN`) | 2026-06-25 (`c08e61f`) | **DELETED 2026-07-03** — eyeball-rejected as a wash (fixes some inverted climax→AR overlays, creates others: GBTG/PLSE/CGNX, `d43e7fd`); dead branches removed from `segment_swings` / `read_market_structure`, keys removed from the freeze manifest. the module itself stays (`macro_bridge_zigzag` refines the same substrate; now `phase_a.py`, formerly `pip.py`) | [pip_macro_phase_a.md](pip_macro_phase_a.md) §history; byte-parity gates green at deletion |
| `CANDLE_SPREAD_AWARE` | 2026-06-30 (`ed6b82e`) | **FLIPPED LIVE 2026-07-04** — operator A/B eyeball (`tools/candle_ab.py`): 61/128 choppy bases docked (mean −0.39, min −3.0), 0 tier flips, clean bases preserved at grade 1.0; shadow baseline re-captured. **FOLDED 2026-07-18** (Purity task 7): the conditional is gone from `score_setup`; key retained until the Task 11 manifest rotation; `candle_ab.py` A/B is inert pending its Task 9 kill | `settings.CANDLE_SPREAD_AWARE`; E1 Track B A/B |
| `PUZZLE_SCORE_ENABLED` | 2026-06-30 (`d2db80c`) | **FLIPPED LIVE 2026-07-04** — operator A/B eyeball (`tools/puzzle_ab.py`): 128/128 fires lifted (mean +4.5, max +8.0), 22 tier flips; shadow baseline re-captured. Forward-return validation still due ~2026-07-15+ (a revisit, not a blocker). **FOLDED 2026-07-18** (Purity task 7): term + narrative compute unconditional (`score_setup`, `taxonomy`, evaluation); key retained until the Task 11 manifest rotation; `puzzle_ab.py` A/B is inert pending its Task 9 kill | `settings.PUZZLE_SCORE_ENABLED`; [engine_pass/](engine_pass/) E2/E3 |
| `PIP_MACRO_PHASE_A_ENABLED` | 2026-07-02 (`25b98cb`) | **FLIPPED LIVE 2026-07-04** — operator eyeball on the overlay A/B (`tools/phase_a_pip_diff.py`): 19/140 overlays re-anchor to the genuine trend-top→reaction, 0 stolen climaxes in EITHER mode, 0 score/fire/tier (Phase-A overlay only). The phase-ordering hold was a REAL overlay defect (macro AR painted inside the box): the box-overrunning macro bridge now abstains (`142bf50`, `bridge_end_max=phase_b_start_bar`); the sibling always-on order-N hole was closed (`5882226`); `test_segment_swings_finds_root_bridge` pinned flag-off (`b514f49`). **FOLDED 2026-07-18** (Purity task 7): the macro read is unconditional in `segment_swings`; the order-N unit test re-pinned to a macro-abstention monkeypatch (same fallback seam); key retained until the Task 11 manifest rotation; `phase_a_pip_diff.py` A/B is inert pending its Task 9 kill | [pip_macro_phase_a.md](pip_macro_phase_a.md); `test_invariants::test_phase_ordering_monotone` |
| `LPS_REQUIRE_PEAK_DOWN` (+ `LPS_PEAK_DOWN_TOL_BOX`) | 2026-06-24 (`dba8005`) | **DELETED 2026-07-06 (engine-α)** — the census found NO peak-down threshold that separates winners, and the OHI run-up class it targeted is killed by the live markup gate (`LPS_RESCUE_MAX_ADVANCE_BOX = 0.21`); the ledger row already recommended deletion. Removed the `find_lps` gate branch + both settings keys + manifest entries + the 3 flag-on tests; config-hash regenerated, shadow byte-identical (the flag was off, so no reading output moved) | council α-audit (2026-07-06) flag reconciliation; byte-parity gates green at deletion |
| `LPS_RESCUE_MAX_ADVANCE_BOX` | 2026-06-26 | **BLESSED LIVE for engine-α 2026-07-06** — the "reaction-not-markup" gate on the rising-support-shelf rescue (net advance/box > 0.21 → reject). Enabled + committed `232afd0` ("Enable LPS calibration: rescue-markup gate 0.21 + hold tolerance 0.95"); validated 2026-06-26 (seed-recall 0 curated winners dropped; drops OHI/AEF/NVT/SPCB run-ups, keeps NMAI's gradual shelf). The council α-audit flagged a stale "HELD uncommitted, revert to None" settings comment with no ledger row; comment corrected + this row recorded so the frozen base does not carry an un-blessed live gate. `None` = disabled (safe default) | `settings.LPS_RESCUE_MAX_ADVANCE_BOX`; `232afd0`; [project_lps_peak_down] |
