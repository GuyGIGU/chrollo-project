# Flag ledger — dark engine flags and the decision each is waiting on

Every flag-gated engine capability that shipped **default-off** is listed here
with the decision that blocks its flip and a **kill-by date**. A dark flag is
not free: it is a dead branch in the reader, a row in the freeze manifest, and
a standing "we'll validate later" IOU. By its kill-by date a flag must be
**flipped live**, **deleted**, or **re-dated with a written reason** — never
silently carried.

Maintenance rules:
- A new default-off engine flag lands with a row here **in the same change**.
- Flips/deletions move the row to *Retired* (keep the evidence trail).
- Live flags (`TRAVERSAL_GATE_ENABLED`, `DESCENT_TAIL_GATE_ENABLED`,
  `SOS_TRIM_ENABLED`, `BOX_BACKEXT_ENABLED`, `TIGHTNESS_ADR_AWARE`,
  `HTF_CONTEXT_ENABLED`, ...) are deliberately NOT here — they are engine
  identity (frozen in `core/freeze/manifest.py`), not pending decisions.

## Dark flags (default-off, decision pending)

| Flag | Built | Blocking decision | Evidence | Kill-by |
|---|---|---|---|---|
| `AR_FIRST_REACTION_ENABLED` | 2026-07-04 (raw-bar); trend-derived 2026-07-05; **big-bounce retarget 2026-07-05** | Re-anchors the drawn Phase-A automatic reaction to **the low of the first *continuous* reaction after the terminal swing** (`market_structure.first_reaction_after`): retrace basis = the trend's **FULL leg** (elected segment start, `elected_trend_leg_base`), closed at the first **big confirmed bounce** off that low (no stall). Retargeted from the terminal-sub-leg+stall read after the operator's dated BC/AR marks on **PH/TOL/AVNT/AAP/AGCO/TFX** (2026-07-05) showed it over-tightened the clean reactions — the retarget is exact on TOL/AVNT and surgically fixes only the AAP-class second-leg overshoots. Tighten-only + overlay-only. Gates green: full pytest, **shadow `--check` flag-OFF byte-identical**, hermetic seed-recall; `CANONICAL_FIELDS` exclude the AR bars so a flip is byte-identical on scoring/tier/canonical outputs. **NOT fully byte-identical downstream:** flipping shifts the ARCHIVED `bin_a_*` measurement columns (`structure.ar_bar` → `measure_bins`, consumed by `analyze.py`), which no freeze gate covers — a live flip needs a `bin_a_*` guard / `engine_config_version` partition in `analyze.py` FIRST or it silently mixes two AR-mode populations into the fingerprint pool. Universe scan: **19/140 overlays re-anchor** (median 8, max 53 bars; AEBI −53 / TAC −47 / AES −38 = the "smear" cases; AAP −12) — more conservative than the raw-bar 37/140. **Council-reviewed for engine-α (2026-07-06): stays default-OFF in α** (the archive seam is a β-flip prerequisite); the retarget rides into α as measure-only substrate, flag off | `settings.AR_FIRST_REACTION_ENABLED`; [strategy_v2.md](strategy_v2.md) "The trend model" + "Phase A — First-reaction AR anchor" | 2026-08-15 |
| `TA_SCORE_V2` | 2026-07-01 (`d4c90bd`) | Build P0–P6.5 reweight-neutral behind the flag, then STOP at the A/B eyeball (weights untouched); flip is an operator A/B decision | [../specs/ta-score-rework.md](../specs/ta-score-rework.md), [../specs/ta-score-hybrid-design.md](../specs/ta-score-hybrid-design.md) | 2026-08-31 |
| `FUNDAMENTALS_ENABLED` (Lane C) | 2026-06-29 (`b8753a1`) | The scoring/enrichment wire-up wave (fundamentals feeding score/archive) — substrate is built and unit-tested, consumption wave not yet scheduled | `core/fundamentals/` docstrings | 2026-09-30 |
| `RS_LINE_ENABLED` (Lane C) | 2026-06-29 (`b8753a1`) | Same wire-up wave (RS-line new-high chip / score input) | `core/regime/rs_line.py` docstring | 2026-09-30 |
| `SECTOR_RANKING_ENABLED` (Lane C) | 2026-06-29 (`b8753a1`) | Same wire-up wave (sector-RS context for the health board / regime label) | `core/regime/sector_ranking.py` docstring | 2026-09-30 |
| `EVENT_MAP_ENABLED` | 2026-07-10 (Event Map Task 6) | Fire-path staging of the Event Map (stamped swing map + role labels for fires, measure-only, underscore diagnostics only). Flip is operator-gated on the scan-metrics cost A/B (EC-8); the downstream consumers (archive column family Task 7, overlay payload Task 13) land behind their own reviews | `core/structure/event_map.py` docstring; [../PLAN-event-tape.md](../PLAN-event-tape.md) Task 6; [../specs/event-map-causality-contract.md](../specs/event-map-causality-contract.md) | 2026-08-31 |
| `LPS_HOLDING_SHELF_ENABLED` (+ `LPS_SHELF_LENGTH_MIN`, `LPS_SHELF_MIN_LOW_POS_BOX`) | 2026-07-10 (Event Map Task 8) | The GEOMETRY-ONLY second LPS completion form (two-form doctrine, [lps_final_structure_canon_2026-07-10.md](lps_final_structure_canon_2026-07-10.md)): a monotone-non-rising, upper-half holding shelf accepted where the pullback form rejects at its depth / volume-dry-up judgments. Flag-ON converts the operator's marked WTS + PBT corpus shelves (DRTS stays red on the frozen frame — box election fails pre-LPS, a data-drift case that already fires as a fresh-data seed hit). Flip is operator-gated on the flag-ON shadow A/B + negative-corpus replay (Task 9's guards) and re-freezes the marks ratchet baseline | `core/structure/lps.py` `_holding_shelf_verdict`; [../PLAN-event-tape.md](../PLAN-event-tape.md) Task 8 | 2026-08-31 |
| `ELECTION_STABILITY_ENABLED` (+ `ELECTION_STABILITY_LOOKBACK`) | 2026-07-11 (Calibration at Scale Task 14) | Measure-only persistence probe on FIRING setups: re-run the eval-twin prep + the structure election alone at D−1..D−k (backward ONLY — no lookahead can enter the archive) and grade "same reading?" via the one cross-frame identity predicate (`core/pipeline/election_identity.same_election`: box-start DATE + rails within 0.10 box-height). Emits `_stability_same_frac`/`_streak`/`_probes`/`_refused` (underscore diagnostics; archive family is a later stage; `_refused` counts prep-refused shifts — universe-gate flicker — apart from election flicker, though both grade not-same in same_frac). Evidence: junk flickers (BODI's flag-ON band pair exists 04-15, dies 04-16) while real boxes persist. **Measured cost (the flip evidence, k=3, corpus fixture fires): mean ≈2.75s per firing ticker** (each probe = full twin prep + election; ~+4.5 min per 100-fire scan) — a flip likely wants k=1..2 or an election-only fast path measured first. HONEST SEMANTICS NOTE (corrected per Council Review 2026-07-11 finding 9): the probe measures backward persistence of the BOX election, not the fire — a box can elect well before its LPS completes, so same_frac at a first fire can be anywhere in 0..1 (corpus fires measured mostly 0 = the election churned into those fires; AVT 0.33 = pre-fire persistence exists). Interpret at calibration time, never as a same-day quality gate. Inert battery: flag-off probe NEVER consulted (raise-if-touched), deterministic raw values, ledger+manifest registered | `core/pipeline/stability.py`; [../PLAN-calibration-at-scale.md](../PLAN-calibration-at-scale.md) Task 14 | 2026-09-15 |
| `BAND_RAILS_ENABLED` (+ `BAND_MAX_BOX_WIDTH`, `BAND_EVENT_MIN_BARS`, `BAND_EVENT_MAX_DEPTH_ATR`, `BAND_EVENT_MAX_BARS`) | 2026-07-10 (Event Map Task 11; phase two = chronological pairs per the operator's rail ruling); **sequence-aware HOLD chain + depth/duration caps 2026-07-16 (PLAN-solve-the-engine task 9)** | Last-resort pair-election pool (consulted only when the strict AND rescued pools are both empty; outer Phase B only): the CHRONOLOGICAL zigzag pairs re-judged with band-leaving excursions typed as events (merged spring-then-test episodes; reclaim/fail-back + HOLD or the pair dies). Judged bars run the UNCHANGED gates; a pair carrying a qualified DEEP below-rail event may measure up to `BAND_MAX_BOX_WIDTH = 0.23` wick-to-wick (the operator measures wick to wick; the allowance exists only with the event). **A/B rail eyeball LANDED 2026-07-11, FAVORABLE** (the blocking decision): the operator reviewed the `tools/fidelity/band_rails_ab/` PNGs — all Phase C rulings correct, and the BODI flag-ON election (the pair we had logged as collapse-anchored junk) is **acceptable to his eye as a structure read**; his stated perception model (a bar respects a rail while its extreme stays in proximity — the structure "hangs") explains the January closes-below-drawn-S divergence. KLAC-elects-nothing is resolved as the spring-test-class LPS gap, a separate ruling. **Phase-C feed shipped 2026-07-11 (still dark):** the qualified DEEP event now types as `bin_c_type = TERMINAL_SHAKEOUT` at the one detector seam (`bin_features._phase_c_candidate` fallback; a calibrated `SPRING` is never re-typed; guards in `tests/test_band_rails.py`). **2026-07-16 (solve-the-engine task 9, still dark):** below-rail events now form a sequence-aware HOLD chain (successively deeper reclaim-and-hold springs = one progressive Phase-C step-down; the working floor steps to the most recent event's extreme; the final event answers the unconditional rule — a pure relaxation) and two caps bound eventhood: `BAND_EVENT_MAX_DEPTH_ATR = 5.0` (BODI chain 0.86→3.34 ATR passes; the EGBN 7.68–9.98 ATR breakdown class refuses) + `BAND_EVENT_MAX_BARS = 20` (EGBN's stale April framing rode a 40-bar "event"; BODI's real episodes are 12–18 bars) + an explicit non-finite/zero-ATR quarantine. Harness proof at the marks: BODI fires tier A at the operator's EXACT rails on 04-10 (previously off-tol 04-15); BOTH EGBN over-reach fires are dead. Flag-off byte-identity re-proven (shadow + hermetic recall green; manifest rotated by the two new keys only). **FLIP PROTOCOL (waits on the operator's explicit go):** flip `settings.BAND_RAILS_ENABLED = True` → full pytest battery → re-capture the shadow baseline → `python -m tools.marks_corpus --check` and re-freeze the ratchet baseline flag-ON (BODI's `band-vs-excursion` pinned miss converts — stage-matched re-freeze with the conversion evidence previewed) → fresh hermetic recall → verify BODI now elects at the operator rails with the TERMINAL_SHAKEOUT Phase C on the frozen frame → frontend: no change needed — the Phase-C region draws type-agnostic off `bin_c_present`, and `chartPhaseOverlay.phaseCDetail` already labels non-SPRING types "Support shakeout or test" (a dedicated TERMINAL_SHAKEOUT label is optional polish) | `core/structure/band_rails.py` `qualify_pair_events`; [../PLAN-event-tape.md](../PLAN-event-tape.md) Task 11 | 2026-08-31 |

| `LPS_OVERSHOOT_WINDOW_ATR_ENABLED` (+ `LPS_OVERSHOOT_WINDOW_ATR_MULT`) | 2026-07-16 (PLAN-solve-the-engine task 10) | For **OVERSHOOT_R windows only** (breakout throwbacks resting ABOVE R), the window-localization gate's denominator becomes `max(box_height, 2.0 × ATR)` — above a NARROW box, box height is the wrong yardstick (CTOS's marked shelf: 1.06 box-heights but only 1.42 ATR; k ≥ 1.67 admits it, 2.0 = margin). Gate-only: the archived `window_range_pct_box` measure is unchanged; `max()` can only grow the denominator so wide boxes and INSIDE/UNDERCUT_S zones are untouched (unit-pinned: the same oversized window INSIDE the box still rejects flag-on); non-finite ATR refuses the rescoped path. **Proof:** combined-variant probe (this flag + `LPS_HOLDING_SHELF_ENABLED`) fires CTOS 2026-07-15 tier S at rails within tolerance, span overlap 1.0 — the taxonomy's lever 4 conversion exactly. Flip is operator-gated with the shelf flag (the two levers convert CTOS together); flag-off byte-identity via the shadow battery | `engine_alpha/structure/lps.py` window gate; [../PLAN-solve-the-engine.md](../PLAN-solve-the-engine.md) task 10 | 2026-08-31 |

| `ELECTION_DETHRONE_ENABLED` (+ `ELECTION_DETHRONE_SESSIONS`) | 2026-07-16 (PLAN-solve-the-engine task 13) | **Stale-frame dethronement:** a rescue-propped (SOS-trim) framing whose buffered R the tape has left FULLY behind for the trailing 10 sessions loses the election **in favor of a later valid framing** (never into an emptier read). One trailing pass over the already-loaded window, pure function of the frame, deterministic; dethroned pairs narrate as `stage=dethroned` in the cascade trace. **Proof:** with the shelf flag, MATX fires 2026-06-29 tier S at rails within tolerance (span 0.96) — the evening before its breakout; MOV fires 07-02 at his exact rails. **Blast radius pinned hermetically:** flag-on over every sealed-corpus pinned hit at its first-fire session, zero elections change hands (`tests/test_election_surgery.py`). **The sibling rescued-pool arbitration lever was built and REJECTED** — it killed VIK's pinned corpus hit (an engaged-but-wrong early framing wins earliest-of-valid then fails to complete; elect-then-fail kills the root) and no clean currency rule separated it from the MOV framing it was meant to save — the shelf-R lesson at election scope; counterexample recorded at the seam. Do not re-attempt without new corpus evidence | `engine_alpha/structure/box_primitives.py` pool seam; [../PLAN-solve-the-engine.md](../PLAN-solve-the-engine.md) task 13 | 2026-08-31 |

## Retired

| Flag | Built | Outcome | Evidence |
|---|---|---|---|
| `PIP_PIVOTS_ENABLED` (+ `PIP_PIVOTS_DIST_MIN`) | 2026-06-25 (`c08e61f`) | **DELETED 2026-07-03** — eyeball-rejected as a wash (fixes some inverted climax→AR overlays, creates others: GBTG/PLSE/CGNX, `d43e7fd`); dead branches removed from `segment_swings` / `read_market_structure`, keys removed from the freeze manifest. `pip.py` itself stays (`macro_bridge_zigzag` refines the same substrate) | [pip_macro_phase_a.md](pip_macro_phase_a.md) §history; byte-parity gates green at deletion |
| `CANDLE_SPREAD_AWARE` | 2026-06-30 (`ed6b82e`) | **FLIPPED LIVE 2026-07-04** — operator A/B eyeball (`tools/candle_ab.py`): 61/128 choppy bases docked (mean −0.39, min −3.0), 0 tier flips, clean bases preserved at grade 1.0; shadow baseline re-captured | `settings.CANDLE_SPREAD_AWARE`; E1 Track B A/B |
| `PUZZLE_SCORE_ENABLED` | 2026-06-30 (`d2db80c`) | **FLIPPED LIVE 2026-07-04** — operator A/B eyeball (`tools/puzzle_ab.py`): 128/128 fires lifted (mean +4.5, max +8.0), 22 tier flips; shadow baseline re-captured. Forward-return validation still due ~2026-07-15+ (a revisit, not a blocker) | `settings.PUZZLE_SCORE_ENABLED`; [engine_pass/](engine_pass/) E2/E3 |
| `PIP_MACRO_PHASE_A_ENABLED` | 2026-07-02 (`25b98cb`) | **FLIPPED LIVE 2026-07-04** — operator eyeball on the overlay A/B (`tools/phase_a_pip_diff.py`): 19/140 overlays re-anchor to the genuine trend-top→reaction, 0 stolen climaxes in EITHER mode, 0 score/fire/tier (Phase-A overlay only). The phase-ordering hold was a REAL overlay defect (macro AR painted inside the box): the box-overrunning macro bridge now abstains (`142bf50`, `bridge_end_max=phase_b_start_bar`); the sibling always-on order-N hole was closed (`5882226`); `test_segment_swings_finds_root_bridge` pinned flag-off (`b514f49`) | [pip_macro_phase_a.md](pip_macro_phase_a.md); `test_invariants::test_phase_ordering_monotone` |
| `LPS_REQUIRE_PEAK_DOWN` (+ `LPS_PEAK_DOWN_TOL_BOX`) | 2026-06-24 (`dba8005`) | **DELETED 2026-07-06 (engine-α)** — the census found NO peak-down threshold that separates winners, and the OHI run-up class it targeted is killed by the live markup gate (`LPS_RESCUE_MAX_ADVANCE_BOX = 0.21`); the ledger row already recommended deletion. Removed the `find_lps` gate branch + both settings keys + manifest entries + the 3 flag-on tests; config-hash regenerated, shadow byte-identical (the flag was off, so no reading output moved) | council α-audit (2026-07-06) flag reconciliation; byte-parity gates green at deletion |
| `LPS_RESCUE_MAX_ADVANCE_BOX` | 2026-06-26 | **BLESSED LIVE for engine-α 2026-07-06** — the "reaction-not-markup" gate on the rising-support-shelf rescue (net advance/box > 0.21 → reject). Enabled + committed `232afd0` ("Enable LPS calibration: rescue-markup gate 0.21 + hold tolerance 0.95"); validated 2026-06-26 (seed-recall 0 curated winners dropped; drops OHI/AEF/NVT/SPCB run-ups, keeps NMAI's gradual shelf). The council α-audit flagged a stale "HELD uncommitted, revert to None" settings comment with no ledger row; comment corrected + this row recorded so the frozen base does not carry an un-blessed live gate. `None` = disabled (safe default) | `settings.LPS_RESCUE_MAX_ADVANCE_BOX`; `232afd0`; [project_lps_peak_down] |
