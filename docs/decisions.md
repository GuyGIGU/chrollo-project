# Decisions — operator rulings, and the levers that are DEAD

**Append-only.** Rows are never rewritten or deleted; a superseded ruling gets a new row
that says so. [`strategy_alpha.md`](strategy_alpha.md) is *what we are trying to see* and
[`engine_reference.md`](engine_reference.md) is *how it is currently seen* — both describe
the present. This file is the only one that remembers what we already tried.

## Why this file exists

Measured on 2026-07-27, during the trend-terminal box gate: four designs were proposed and
falsified in one session. Two of the four were **already documented** in `strategy_alpha.md`
("the root is only a scan origin, not the box's cause"; the back-extension behaviour) and
were still missed, because they sat inside a 1700-line file in a parenthetical. Prose that
is technically present but unfindable does not prevent the bug. A short, boring registry of
*dead things* does.

**Read the Tested-DEAD table before proposing any new knob, threshold, or heuristic.**

---

## Tested-DEAD — do NOT re-propose without new evidence

| Lever | Verdict | Why it died | Recorded |
|---|---|---|---|
| **A retracement ratio as the swing-commit rule for the line** (a turn commits when the counter-move reaches a fifth, a third or a half of the leg it counters, over a floor) | **DEAD, measured inert** | On his 35 marks every ratio form recalls exactly what its floor alone recalls (0.25 over 0.75 identical to 0.75; 0.33 over 1.0 identical to 1.0; 0.38 kills 12 of his 186 named turns): his legs run 1.5 to 5.3 daily ranges, so a third of a leg is already 0.5 to 1.8 ranges and the floor dominates. His hint ("it depends on the gross move over it") survives as the retrace share archived per swing, never as the commit rule. | 2026-09-13 |
| **A bar-level Phase D opening: three consecutive higher lows with highs in the resistance area** | **DEAD, opens Phase D inside Phase B** | Placed from his PBT words (spreads contract near resistance, support rises like a staircase). Measured on all 35 marks it opens the right side at 0.06 of BODI's box, 0.07 of SKYT's, 0.13 of NTCT's, before a third of the box on 11 marks, and lands PBT 16 trading days before his date. Phase B hugs resistance for three days routinely. The swing-level staircase (two consecutive line valleys, the second higher, both above the box midpoint, the peak between them at resistance) opens after the middle on 31 of 35 and is the form kept. | 2026-09-13 |
| **A K-day look-back for the LPS window's top** (the highest high of the last K trading days before the window) | **DEAD, the number is the rule** | K=5 reproduces his start on 32 of 37 windows, K=7 on 26 of 37; on VIK, PKE and NGL January the 7-day high sits five to six trading days before his window and the top he hangs the pullback from is a small later turn. The top comes from the line (the last turn before the window low) and from the receding-run rule (the day before the last run of receding days), never from a look-back count. | 2026-09-13 |
| **A close-dated hand-over** (the parent box freezes on the R15 breakout day, a close 0.10 ranges above the prior high, or on any close a full daily range above R) | **DEAD, breaks his one-box drawings** | A close a full daily range above R sits INSIDE 10 of the 35 boxes he kept as ONE before their final LPS (ST 7 closes, NTCT 6, MATX 5, VIK 4, NGL April 4), and a whole bar above R inside 15 of 35; on SYRE the Fri 13/02/2026 SOS is an R15 day, so the parent would freeze before his second LPS (Fri 13/02 to Tue 17/02) and the SYRE:2026-02-19 fire is lost. His words: *"its tricky to define it with Close value we need to deduce it Swings as well"*. The hand-over is a swing whose valley holds the parent's R area, confirmed by a later turn at the child's anchors; the close is used once, for the breakout day. | 2026-09-13 |
| **A post-window "fatal character change" as a hard rule** (a close 5 percent of price under the LPS low; a later low a full range under it; the spread test after the window) | **DEAD, toothless or a numeric-gap gate** | 5 percent of price is 0.50 daily ranges on BODI and 2.34 on UNF (median 1.34), one rule four times stricter on one stock than another; the fatal placed at his corpus maximum (drop 1.53 and spread 1.71 ranges) was identical on every count and at his p80 refused MRK and NGL April and zero junk; a 1.0-range step fatal has a 0.07-range margin to YPF's 0.93. Under R18 plus the trigger law no post-window day exists: every later day either crosses the last day's high (the buy) or recedes into the window. Depth from the top (the 2.5-range yardstick), the largest step and the length are graded. | 2026-09-13 |
| **The smallest-box form of "tightest wrap wins"** (the smallest box among all qualifying framings) and **the most-turns selector / the turns-per-rail exam as a GATE** | **DEAD, both measured against his marks** | Smallest box elects sub-ranges (SYRE 0.4 ranges, NTCT 0.6), loses 6 pinned fires (BWA, MSGS, NGL April, SILC, ST, WTS), matches his box on 4 of 30. Most-turns as a ranker loses 8 pinned fires (turns accumulate over longer windows); two turns per rail as a GATE regressed ANRO's and VIK's pinned fires. Earliest-respected stays the span law (R4 final, verbatim *"1. yes"*), tightest only a same-day tie-break, turns an archived graded fact. | 2026-09-08, 2026-09-10 |
| 2026-09-08 | **The engine tests against his CURRENT drawings, always. The sealed-snapshot standard is retired.** Operator, verbatim: *"lets make sure that from now on, the engine always Tests against the updated marks as a rule, to check for the latest thing. I thought it was a little bit obvious, no need to hold on to an old drawing if the Operator deleted it or changed it..?"* **He is right, and the state it had produced is the argument:** the sealed Guided List still demanded **three marks he had DELETED** (CTOS 2026-07-23, ORMP 2026-05-08, PKE 2026-02-24) — and two of those were recorded as permanent EXPECTED MISSES, so the engine was being excused forever for failing charts that were no longer ground truth. Meanwhile **five he had drawn since were tested by nothing** (ANRO, MDT, MRK, PKE 2026-04-07, UNF) and **ten of the thirty survivors had been EDITED** — rails, LPS windows, triggers — with the fingerprint able to say only that *something* moved. **The concern I put to him before building, recorded because it is the reason the seal existed:** a standard that silently follows its own ground truth cannot fail, and his own regression signal (EC-29) is *"calibrated-list stocks no longer firing"*. The design answers it by separating the two jobs the seal was doing. **POPULATION is his and follows the DB.** **VERDICTS stay pinned, per drawing** — `marks_corpus.setup_digest` over rails / LPS spans / trigger / knowable-from / frame. An UNCHANGED drawing keeps its verdict, so a pinned hit that stops firing still FAILS and an expected miss that starts firing still FAILS. A REDRAWN or newly DRAWN mark voids its old verdict — it was about a chart that no longer exists — is re-measured, and if the engine cannot read it enters `unreviewed-new-mark`: pinned against silent conversion, reported separately as AWAITING HIS REVIEW, never blocking the rebuild. Drawing a chart must not turn his gate red before he has looked at it. A DELETED mark leaves and its excuse leaves with it. **`--check` now FAILS on a stale corpus instead of printing an advisory** (it had sat drifted for six weeks); an absent marks DB is NOT drift, since CI grades the committed fixture. **Result of the first refresh: the standard went 28/33 → 30/35.** Four of his five new drawings the engine already reads (ANRO, MRK, PKE 04-07, UNF); MDT it does not, and ST 2026-04-17 — whose LPS he moved from March to April — no longer fires under its current window. Those two are the whole review queue. **EC-9 amended and EC-7 narrowed** to keep the half that mattered: he may redraw, an agent may never edit a mark to pass a test, and since a redrawn mark's verdict is VOIDED rather than carried, editing one cannot launder a regression into a pass. Seven new guards, each mutation-proven. | `conventions.md` EC-9 (amended) + EC-7 (narrowed); `tools/guided_list_export.py` (both refusals removed); `tools/marks_corpus.py` `setup_digest` / `population_is_current` / `NEW_MARK_STAGE`; `tests/test_marks_corpus.py`; baseline `tests/baselines/marks_corpus_baseline.json` rebuilt at 30/35 |
| 2026-09-08 | **The first-reaction AR anchor is DELETED — its shape was right and its anchor was not.** Operator, verbatim: *"Lets DELETE the first two."* Ruled a week before the 2026-09-15 kill-by that this flag's ledger row had already declared non-re-datable. `AR_FIRST_REACTION_ENABLED` was dark from 2026-07-04 to deletion, so **nothing the engine reads changed**. **The READING it implemented survives in `strategy_alpha.md`**: the automatic reaction IS the low of the first continuous reaction after the terminal swing — that is still what the AR means, and the retarget's SHAPE was vindicated (spans 1-7 bars against his own 1-5; on HTH its AR landed on his trend end to the bar, exactly as he said: *"the green circle is correct but not for the AR"*). What was deleted is an implementation that could not be right while `climax_bar` is not the trend end. Measured twice against his own dated marks and it lost both times: 2026-08-14 on the cohort chosen to FLATTER it (the twelve largest tighteners in the universe) flag-OFF was closer on 4 of 6, |off| 133 bars vs |on| 266; re-measured 2026-08-31 against the repaired climax polarity it moved FURTHER from flag-off, not closer — touching fewer charts (101 of 335) and disagreeing by more on those (tighten median 19 -> 35 bars) — and on his own marks it is a no-op on 3 of 4 live reads and loses the fourth badly. **The mechanism is the reason deleting costs nothing**: flag-OFF pins `ar_bar` to `box.start_bar`, and his AR IS the box open by his own repeated definition (*"AR at 17/06 which also serves as the root swing for the consolidation"* — IRMD, IART, CYRX), so flag-off satisfies his identity by construction while the retarget broke it to chase a reaction off a climax that is reconstructed backward over a FIXED 60-day window. **That defect stays open as a program** (climax_anchor_diagnosis_2026-08-19) and is the actual work. **Stated cost of this ruling, not hidden:** he was offered the row's own alternative — keep it dark purely as the anchor program's measuring instrument — and chose delete, so the flip half of `tools.operator_marks_diff` retired with it and the program lost a toggle. Its ANCHOR half survives and is now its whole purpose. | `bricks._first_impulse_ar_end` + `market_structure.first_reaction_after` + the flag and its four `AR_*` constants + five `engine_alpha/freeze/manifest.py` rows + `tools/ar_first_reaction_diff.py` all removed; `elected_trend_leg_base` and `analyze.PHASE_A_ANCHOR_FEATURES` deliberately kept; [flag_ledger.md](flag_ledger.md) Retired table; [engine_reference.md](engine_reference.md) "Phase A — First-reaction AR anchor — RETIRED"; [strategy_alpha.md](strategy_alpha.md) "The trend model"; measurement [ar_remeasure_2026-08-31.md](ar_remeasure_2026-08-31.md) + [anchor_marks_ruling_2026-08-14.md](anchor_marks_ruling_2026-08-14.md) |
| 2026-09-08 | **The trend-terminal box gate is DELETED — the doctrine stands, the code does not.** Operator, verbatim: *"Lets DELETE the first two."* Ruled a week before the 2026-09-15 kill-by, which the flag's own ledger row had already declared its **second and last** re-date. `TREND_TERMINAL_BOX_GATE_ENABLED` was dark from the day it was built (2026-07-27) to the day it was removed, so **nothing the engine reads changed** — the deletion is byte-identical by construction, and the only runtime consequence is the manifest rotation. **The RULING it implemented is untouched**: a box may not open before the trend running into it printed its climax (2026-07-27, LIVN, *"way too young to be considered anything but some false breach of the range"*), and the 2026-08-14 corroboration stand append-only above — they rule on the CHART, not on this code. What could not be carried was the implementation's evidence: across three A/Bs (39 → 21 → 17 losses) **the loss cohort turned over completely every time — the intersection of any two of the three loss sets is EMPTY** — so a maturity rule's evidence can never bank and every re-ruling would need a fresh sitting on names nobody has seen; the 2026-07-28 IRMD/IART/CYRX KEEP ruling can no longer be re-tested at all (two are out of universe, the third now reads identical). The defect fingerprint survived the polarity re-key on two disjoint cohorts: class-B losses carry their claimed climax ABOVE the box's own R, 9 of 9 at median +0.45 box heights then 5 of 5 at +0.558 — `segment_trends` box-blindness, not an early box. Recorded honestly against the deletion: 7 of the final 12 class-A losses sit with their climax essentially ON the box ceiling, so the gate was probably right about most of what it removed — which argues the doctrine, not this code. **`market_structure.trend_terminal_floor` SURVIVES untouched and unconditionally live**, feeding Phase A's climax polarity re-key (`bricks._cause_is_up`, 2026-08-19) — the expensive half is kept and its Layer-3b guards MIGRATED to `tests/test_trend_terminal_floor.py` rather than retiring with the flag. Re-implementing the gate later is a fresh A/B against a repaired `segment_trends`, not a revert. | `config/settings.py` key + `engine_alpha/freeze/manifest.py` row + `bricks.validate_equilibrium`'s `terminal_floor` parameter + `box_primitives.trend_terminal_legal_open` + `tools/trend_terminal_ab.py` + `tests/test_trend_terminal_gate.py` all removed; [flag_ledger.md](flag_ledger.md) Retired table; [engine_reference.md](engine_reference.md) "Phase B — trend-terminal box gate — RETIRED"; measurement [trend_terminal_killby_2026-08-31.md](trend_terminal_killby_2026-08-31.md) |
| **Overshoot-magnitude test for box legitimacy** (refuse a box whose trend ran N box-heights past R) | **DEAD — falsified 3×** | Anti-correlated with the operator's judgment. He ACCEPTS 47.9% (PXS), 82% (VIK), 101% (MATX) — those are upthrusts *inside* an established base; on PXS he deliberately draws R at 4.66 beneath the 4.92 spike — and REJECTS LIVN at 20.57%. A `TREND_TERMINAL_OVERSHOOT_BOX` knob was built, measured, and removed the same day. The live discriminator is **post-climax maturity** (LIVN 13 bars vs PXS 53, floor `MIN_BASE_DAYS`). | 2026-07-27 |
| **Keying the box's cause trend on `root.kind`** | **DEAD** | The root is a *scan origin*, not the box's cause. LIVN's winning root is an `SC` 420 bars away from the advance that actually swallowed its box, so a kind-keyed gate passed the very defect it was written for — silently. Derive the cause from the segment covering the bar. | 2026-07-27 |
| **Direction-blind trend-segment overlap (later segment wins)** | **DEAD** | Segments share a leg — an uptrend runs to its CHoCH, which IS the next downtrend's start. Letting the later one win lets a base's own automatic reaction (BC→AR) veto the box open at the top where it belongs. Broke 6 pinned Guided-List hits (AVT/CTOS/MATX/NGL/SYRE/VIK). Cause segment (first-write) wins. | 2026-07-27 |
| **Gating a box on its raw `cand_start`** | **DEAD** | Must judge the **back-extended** start, the bar that actually becomes `box.start_bar`. The trend floor is not monotonic: a handover bar carries the OLD (printed) terminal while the next carries the new leg's, so a legal box can sit one bar the far side of an illegal anchor (MATX). | 2026-07-27 |
| **Shelf-R overshoot re-anchoring** | **DEAD — REVERTED** | No clean rule matched the eye; the respect gate kills shelf-R framings on 16 above-rail bars, and that band is also the upthrust defense. Never loosen the respect gate to chase it. | pre-2026-07 |
| **The ATR V-arm check in the Phase-C spring detector** | **DEAD** | Superseded by the bounded-excursion model (penetration → reclaim → hold). Do not re-add. | pre-2026-07 |
| **Two-pass root walk to fix last-resort scope** | **DEAD** | Measured: it reverses CMPR's operator-accepted band election, creating a new violation while fixing another. The per-ROOT `if not pool` guard is pre-existing shared design, not a story/band regression. | 2026-07-27 |
| **Scoring `rs` / `uptrend`** | **DEMOTED to 0** | Edge read 2026-07-22 over 1977 episodes: actively harmful. Merged `fd7fe3b`; S-tier share 51%→29%. | 2026-07-25 |
| **Scoring HTF (weekly/monthly) context** | **DEAD as a score input** | Edge verdict: don't score it. Stays measure-only. | 2026-07-04 |
| **Bar-dwell gate form (EGBN class)** | **DEAD** | EGBN is a confirmed basis artifact, but the gate form breaks 10/26 identities and lets DGII/FLG fire. Merged as measurement only (`d8fd6f6`). | 2026-07 |
| **Multi-ticker `yf.download` batching to cut request count** | **DEAD — refuted at the provider** | Reads like free money (5,488 single-ticker calls → ~110 batched) and the batching code path still exists, so it invites re-proposal. It buys nothing: `yfinance/multi.py` loops **per ticker in both thread modes**, and `base.py` puts the symbol in the URL path — one HTTP request per symbol regardless of batch size. The 2026-07 docstring blaming batching for rate-limit storms is imprecise: what died was `threads=True` (uncontrolled inner threads), not batching. Per-ticker also buys real isolation — one bad symbol cannot taint a batch. | 2026-07-27 |
| **"The cold fetch is slow because it pulls 5 years"** | **DEAD — measured false** | Cold-fetch stages measured 510 s / 499 s against incremental 585–740 s; the *incremental* window is not faster. Request **count**, not payload size, sets the wall clock, and the pool is ~80% idle waiting on rate-limit tokens. Shortening `DOWNLOAD_PERIOD` would cost HTF history and save nothing. | 2026-07-27 |
| **Species-scoped occupancy/traversal floor relaxation** (drop `EQ_MIN_HALF_DWELL`/`EQ_MIN_COVERAGE`, `EQ_MIN_TOUCHES_PER_RAIL`→1, `EQ_MIN_TOUCH_THIRDS`→1, `TRAVERSAL_MIN`→0, `TRAVERSAL_MIN_DENSITY`→0 inside the species read) | **DEAD — measured, operator-approved experiment falsified same day** | The ABVX/MAN autopsies made the floors LOOK like the species wall ("its virtue is its crime"); the full-cache measurement says no: with every floor OFF at clock 8, only **36 of 1,591** refusals flip to episode elections (1,525 hold — the walls behind bind), the flipped cohort **loses money** (fwd_20 median −5.1%, 31% winners, n=35, vs +2.8%/54% for the floors-on elections), **ABVX — the one refusal the operator wanted admitted — STILL does not elect** (its wall is elsewhere: elected-older-box-with-no-LPS + the dragged AR), and the one S4 card that DOES newly elect (ANL) is one he junked. The floors were doing paying work all along. Real levers for the species remain the BREAKOUT WALL (drift-up misfile, S1 6/10 keeps + MAN's recaptured `refused_clock` fact) and the first-reaction AR. Sidecar: `output/power_play_census_relaxed.json` (overrides stamped), vs `output/power_play_census_species.json` clock-8 rows. | 2026-08-18 |
| **A log price scale as the replacement for the vertical proportion trim** (frontend chart framing) | **DEAD — measured worse** | Reads like the obviously right axis for a base sitting on top of an advance: percentages, not dollars, so the rest should get a fairer share of the pane. Measured over the live 230 on identical windows: box share of the visible price range **linear med 0.227 / p10 0.104 vs log med 0.203 / p10 0.085 — log is better on only 18 of 230 charts**. Structural, not incidental: these bases sit at the TOP of the advance, which is exactly where a log axis compresses them against the full visible range. Vertical proportion is bought with a TALLER WELL, never with the axis and never by deleting trading days. Reproduce: `node tools/chart_framing_census.mjs`. | 2026-09-02 |
| **"Just match his TradingView density"** (pin the card to a fixed px-per-trading-day) | **DEAD — measured worse than the shipped model** | The most intuitive proposal in this space. His TradingView reads at ~7.0 px per trading day, so the card should too. In the card's real 687px plot that is a **98-day window: base owns a median 37.8% of width, 88/230 over 45%, 66 over 60%** (5.0 px/day -> 137 days, 27.0%, 65, 38) against the shipped 140-day reference's **26.4%, 17, 3**. He reads at 7 px/day BECAUSE his pane is 1389px wide; copying the density into a half-width pane copies the wrong half. Density is a legibility budget, the SPAN is the invariant. | 2026-09-02 |

### Nulls — measured, discriminated nothing

Do not re-run these as defect detectors; they describe the corpus norm, not a defect.

| Measurement | Result |
|---|---|
| Box high above R | 274/332 boxes (82%). Rails are *worked-equilibrium* levels, never climax→AR extremes, so this is design. |
| A later high exceeds the Phase-A climax high | Median +7.5% across the corpus; LIVN sits at the **39th percentile**. |
| Position of the box high within the box | Corpus median 75% depth; LIVN at 43% = 23rd percentile (i.e. *better* than typical). |
| Operator's drawn R vs the window high | His own 33 marks sit a median **+7.7% BELOW** the window high, vs the engine's +3.1% — the opposite of the expected direction. |
| atr_squeeze ADR-rebase (“the open twin”) | Not applicable — measured 2026-08-22: `ATR_10/ATR_50` is already self-relative (the stock's own volatility against its own horizon), so there is no absolute unit left to rebase; corr(score_atr_squeeze, adr_pct) = −0.254 vs the −0.73 that condemned absolute tightness. Recorded so the standing “rebase for consistency” note stops resurfacing. Side fact for the next edge read (not a knob request): 67% of fires sit at ratio > 1 — the term is inert on two-thirds of the fleet. |

---

## Rulings

| Date | Ruling | Where it lives |
|---|---|---|
| 2026-09-13 | **Build step 1 of the final method LANDED: every guard grades the fired-policy window, the junk population follows his rulings, and the one-day fixtures' blind spot is measured and pinned for his eye. Nothing in the engine moved.** The junk corpus (`tools.negative_corpus`) and the fleet fixture (`tools.shadow_diff`) read ONE day per frame; under R18 the fire lands on the last receding day and the buy day reads "no actionable candidate", so a fire that moves a day earlier looked lost and a junk chart firing on an earlier day was invisible. Both now walk the marks ratchet's own window (`tools.replay.fired_window_walk`: the last 10 faithful trading days ending at the frozen day, every clamp named, every fire day kept). **What the window found, under the engine of this day:** the fleet has 34 firers, not 32 (HESM fires Tue 26/05 and Wed 27/05/2026 and never on the frozen day; BBVA fires Thu 04/06/2026; MAN, JRSH and PKOH fire on no window day; TKO fires on the frozen day only); and **9 of the 17 junk charts fire on an earlier window day while staying clean on the frozen day**: CHCT Thu 18/06/2026 (109.1, A), BMRN Mon 22/06 (90.8, B), FRPH Thu 18/06 and Mon 22/06 (B), OHI Tue 30/06 and Wed 01/07 (A), AEF Thu 04/06 (C), SPCB Wed 24/06 to Mon 29/06, four days (A), ABEV Wed 03/06 to Wed 10/06, five days (C to B), ENIC Fri 26/06 (109.2, A), BBVA@2026-06-05 Thu 04/06 (88.1, B); clean on every day: DGII, KWR, DBD, RLGT, FLG, BBVA, COLM, GOOD. **Decision (engineering; his eye owed):** those fire days are PINNED as known in `tests/baselines/negative_corpus_baseline.json` (`--pin-known-fires`, a declared seam) so the gate reds on any NEW fire day and prints the pinned ones as KNOWN on every run; a pinned day that he rules a real junk fire is an engine defect for the later steps to remove, one he would take leaves the junk list. The dark flag-ON replays (holding shelf, story pool, miss lanes) stay on the frozen day so the suite pays the window once. **Population changes at this seam:** NVT left the junk corpus (his Thu 10/09/2026 word, "correct but would grade low"; 17 cases remain, COLM stays); the shadow baseline recaptured (EC-29 seam); the reader pin recaptured (population only, zero word drift on 35 marks, 17 junk, 37 fleet); the marks corpus RE-SEALED to his current drawings through `tools.guided_list_export` (fingerprint 88db0fcf, 35 marks): his new SYRE mark (Thu 19/02/2026 card) FIRES under today's engine on Wed 11/02/2026 (tier B), the 02-12 card left with it, and the two marks that entered unreviewed on Tue 08/09/2026 now carry the stage the measurements name, MDT `universe-gate` (the 200-day leg refuses it on every read day, 3.77 ranges under) and ST `lps-ceiling` (its LPS low sits 1.30 ranges above his resistance against today's 0.5-range ceiling; R12's 1.35 converts it at build step 3). Ratchet after: 30 of 35 pinned hits fire, 5 reviewed misses (EGBN, NOK rail-placement; SKYT, MDT universe-gate; ST lps-ceiling), corpus matches the live DB. **Guards proven by mutation** (defect applied, red seen, restored byte-identical by SHA-256, green seen): the walk cut to the final day; the diff ignoring the window fields; the frozen-day view without its filter; an EVAL_ERROR day read as a rejection; the report without its windows; the first fire's fields taken for the last; the pre-change meta; the pins ignored; the frozen-day switch ignored. One process finding recorded for the method: the mutation-prover agent died on an expired login token with its first mutation still applied in the tree; caught by the marker grep before any guard ran, and the rule is now to grep for markers after any killed prover. | `tools/replay.py` `fired_window_walk`; `tools/negative_corpus.py` (`check_corpus`, `pin_known_fires`, `load_known_fires`); `tools/shadow_diff.py` (`WINDOW_FIELDS`, `frozen_day_view`); `tools/marks_corpus.py` `STAGE_TAGS`; `tests/baselines/negative_corpus_baseline.json` (new), `negative_corpus_meta.json` + parquet (NVT out), `shadow_baseline.json`, `reader_pin_baseline.json`, `marks_corpus.parquet` + baseline (re-sealed), `docs/marks/guided_list_2026-07.json` (the export); tests in `tests/test_negative_corpus.py`, `tests/test_regression_guards.py`, `tests/test_marks_corpus.py`; [final_method_2026-09.md](final_method_2026-09.md) build order step 1 |
| 2026-09-13 | **THE FINAL METHOD IS RULED, AND THE BUILD STARTS: one line, two rails, an event map, a grade, a display. Operator: "Go!"** His framing (Sat 12/09/2026), verbatim: *"the goal for this session is to finally set on the final and ultimate Engine algorithm Method. and I think the answer relies within our Phase Computing stages by simplifying the chart into Lines (like the one I draw on screenshots) for both consolidation boundaries and the inner price action itself -> then we build an Event map for each stock -> Grade it -> Display the final result. One Simple, Smart, Adaptable method to reach as many high quality setups as possible."* His go-ahead (Sun 13/09/2026), verbatim: *"Go! I hope I provided enough context and valid enough answers for you to program a solution that catches my intent, I really want the engine to be as simple and easy to understand as possible since essentially we just draw 2 lines around a tight area of a chop in a stock and display it to me so I can see it and figure out if I want to trade it or not. I get it when we get into specifics it become tricky but I hope we can develop a smart/adaptable way to catch as many good setups as possible"*. **The whole-engine inventory that preceded it** (eight readers, 16,419 lines, 337 decision points: 101 thresholds, 85 gates, 63 flags, 45 elections, 28 rescues, 15 vetoes) found one structural inversion everywhere: the box is elected by gates BEFORE any event is read, the LPS is a brick of that election, the event map runs after the fire as diagnostics, and the grade puts 163 of its 171 points on box statistics. **The method as ruled, one pass per stock per read day:** (1) one threshold line on the whole chart, 0.75 daily ranges wick to wick, the first and last bar turns by their shape, named events turns by law; runs of higher highs and higher lows are trends, the climax is the run's highest turn, the automatic reaction the first valley after it; (2) the rails: candidate pairs walked forward from the climax (the climax and its reaction first, else one swing right), the root is the first pair whose two rails each receive a later turn within the rail area, tightest only breaks a same-day tie, the box opens on the earlier anchor and never extends left, its unit is the election day's ATR frozen with the rails, 15 trading days is the age floor; (3) the events, forward in time against the rails: turns at a rail, one Phase C per box by the swing-recovery rule, every thrust over resistance typed by the swing after it under ONE precedence table (LPS above R, upthrust, SOS, child candidate, open), shakeouts, mini consolidations with their own rails, Phase D opening at the first right-side evidence, the LPS window as the last run of receding days with the day before it as its top, the trigger the last day's high, the buy its cross; (4) the grade: tightness (height in ranges, spread profile), the event map, context, all graded, a named event's swing never docked; (5) the display: one state word per chart. **The yardstick for every step is concordance, never rail replication**, and his Sun 13/09/2026 words make the LPS the judge of a pair: *"some Earlier & Wider Variations can be acceptable as long as they follow and understand most of my Idea's within that mark, especially the LPS"*. **The build order, one flag each, measured alone against his 35 marks and the 18 junk charts:** 1 the guards (the junk and fleet fixtures grade the fired-policy window like the marks ratchet; the junk population re-frozen with NVT out on his Thu 10/09/2026 word), nothing in the engine moving; 2 the ruled stack R7 to R18 behind flags plus one window per read day; 3 LPS refusals to grades; 4 the one line feeding the event map and the trend labels only; 5 the words on the line; 6 the 15-day floor from the first anchor; 7 box gates to grades (width caps, crash floors and the spring cap, then respect share and the run cap); 8 the LPS leaves the box election, the state table on the wire; 9 the box opens on the earlier anchor with the back-extension deleted; 10 the climax painted back from the elected pair and root candidates as one ordered list; 11 the hand-over by swings, then the stale-box rescue retires; 12 the grade re-partition with the Power Play lane deletion. The theory moves into `strategy_alpha.md` with each step that makes it true in the engine; the rulings and every measurement live here and in the ledger now. | [final_method_2026-09.md](final_method_2026-09.md) (the answer sheet: every point, the build order); [final_method_answers_2026-09-13.md](final_method_answers_2026-09-13.md) (his 26 + 12 answers verbatim); [final_method_questions_2026-09-12.md](final_method_questions_2026-09-12.md); [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) (R1 to R18 with every MEASURED block) |
| 2026-09-13 | **THE LINE: one zigzag on the whole chart at one sensitivity, 0.75 daily ranges wick to wick, edge bars as turns, no retracement ratio.** Asked for the smallest counter-move that counts as a swing on his line, he asked for a search, verbatim: *"could you help me with that one? can you conduct a search, even within archived setups to look for for an answer for the possible smallest counter moves, its hard to tell, it depends on the gross move over it can be one big bar or a collection of them."* On the first and last bar as turns: *"Yes even with out "Future" Information we can deduct them as turns by their current state swing shape"*. **Measured (his 35 marks, 94 drawn events, the archive's 11,618 fires):** his 37 LPS pullbacks run 0.88 to 2.33 daily ranges (median 1.27), retrace 0.26 to 1.14 of the leg they counter (median 0.45), 16 of 37 are one bar's move; his smallest named counter-move anywhere is VIK's LPS at 0.88. A threshold line scored on his 186 named turns (both anchors, every LPS peak and low, every spring and test tip, every SOS peak): floor 0.5 with the first and last bar allowed 183 of 186 (7.2 extra turns per 10 trading days); **0.75: 180 of 186 (6.0)**, landing on every LPS peak and low, every SOS peak, every spring tip, every R anchor, the six misses being S anchors that share the climax's bar; 1.0: 174 (loses RGR's and VIK's LPS); 1.5: 107; **today's skeleton (order 1, order 2 at 40 trading days, the 15-percent-of-box collapse): 112 of 186**, because an order-2 pivot needs two lower bars on each side and 25 of his 37 pullbacks bottom within two days, and the edge mask hides the first and last bars. A retracement ratio (a fifth, a third, a half of the leg, over a 0.5 or 1.0 floor) recalls exactly what its floor alone recalls: his legs run 1.5 to 5.3 ranges, so the floor always dominates (Tested-DEAD row). On the 18 junk charts turn density is the same as his (8.5 vs 8.1 per 10 trading days) but the rail share differs (58 percent of his turns within 0.25 ranges of a rail; 24 to 29 percent on the tall junk boxes): a graded trait, not a floor. The archive's own LPS counter-move has median 1.13 ranges, his six liked fires 0.55 to 0.94. **Ruled into the build:** one line, one floor of 0.75, the first and last bar as turns, an outside bar may carry both turns, named events (the LPS valley, the trigger cross) are turns by law, the retrace share archived per swing; the line is NOT the root swing (his anchor pair is a consecutive line swing on 21 of 35) and NOT the LPS window (a sub-floor bounce sits inside 25 of his 37 windows). Once a box is elected its rail area, hand-over test and ceiling use the election day's ATR frozen with the rails. | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) fifteenth sitting (swing_sweep); [final_method_2026-09.md](final_method_2026-09.md) §1, §2 |
| 2026-09-13 | **THE RAILS AND THE ROOT: the climax is the trend's end, the root swing is the first pair that wraps the respected range, first in time wins, the box opens on the anchors, respect and height and depth are graded, fifteen trading days is the one floor.** The climax, verbatim: *"that extreme is the Buying/Selling climax and the counter move after is what's known as the automatic rally. Since the price graph has only 2 states either Trending or Consolidating this question is easier to answer even if there isn't a clear cut answer for an extreme running end, I just look for the transition between the Trend and the first sensible structure that resembles a consolidation"*. The root swing, verbatim: *"as noted before the Root swing can be the BC/SC -> AR, but in plenty of bases and consolidations it ISN'T and the ROOT SWING is the first Set of limbs(Swings) that their anchors answer to the true trading range of said consolidation -> so not the first swing after said climax but we look for the first place in the timeline (after the climax obviously) where drawing the anchors for Resistance and Support lines Wrap around the Consolidation trading range that respects it. - ngl is a nice example"*; and the walk: *"Option A: Trend -> BC/AR = Root Swing -> Consolidation Box. Option B: Trend -> BC/AR -> RootSwing -> Consolidation Box."* The box opens: *"No, Box opens on the anchors of each of the Boundary rail (Resistance & Support)."* Respect by context: *"a whole lone bar beyond the rail area isn't respecting it but if we come to learn that price action before that bar and after that bar DO then it changes the way we treat it, a Long run of bars beyond the rail could mean a long Spring or UP thrust as well, if the price continues to Rise/Fall with out recovering we can deduce that either that the consolidating structure we measured ended and the price began to trend ... Context is key"*. Height: *"a box is a box because of its consolidating Zig zag behavior not it's height but generally Since we prioritize tight Boxes we generally prefer to avoid HUGE boxes"*. Depth: *"It's hard to gate using a raw number in case we reject a valid setups because of a small neumeric gap"*. Length: *"Lets go with 15 days"*, then *"15 for a base minimum yes."* On the earlier, wider pair the forward walk elects on 9 to 11 of his marks: *"nothing really disqualifies them (not until I've seen it ofc) ... for the engine's sake it's probably better to keep it that way so we won't have the problem of tuning the meaning of the word "correct" or the "tightest" being some really far pair that cuts the consolidation length in half"*. **Measured:** his anchor pair is not a consecutive swing on the 0.75 line on 14 of 35 (nine carry a 0.95 to 1.6-range bounce between the anchors, five anchor S on the climax bar with no counter-move); the rule that reproduces eight of the nine: the S anchor is the lowest low of the reaction before the first higher low. Every cheap "answered to" test elects an earlier, wider pair on 9 to 11 marks (ALB, AVT, BWA, JAZZ, NTCT, WTS, YPF, RGR, SILC, both NGL); he accepts that at the cost of the LPS judging it. The eleven boxes the engine opens before his climax today are older roots electing other anchor pairs (MRK 89 trading days early, UNF 53, ALB 34), not the back-extension (0 days on ten of them). At his rails the whole-bar respect share is 0.79 to 1.00 and every run beyond a rail that resolves inside his span re-engages within 1 to 7 trading days; the long runs are all events he drew. His heights run 0.98 to 3.13 ranges, the junk fires 1.3 to 4.9; the percent-of-price caps refuse BODI (2.1 ranges, 21 percent of price) and demote ANRO; the archive's tail falls almost monotonically with height. BODI's drawn Phase C (3.3 ranges under S, 17 trading days) is refused three ways today (the 3-range cap, the crash floor, the width cap). The run into his climax: min 3.2, median 7.4 ranges over 35 trading days. **Ruled into the build:** candidate pairs walked forward from the climax, first in time wins, tightest only a same-day tie-break; the box opens on the earlier anchor, each rail from its own anchor, never extended left, no drawn end; respect refuses nothing, every run beyond a rail typed by the swing after its tip; no height gate, height the largest graded term; no depth number, a dip beyond the support area is a Phase C candidate until the swing rule names it a spring or a breakdown; 15 trading days counted from the first anchor as the one floor. | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) fifteenth + sixteenth sittings; [final_method_2026-09.md](final_method_2026-09.md) §3 to §9 and "Sixteenth sitting" 1, 2 |
| 2026-09-13 | **THE EVENT MAP: upthrust and shakeout join the vocabulary, the SOS never gates and is typed by what follows, Phase D opens at the first right-side evidence, the mini consolidation has its own rails, recovery is judged by the swing, the last supper is just another event, one Phase C per box.** Verbatim: *"we can add Up Thrust, Shake Out as I use them on a daily basis."* The SOS: *"an SOS will never gate a setup firing, It's a sign of strength and that definition is easy to decipher, it dosen't have to be exactly from the support line and it dosen't have to break through Resistance it's just that the easiest and cleanest examples do, but Since I don't want you highlighting every piece of running buyers it most be in Phase D (The Right Most Area in the consolidation after the 'V' tip (either by after Phase C) or Price Contracting behavior which SOS is one of)) and it's Commonly spotted after some sort of Shakeout Like move and it reaches or breaches local Significant Highs like the Resistance."* Phase D: *"Phase D can open at the SOS if it's the first sign of Price action supporting the fact that we are in the Right side of the 'v'. at the worst Phase D starts at the LPS (as a fail safe) but most cases that isn't going to happen, generally phase D needs to start after the middle of the structure"*; and on his two examples: *"On Syre I would say Phase D opens roughly after the 26/01/26 because its after the spring and after the price recovers and its a bunch of few bars before the SOS and The LPS. and on PBT I would say it starts on 23/04/26 because price contracts (bar Spread is lower which means the Price Moves less) Closer to Resistance (Meaning gradual Support rise like a stair case) before a Thrust and a final pull back"*. The mini consolidation: *"The Mini consolidation is always built with it's own rail set - the LPS read can be against it if it is inside/near it/resting on it and the trigger is always read against the LPS by itself."* Recovery: *"Recovery better be determined by the Swing rather then a singular bar ... we need to extend recovery to bars that are also near the support area as well like Semi support breaching bars (Poking through) and ones that touch support from below as well, but since we don't want to invite Junk I think its better to be kept by the swings for added context."* The last supper: *"It's just another event that happened, lets not dwell on it too much"*. Phase C: *"A shake out is also a dip I would probably name, but we can stick with Phase C only for now, and a Dip inside the support area will probably not ever qualify since that describes more of the basic Chop common pattern we are used to see."* The pullback above resistance: *"this can be all 3 depending on what comes next. in the case of ANRO its an LPS above Resistance that fires. in other cases if that "Rest" above R evolves to be a mini consolidation then it can be considered for a candidate for that mini consolidation's root swing ... our engine can detect pull back "LPS" like behavior across the entire base and earlier points for an LPS can be valid and be pointed out ... we returned to the original intent of highlighting a single LPS per stock, the freshest and most relevant one"*. **Measured:** on his 35 boxes the line prints 136 up-swings over the resistance area (24 he named SOS, 12 inside an LPS, 5 inside a last supper, the rest unnamed); no rule read at the peak separates them, only the swing after; five separate card rules named 61 of his own valleys three ways, hence ONE precedence table: the down-swing recedes with the trigger overhead and its low holds R minus 0.5, an LPS above R, fires; the next valley takes out the thrust's launch low by more than 0.5 ranges before any LPS, an upthrust; otherwise an SOS with "last supper" as hindsight data; "child candidate" a flag confirmed only by a later turn at the child's anchors; at the right edge, open. The engine today names his SOS on 2 of 21 (its typer demands a held wave inside a hold band) and opens Phase D at the deepest valley, median 0.39 of his boxes, where his first right-side event sits at median 0.79. **The Phase D rule placed from his two dates and measured on all 35:** opens at the earliest of the round trip after the spring (the recovery swing reaches the resistance area, then the first valley that holds in the support area: SYRE Mon 26/01/2026 exactly), a staircase of rising swing lows in the upper half with the peak between them at resistance, the first SOS, the first LPS; median position 0.74, after the middle on 31 of 35, never before a third of the box; PBT lands Tue 28/04/2026 against his Fri 23/04 (his staircase there is bar-level inside one swing, and the bar-level form opens Phase D inside Phase B on 11 marks: Tested-DEAD row). His ten Phase Cs recover by the swing in 2 to 9 trading days (median 4); his five minis are 12 to 42 trading days and 0.70 to 1.79 ranges tall, the LPS low inside the band on 4 of 5; his ten last suppers dig 1.75 to 3.76 ranges in 1 to 4 days off tops 0.6 to 2.4 ranges over R. **Ruled into the build:** the two words; the SOS as an up-swing launching at or after the box's deepest low, covering at least his minimum in ranges (1.70), top at the resistance area or above the last swing high, provisional at the edge, THE SOS being the thrust whose pullback is the LPS; Phase D by the four openings, "after the middle" graded; the mini as a second rail set written as one event on the parent's map, the LPS elected against the parent and displayed against the mini when inside or within 0.5 ranges of it, the trigger always the LPS's high, a band whose bottom holds the R area being a child instead; recovery = the up-swing from the tip reaches the support area and the next down-swing makes a higher low, failed on a lower low before any thrust; one Phase C per box, the deepest recovered, a dip inside the area a turn at S; the last supper named in hindsight only, never weighted; every LPS-like pullback an event, one highlighted. | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) fifteenth + sixteenth sittings; [final_method_2026-09.md](final_method_2026-09.md) §10 to §15 and "Sixteenth sitting" 3, 5, 12 |
| 2026-09-13 | **THE LPS AND THE TRIGGER: one window per read day, the last run of receding days with the day before it as its top; a cross inside the pullback is a buy; the LPS is ON support when its bodies sit above it; no post-window fatal; volume is data.** Verbatim, on SYRE's two windows ending the same day: *"10/02 to 11/02"*; on the start: *"Lets go with the day after"*; on the support side: *"Lets say not below the support area (the area below the support that can be considered respected)"*, then on JAZZ: *"I would say the LPS is on the Support rather then Under it since most of the move is above it and only small parts of it poke out down"*; on the fatal character change: *"idk"*; on volume: *"lets go with data only?"* and *"Can't say nailing down volume to points is tricky, we generally want to see Low Volume at key points Like Phase C and LPS, but a high Volume at Phase C can also mean that spring was a successful choke point of sellers ... I don't want to gate setups by placing Hard Volume Gates that may reject setups just because "the volume" isn't low enough or some other execuse."* On MRK, where the Wed 05/08/2026 high crossed inside his drawn window: *"Bought on 05/08/26 because that's the day I saw the stock on, the prior move on 03/08 - 04/08 seemed like a valid pullback to me to place my trade on"*. **Measured (his 37 windows):** every day after the first recedes on 32 of 37; the rule "the window is the last run of receding days ending today, the day before it is the top" reproduces his start on 30 of 37, and the seven others carry a bounce day inside his drawing (MRK's bounce is his buy day; MS, MSGS, ORMP, SILC a one-day bounce; JAZZ and FOSL a longer run he trimmed), so the engine fires on the first run and the bounce day is the cross, exactly his MRK answer. His drawings shade the top day on 30 of 37 while his answer starts the day after; with the top as the anchor nothing measured moves (same low, same trigger, same dig), so his shading stands as drawn and the engine's window opens the day after. Today's code elects the LONGER of two windows sharing an end (the opposite of his answer) and refuses one-day windows (five of his are one receding day). JAZZ's window: every close above S, every high 0.5 to 0.8 ranges above S, only the lows poke under (to 0.74): "on support" is typed by the window's bars, never its lowest wick. Under R18 plus the trigger law no post-window day exists to test (every later day either crosses the last day's high or recedes into the window), so the three post-window checks (a close 5 percent of price under the LPS low, which is 0.5 ranges on BODI and 2.3 on UNF; the low test; the spread test) have nothing to do; 11 of his 37 windows sit at or over today's volume ask of 0.87 and BWA's only wall was volume. **Ruled into the build:** one window per read day, no election; the top defined on the line as the last turn before the window low; one-day windows legal; a cross inside the window is a fire kept with the story position "crossed inside the pullback", the window continuing and firing again on its last receding day; the low's position graded with no support-side refusal; the zone typed by the window's bars; the three post-window checks deleted, depth from the top and length graded (steep past 5 receding days, his max); volume never refuses, never elects, carries no points, is shown as a number. | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) fifteenth + sixteenth sittings; [final_method_2026-09.md](final_method_2026-09.md) §16 to §20 and "Sixteenth sitting" 4, 7, 8, 10 |
| 2026-09-13 | **GRADE, FIRE, DISPLAY: the grade is tightness plus the event map, a fire is box plus LPS plus trigger, every chart carries one state word, the door stays, the Power Play is the same map on a young box, the hand-over is read from swings.** Verbatim: *"grade is built from a few important traits: Consolidation tightness (Consolidation width + Spread Profile), Event Map, base age dosen't matter to me, a found spring isn't overshooting anything it's part of the event map? did not get the question"*; fires and the no-LPS display: *"Yes"*; the buy day: *"we can go with nothing"*; refused charts: *"lets go with not scanned"* and *"keep the 200 day filter, idk what yearly leg refers to"*; Power Play: *"the same map on a young box"*; the hand-over: *"the child box inherit the parent as its cause + its tricky to define it with Close value we need to deduce it Swings as well"*. **The spring question he did not follow, answered for him:** today a found spring excuses the whole box from the dead-space dock (up to 8 of 171 points) and counts as a story piece; that blanket pardon is how junk NVT rose from B to A when the spring bounds were lifted, and 1,468 of 6,775 archived fires owe grade points to it. His words made exact: a named event's swing is never docked, an unnamed lunge beyond a rail is. **Measured:** base length does not separate the tail (rank correlation 0.04 with a 25 percent move in 20 days); the buy is the day whose high crosses the trigger on 37 of 37 but the close ends back under it on 12 of 37, and the engine's still-live test reads the close (default: read the high, so "nothing" holds on every buy day); the door's "yearly leg" is the 12-month return floor (a stock down more than 20 percent over the last year is not scanned), refusing the same population as the 200-day leg (MDT on his marks; MAN's June box on Wed 03/06/2026 in the field), so both stay together; the premise put to him that R18 stopped MAN was FALSE: the fleet's Fri 05/06/2026 MAN fire was a 140-day box refused on its last receding day by the 200-day leg, and his named Power Play MAN is the Jul/Aug 2026 shelf (19 trading days) that never fired in the paying read; HESM's lost window was five days of rising highs and lows, a rally, not an LPS; a close a full daily range over R sits inside 10 of the 35 boxes he kept as ONE before their final LPS, so a close-dated hand-over would freeze SYRE's parent on Fri 13/02/2026 before his second LPS and lose the fire (Tested-DEAD row); the swing rule (after the breakout, the first swing whose valley holds the parent's R area is the child's root candidate, confirmed by a later turn at a child anchor) finds NGL's root Mon 09/02 to Wed 11/02/2026 to the day and MRK's Tue 30/06 to Wed 01/07/2026 with its valley 0.13 ranges under the old R. **Ruled into the build:** the grade reads height in ranges, the spread profile inside the box and the window, and the event map; base age zero weight, archived; turns per rail replace touch bars; the named-leg rule replaces the blanket pardon; one grade ledger with a fate for every one of today's 171 points is written before the flip. A fire is box plus LPS plus trigger; the LPS leaves the box election; one state word per chart (fired, crossed, lines with no LPS yet, forming N of 15, root candidate unconfirmed, beyond R undetermined, under S undetermined, broke down, not scanned, no lines), the no-LPS charts in a watch lane. Nothing on the board on the buy day; the ticker page may print "fired dd/mm, crossed dd/mm" from the archive. The door stays outside the four stages with its refusal named on the ticker page. One walk, one map, one clock of 15 trading days; the species lane and its 8-day clock retire; the pole is a trend fact on the map. The parent stays operative until the child is confirmed by a turn, then freezes back to its breakout day and the child inherits it as its cause; the close is used once, for the breakout day (R15). | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) fifteenth + sixteenth sittings; [final_method_2026-09.md](final_method_2026-09.md) §21 to §26 and "Sixteenth sitting" 6, 9, 11 |
| 2026-09-12 | **R18: THE LPS WINDOW ENDS ON THE LAST RECEDING DAY BEFORE THE BUY.** Operator, verbatim: *"The LPS window ends on the last receding day before the buy"*. **Measured on his 37 windows:** the window's last day against the day before it, by bars: a lower high on 32 of 37, a lower low on 32 of 37, both on 27, EITHER on 37 of 37, neither on 0; the day after the window has its high above the last day's high on 37 of 37 (min 0.10, median 0.54 ranges) and its close above it on 25 of 37. So "receding" = a day that makes a lower high OR a lower low than the day before; the window ends on the last such day; the buy is the first day whose high crosses that day's high; the trigger is that high (his trigger law, 35 of 35 to the cent). As a gate on the ruled stack every count was identical to the stack before it while nine window ends moved, every moved end becoming his (SYRE Wed 11/02/2026 trigger 34.13, EWTX Mon 30/03/2026 trigger 31.15); honest cost on the fleet fixture 36 to 34 and on the junk 9 to 8, with the method finding that ONE-DAY fixtures undercount under R18 (the fire lands on the last receding day and the buy day reads "no actionable candidate"), which is why build step 1 makes the junk and fleet fixtures grade the fired-policy window like the marks ratchet. | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) twelfth sitting (lastday_recede, rulings_ab7, rulings_ab8) |
| 2026-09-12 | **R16 and R17: the phases the engine scans are the ones he draws, SYRE's December box is a valid box too, and Phase C is a noticeable dip under support that recovers shortly into the consolidation.** His SYRE drawings, verbatim: *"That's the phases that engine should scan. SYRE as an example"* (BC at the top on R, AR at the reaction low on S, the stripe labelled "ALSO ROOT SWING", the zigzag of swings turning at S and R, "LPS" on the last down-swing, "Buy" at the breakout); then on the earlier climax: *"You are correct to point out that the Climax of the prior trend ended on 11/12/2025 - and acceptable valid box could also look like this!"*; then R17: *"1. the LPS you drew is good. 2. Correct that it is a Dip -> but we can classify it as a Phase C since: 1. A. Its a pretty noticeable dip under the Consolidation's support. B. it recovers shortly into the Consolidation (and it also has a test midway)"*; then: *"Lets follow your recommendation, that's the point of splitting the Consolidation through Events - we recognize them, highlight them we BUILD the Setup - notice how after the Spring the Price Recovers into the Consolidation within our boundaries and then Completes an LPS, just like how strategy doctrine explains it should"*; and on his updated SYRE mark: *"It's important for us to take High quality setups so we can study their Moves, understand how to read and interpit them and widley apply it across all stocks we find, that's how we detect setups. ... Lets walk through all of the Events I marked tell me if you understand them and if you understand the point of me Walking you through this (it's have unlock an omni vision from above)"*. **Measured:** SYRE's December box on the data: BC Thu 11/12/2025 high 35.31 = R, AR Tue 16/12/2025 low 32.26 = S (the climax and its reaction ARE the root swing), his Phase C Mon 05/01/2026 low 28.00 = 2.22 ranges / 1.65 box heights under S, back inside within 5 trading days, a test midway Thu 08/01; at his rails 29 of his 35 boxes carry a noticeable dip under S, 17 deeper than the 0.65-box-height cap, 22 in the first half of the span, so both spring bounds (the depth cap in box heights and the late-half rule) refused his own drawings and were lifted; with them lifted the detector finds a spring on 17 of his 35 boxes (where he marked it 4 of 7 read; 10 where he drew none, for his eye) and on 8 of 18 junk, and the junk grades lifted only through the blanket spring pardon (NVT B to A), the door the 2026-09-13 grade ruling closes. His updated SYRE (mark id 76): Root 11/12 to 16/12, Phase C 02/01 to 05/01, LPS 09/02 to 11/02, SOS 11/02 to 13/02 (the thrust through the rail, 2.6 times the volume), LPS 13/02 to 17/02 with the old R holding as support, trigger 37.22 = the last day's high, buy Wed 18/02/2026. | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) seventh to eleventh sittings (syre_dec, his_springs, rulings_ab6, syre19_walk) |
| 2026-09-10 | **R10 to R16, the second to seventh sittings: the root swing anchors the rails, a box hands over after its breakout, COLM is junk, the LPS stays close to the box (ceiling 1.35 ranges), the bar is the unit of dwell, the date convention, the close is for the breakout day only.** R10, verbatim: *"Yes, I always described the Root Swing as the Swing that Anchors the Rails of the consolidation you can even search that in the damn chats, and the elected Phase A should consists of Two parts yeah? the Climax and the automatic rally"*, later *"Correct that's why its weird it didn't go by that name because I authored it long ago, it works like that already."* R11: *"Regarding EWTX I can see why a box was elected there, which could may have counted as a setup, but after that 'setup' you marked breached out another consolidation was formed right after, And I need to know our engine can recognize that"*, and as a story step: *"Trend -> Climax -> Consolidation (+ whatever traits that consolidation holds) -> Break out -> (That's the second Box that I drew) Trend -> Climax -> Consolidation"*. R3 amended: *"Looking today at COLM I say its complete junk -> we need to strive further from these kinds of setups they are the opposite of what were looking for ... Extreme Width, Spread, Messy non tight structure, almost no quality traits of a setup (the story) events that we are looking for wrongly anchored from the bottom First when the entire price move is still rising absolute garabge"* (COLM stays on the must-not-fire list; the Sat 05/09/2026 "good" was given on the engine's render). R12: *"I don't know what is the measurable limit but preferably pretty close Since we prefer the LPS be close to the consolidation process (the energy gathering stage)"*, then *"Lets go with that ceiling 1.35 I guess...."* (the LPS zone's ceiling = 1.35 daily ranges above R, from his above-R LPS lows WTS 0.12, VLO 0.27, NGL 0.29, VIK 0.33, UNF 0.45, ST 1.30), and *"The 50-day filter stays on for now."* R13: *"I don't mark Resistance and Support by Close Values nor use it to understand Price action moves (I do it by understanding the Swings rather then individual bars) the only place where Close values have any sort of significance to me is when I'm already in a position measuring the advance of the price. other than that the bars themselves (The entire bar from High to Low) tells me everything I need to know"*, delivered *"We can go with your recommendation for Whole bars as I ruled"*: the occupancy exam reads whole bars and is graded (the four close-based legs refused 8 of his 35 boxes; on whole bars 0 of 35). R14: *"The time scale is 24-hours, and the date format is 'day in week' day/month/year e.g monday 27/09/1999 and the time zone we are to follow is UTC - 4 New York."* (New York time, America/New_York, not a fixed offset). R15: *"Yes Close Value is the right tool to measure that, but the Pull back itself is to be measured by High and Low values, like always."* (the breakout day is read on the close; every measure of the pullback reads highs and lows; the placed close floor re-placed on the last bar's high, identical on every count). NGL, his drawing: *"Red marked stripes are Root Swings, LS is Last supper"* (two boxes, the second's BC and AR overshooting both rails and the root swing coming after them: Phase A and the root swing are two things). **Measured:** today's painted Phase A equals the rail-anchoring swing on 0 of 30 (polarity wrong on 16 of 30); the hand-over in ranges at 1.5 gave PBT its exact box and EWTX its Jan box at no fleet cost; the ceiling 1.35 costs DGII (junk) and gains ST and NOK. | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) second to seventh sittings of 2026-09-10 (rulings_ab2 to ab5, his_dwell_bars, pullback_bar_floor, syre_facts) |
| 2026-09-08 | **R4 final, R6 to R9b: earliest-respected stays the span law with tightest a same-day tie-break; a turn at the rail is his definition; the name is LPS; the LPS is measured in daily ranges; the LPS is defined by graded traits and broken only by a character change.** R4 final, verbatim: *"1. yes"* (the span stays the earliest consolidation after the trend end that is respected; tightest only between framings starting the same day). R6: *"well uhhhhhh It's a little bit complicated? im not sure what the conflict is? since all boxes have definitely more then 1 turn, that dosen't even make sense?"*, with his numbered screenshots NOK 3 at R / 2 at S (the spring below S counts at S), WTS 4 / 4 (the upthrust above R counts at R), YPF 3 / 2: a swing top that reached the resistance area and turned back is a turn at resistance however far it poked, a swing bottom that reached the support area and turned back is a turn at support, springs included; over his boxes the minimum is 2 turns per rail. R7: *"by rest do you mean The LPS? it has a name you know and we better start getting used to it"* (every served phrase says LPS; "rest" is banned). R8: *"okay so that's the way to go then"* after the units research (his 36 LPS windows in daily ranges: min 0.80, median 1.23, max 2.43, a 3.0x spread; in box heights 5.0x; in percent of price 4.6x): every box-height clause in the LPS read moves to daily ranges; the junk stays separated by the BOX height in ranges as a graded trait (R2). R9: *"an LPS is broken usually when we detect a rising seller strength rather then decline and its seems like the correction is getting to deep, it's also part of where it fits in the story most of the time. 'the last day need not be the lowest' that's good, 'And an LPS is broken only when a later trading day closes under its low;' that may be problematic because we can excuse more days that close below as long the general structure of the move is still the same, that's what extends a 2 day LPS to 3 day LPS or 4 day LPS ... I generally prefer to define it by the traits I want to see and deem high quality like Receding Price move over time, Lowering (and low) spread below average, 2 - 5 bars. stuff like that. do you get the gist?"* R9b: *"I say it's tricky because I do no want 'Each day's move is smaller than the last;' to be the gate that reject plenty of Setups that are good. because it dosen't STRICTLY follow that rule"*. **Measured:** the rest-on-its-low law refused 6 of his windows and goes; the close-under-the-low hold as a hard rule is refused; only 28 percent of his own windows recede strictly day over day (13 start flat and dip on the last day), so receding, lowering spread and low spread are grades; the biggest single-day step inside his windows is at most 0.93 ranges (median 0.33); the yardstick in ranges blessed his exact windows 11 of 36 to 19 of 36 and recovered SILC's pinned fire at zero junk exposure; the turns exam as a GATE regressed ANRO and VIK and the most-turns selector lost 8 pinned fires (both closed). | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) R4 final, R6 to R9b (three_rulings_ab, yardstick_ab, lps_units, lps_traits, lps_receding, traits_ab) |
| 2026-09-05 | **R1 to R5, the first sittings of the step-by-step walk: respect is read in whole bars against the rail area, box height is graded never an exclusion, tightest wrap wins (later narrowed), and the chart is either trending or consolidating.** R1, verbatim: *"if a bar's values is within that Resistance/Support area Meaning either low or high of that bar is in that area then it counts (see the screenshot) that's why its also important to dissect the individual legs and swings and understand the patterns that the price is doing"*, and *"'A bar is outside the box only when the whole bar sits beyond the area.' correct, and after we dissect individual bars we structure and split them into Swings and build the narrative from there, because a Spring/Up Thrust's are bound to have bars that exists beyond the Resistance/Support tight area, and that is Acceptable as longs as we can detected the rest of the story. I want you to build the scanning process through visual phases that we can understand"*. R2: *"I don't know what 'Daily range mean' but my answer is that the box where the chop actually happened if most chops are at around 2-3 'Daily ranges' then we know the common ground for most consolidations that we want to find, but I don't want to exclude a perfectly 'good' consolidation just because it's taller then 3 'daily ranges'"*. R3 (Sat 05/09/2026): *"both SILC and COLM charts you sent are good"*, given on the engine's render and AMENDED Thu 10/09/2026 on the real chart (COLM is junk). R4: *"Yes lets go with the Tightest wrap Wins, since that rule will probably lend us to more high quality setups overall that can fit in a tightwrap rather then messy chops that we rather avoid"* (narrowed Tue 08/09/2026 to a same-day tie-break after the smallest-box form lost 6 pinned fires). R5: *"where do we look for consolidations at the first place? the graph is always in either 2 stages either Trending or Consolidating and usually when a trend ends it reaches some sort of climax -> counter move -> consolidation: and not always they will come one after the other so were looking for the first instance that a consolidation is respected!"* **Measured:** a bar engages a rail when its high or low lands inside the 0.5-range area and is outside only when the whole bar sits beyond it; under that rule his boxes pass respect 28 of 30 (7 of 30 under today's read) and as the live gate alone 5 of 18 junk fire (SPCB, DBD, FLG, ENIC, BBVA); his boxes run median 1.9 daily ranges, the tallest 3.1, the junk boxes 2.1 to 4.7, a cap at 3.25 refused by R2; his rails are interior levels the swings TURN on, not the span's extremes (R a median 1.2 ranges below the span's highest high). | [rulings_ledger_2026-09.md](rulings_ledger_2026-09.md) R1 to R5 (his_rule_ab, his_rule_height_ab, which_root, first_respected, rails_vs_extremes) |
| 2026-09-07 | **The scan-failure report is cut to TWO states, named for the ACTION, not the cause.** Operator, on reading the feature description: *"I don't need that much of a detailed report, I just need to know 2 states either the Scan failed because of a technical issue (meaning, I need to run the scan manually and everything works), or there is a real issue that needs to be tended by you!"*. He is a discretionary trader glancing at a dashboard, and the only decision the surface has to support is **do I click re-scan, or do I ping Claude**. So the headline is now **Re-run the scan** (`rerunnable`) or **Needs attention** (`needs_attention`), and the taxonomy, the reason and the proposed solution are **kept — demoted to the second line**, not deleted; he said he did not need the detail UP FRONT, not that it should not exist. **Re-runnable** covers every interruption kind (shutdown / service-only / unrecorded / not-yet-known) and an aborted run: the machinery is fine, the run just did not reach the end. **Needs attention** covers a deliberate engine refusal (`stale_data` — running the same scan again meets the same refusal), a program error / non-zero exit, and **anything unrecognised, which fails CLOSED**: two live rows carry a string whose writer no longer exists, and we cannot promise a re-run fixes what we cannot explain. **The escalation is the part that earns its keep: the same failure on three consecutive runs of one job (`REPEAT_ESCALATION = 3`, server-side) stops being re-runnable**, because someone re-running the same broken thing nightly is the exact trap this feature exists to prevent — one night is noise, two can be two nights of his habitual power-off, three is a pattern. The streak is keyed on (job, status, resolved failure kind): a different failure breaks it, another job's run in between does not, and it can only ever HARDEN a verdict. **Consequence worth recording:** the health pill and `/scan-status/latest` now read a short WINDOW of recent runs rather than one row — with one row the pill would say "re-run it" about the very run the registry three inches below had already escalated. **EC-28 throughout:** the verdict is resolved in `scan_diagnosis` (one mapping table, a coverage test that fails when a stampable kind has no row), `/health` serializes it as `verdict` alongside a per-check verdict on `failing`, and the frontend holds only the two labels and their tones in `wireVocabulary.js` — a grep guard fails if either slug appears in a rendering module, which is what a JS-side derivation would look like. Every guard mutation-proved (22 mutations, each watched red, each restored). | `services/scan_diagnosis.py` (`RERUNNABLE`/`NEEDS_ATTENTION`, `KIND_VERDICTS`/`STATUS_VERDICTS`/`DEFAULT_VERDICT`, `REPEAT_ESCALATION`, `_escalate_repeat`, `overall_verdict`); `services/health.py` (`verdict` on the report and on each failing check); `/scan-status/latest`; `components/wireVocabulary.js` `RUN_VERDICT_LABELS`/`RUN_VERDICT_TONES`; `utils/appFormat.js` `buildHealthPill`; `components/ScanHistoryModal.jsx`; **evidence: [scan_interruption_incident_2026-09.md](scan_interruption_incident_2026-09.md)** "The operator cut the report to TWO states"; `tests/test_scan_diagnosis.py`, `tests/test_backend_services.py`, `utils/appFormat.test.js`, `components/wireVocabulary.test.js` |
| 2026-09-07 | **Review of the scan-diagnostics change: the shutdown claim was anchored on the wrong moment, and the health pill described three healthy states as a failure. SUPERSEDES the window stated in the row below.** Three independent reviewers returned ship_after_fixes. (a) **The window `[started_at, min(detected_at, started_at+2h)+90s]` swallows this operator's habit.** A reviewer read the real System log: all eight recent `User32` 1074s (2026-08-30 .. 09-06, 21:59Z-23:58Z) fall inside the 18:00 scan's own two-hour cap, so an orphan detected late — the dashboard survives, `finished_at` is stamped at next morning's boot — was stamped *"the computer was shut down while the scan was still running"* whatever actually killed it, with a remedy that would not have helped. The claim is now anchored on **`detected_at`**, the one instant we can PROVE the run was still alive (the reconcile finding the row still 'running'): a 1074 must sit within the 90 s detection grace of it, keeping the existing cap. The real incident still classifies (the 1074 landed 4.95 s before the reconcile); the late-detection case drops to `interrupted_unrecorded`. (b) **The `last_scan` health check said "The most recent scan did not finish cleanly" in three states where the scan finished perfectly** — never-run, hung-running, and ok-but-stale. The third IS the morning after this incident: the machine was off, so no scan ran, and the newest row is yesterday's clean scan aged past its allowance — the interface described a run that wrote 12 setups as not finishing cleanly. Resolved per state from one server-side map. (c) **`interrupted_power_loss` DROPPED, not fixed.** Windows stamps EventLog 6008 and Kernel-Power 41 at the NEXT boot, so their timestamp is the reboot, not the crash, and an overnight power cut lands hours outside any run-anchored window: the arm was near-dead and its absence is covered honestly by `interrupted_unrecorded`, now softened to *"Windows has no record of the computer going down while it was running"*. (d) **The `wevtutil` non-zero-exit refusal had NO test** — a reviewer deleted it and all 1821 tests stayed green, because `parse_events` returns `[]` for empty stdout AND for plain error text, so that one branch is the whole difference between "could not read the log" and "read it, found nothing". Now pinned and mutation-proved. (e) EC-3 folds: `HUNG_RUNNING_HOURS` had three homes (diagnosis / watchdog / a bare `2` in health) and is now one imported constant; "which checks count as an operator-facing failure" was written twice in `health.py` and is now one predicate. (f) EC-28: `/health` now serializes `failing` — which checks are wrong, in order, in plain words — and the frontend's `failingChecks` is a pass-through instead of a second membership rule in JS. (g) The remedy prose no longer hardcodes "18:00": the hour is a setting he is being asked to move, and only `missed_slot_notice` (which reads it live) may name it. (h) Non-scan rows get their own noun and their own re-run instruction, so an outcome-backfill failure no longer reads "the scan" with a "press Evaluate" remedy. | `services/scan_diagnosis.py` (`classify_interrupted`, `JOB_NAMES`/`JOB_RERUN`, `_LAST_SCAN_STATES`); `services/health.py` (`_operator_relevant_failures`, `failing` on the wire); `services/scan_watchdog.py`; `utils/appFormat.js`; `components/wireVocabulary.js`; **evidence: [scan_interruption_incident_2026-09.md](scan_interruption_incident_2026-09.md)**; `tests/test_scan_diagnosis.py`, `tests/test_backend_services.py` |
| 2026-09-07 | **A scan killed by the nightly shutdown must SAY SO, and the "Degraded" pill must click through to a diagnostics registry.** Operator: *"Well if the Run was ended because the computer was offline, just make it say that, or If I click on the 'Degraded' area it just takes me to a log registry with the proposed solution and reason of failure"*. Both halves shipped. **The load-bearing finding is that the obvious implementation is FALSIFIED, and it is recorded here so it is not re-proposed.** The intuitive rule — at boot-reconcile time compare the orphaned run's `started_at` against the machine's last boot — returns **False for the exact incident**: NSSM restarted uvicorn *inside* the dying Windows session, so the reconcile ran at 01:00:30.464 local, five seconds after Shut Down was pressed and one second AFTER the Event Log service had stopped; the machine's last boot at that instant was the previous MORNING. It also lies in the other direction (a scan that crashes alone at 01:00 on a machine that stays up, rebooted normally at 09:00, satisfies `boot > started_at`). **What shipped instead is a two-moment design.** The boot reconcile RECORDS A FACT — plain-words error text plus `failure_kind='interrupted_unknown'` — and claims no cause, because at that instant the cause is provably unreadable. `scan_diagnosis.resolve_pending`, run off the event loop at lifespan startup when the Event Log service is actually up, reads the Windows System log for the run's OWN bounded window and stamps a TERMINAL kind ONCE. **The kind is stamped, the prose is derived at the publisher** — so wording is a code change with no migration, and (the reason for the column) a **circular 20 MB System log that rolls cannot rewrite a historical answer**. The offline claim is affirmatively qualified: a `User32` 1074 inside `[started_at, min(detected_at, started_at+2h)+90s]`. A readable-but-empty log is a DIFFERENT value from an unreadable one, and "the computer stayed on" is asserted only when the boot clock also shows no restart since the run began — the clock may only REFUSE, never grant. **Both topbar pills** now open one registry (the failure text has always lived on the second one, so wiring only "Degraded" would have been the wrong half); it leads with the failing health checks, carries a synthetic line for the night the scan never STARTED (2026-09-06: powered off 30 s before the slot, no row at all, and `_missed_todays_slot` structurally cannot catch it up), and lists all three job kinds — surfacing 11 non-ok maturation/download rows no surface had ever shown. **Cosmetic bug fixed in the same change:** the topbar rendered an interrupted run's NULL setup count as "0 setups" (`Number(null)` is zero and zero is finite) and the operator read it as "the scan ran and found nothing" — the file had re-declared the house null guard instead of importing `finiteOrNull`, exactly the drift the null-safety rule exists to stop. | `services/scan_diagnosis.py` (the one classifier); `scan_runs.failure_kind` + its backfill; `startup._reconcile_orphaned_runs`; `scan_status.never_run`; `/scan-status/history?kind=all`; `components/ScanHistoryModal.jsx` + `hooks/useScanHistory.js`; `utils/appFormat.js`; **evidence: [scan_interruption_incident_2026-09.md](scan_interruption_incident_2026-09.md)**; `tests/test_scan_diagnosis.py` |
| 2026-09-02 | **The screener card's "considered" check-off becomes a LIKE — a preference signal, collected measure-first.** Operator: *"Make the 'Concidered' button on the Mini chart a 'Like' Button, basically signaling that I like that setup, so we could perhaps single out 'good' setups that I like and perhaps we can study them to priortize them out of the bunch that gets in with the results after scanning"*. **Ruled a good idea, and it does NOT collide with the drawn-marks rule** (2026-08-26: ground truth is what he DRAWS, never a mined candidate). That rule governs placing a THRESHOLD from charts that may not be setups at all; a screener card is the ENGINE'S OWN FIRE, and a like on it answers a RANKING question — where a chart belongs among the ones that already fired — which his drawn marks structurally cannot answer, because he only draws setups he already likes. **The load-bearing design point: the like is only informative against a NEGATIVE class.** Likes alone cannot separate "saw it and shrugged" from "never looked at it", so the archive's existing 'saw & passed' mark is deliberately KEPT (it stays on the Archive table) — liked / passed / never-engaged is the three-way label a study needs. No migration was required: `setup_reviews.verdict` was already a string column, so 'liked' is a second value in a now-closed two-value set, mutually exclusive by the existing UNIQUE(ticker, scan_date) — changing his mind REPLACES the verdict rather than stacking, or the same chart would be double-counted in both corpora. One `_toggle_verdict` helper serves both routes so they cannot drift. **Two conditions recorded with the ruling:** (a) **nothing in the engine may read a like until it is measured against the archive** — the house measure-first rule (never-gated, never-penalizing, raw value archived, recalibrated later); what a like buys TODAY is a filter/sort on the screener, and feeding it into tier or score is a separate evidenced decision. (b) **Sample size makes this a slow program**: 56 'considered' marks exist after ~4 months, so a liked-vs-passed feature comparison wants ~50 of each and is a study for late 2026, not next week. Collect now precisely because of that lag. | `models.SetupReview.verdict` (closed set); `archive_reviews._toggle_verdict` + `POST /reviews/like` + `GET /reviews/liked`; `hooks/useReviews.js`; `ScreenerCard.LikeButton` (♥, key `L`); `tests/test_setup_like_verdict.py`; DESIGN.md |
| 2026-09-02 | **The CONSOLIDATION sets the window, the card goes back to three-up, and the card's spare room is VERTICAL — the operator's correction to the same day's framing change.** He looked at the merged result and ruled: *"you made that only 2 cards are visible per row, return it back to 4. Return it to the original width, I asked for a more accurate representation of the price data bars and the stock rather then a streched out version to make the consolidation tight by focusing on the consolidation area and then some room behind it for added context before the consolidation (this is way way too much). im requesting for a dynamic scaling based on the found consolidation that will generally will fit most stocks in the standard way. + I wanted to make use of the vertical space that each card has to display the stock more accurately slightly more a cube like"*, pointing at Finviz's screener charts view. **He is right on both counts, and the first cut made the SAME mistake as the trim it retired, pointing the other way**: a fixed 140-day span makes the window a property of the SURFACE instead of the SETUP, which is far too much room behind a 24-day rest. **(1) The window is now dynamic**: `capWindow = ceil((rest + pad) / baseWidthCap)`, floored at `minWindowBars` (55, a floor and not a span — inert on all 230) and capped at `legibilityCeilingBars`. A 24-day rest earns 83 days at 5.24 px/day, a 33-day rest 109 at 3.99, an 82-day rest 220 at 1.98. `referenceBars` is RENAMED `minWindowBars` so the field cannot be read as a span again. **(2) Three-up at the original 480px minimum** — reverted from 620. At 1536 the arithmetic does not permit four (that needs a ~364px card, NARROWER than the original); shown both options with measured consequences he chose three. Two-up made the chart 3.1:1 — wide and short, a stock's whole run squeezed into a strip; three-up at 490px with the 225px well is **2.17:1**, within a rounding error of the Finviz charts he pointed at (2.1:1). **The card's spare room was vertical, not horizontal** — that is the general lesson. **(3) A counter-intuitive finding worth keeping: the legibility ceiling has to sit HIGH.** Lowering it to 110 reads like a legibility win (120 cards land in the faithful band instead of 47) but it makes the window stop growing while the rest keeps growing, so past the ceiling a LONGER rest draws NARROWER than a shorter one — the lie returns on rests as short as **37 trading days** and order inversion goes to **9.7%, worse than the 8.8% this program exists to remove**. At 220 it is **0.0%** (6 pairs of 25,904, all rests of 130+ days). Legibility on the long tail is bought with the yield, never by lowering the ceiling. Measured after: base share of card width med 30.6% (was 51.9), over-60% 6 (was 97), 0 boxes cropped; the price is 82 cards under 3 px per trading day (was 8), all long rests — the triage cards sit at 4-5.2. | `CHART_FRAMING` (`minWindowBars` rename, mini 55/220/0.35/0.75); `CARD_MIN_PX` 480 at all five sites; [chart_framing_2026-09.md](chart_framing_2026-09.md) "What the first cut got wrong"; DESIGN.md |
| 2026-09-02 | **Branch review: the chart pane could grow with the card but never shrink back, and four guards could not fail.** A 6-lens adversarial review of the framing branch (24 findings, each independently refuted or confirmed by execution; 11 survived). Two mattered. **(1) The mini chart RATCHETED.** Its container is a row flex item and carried `minHeight: 0` but not `minWidth: 0`, so it kept `min-width: auto` = the inline pixel width lightweight-charts stamps on its own element. Reproduced live: load the screener at a one-column width, widen to 1536, and **all 24 cards keep a 1160px canvas inside a 740px well — a 420px overhang (57%), clipped silently by `.screener-card { overflow: hidden }`, taking the right price scale and the newest bars with it, and it never recovers** because the ResizeObserver never sees a shrink. Pre-existing (reachable on `main` at a 30% clip); this branch's 480 → 620 card minimum moved the boundary and widened the overhang. Fixed with one token, A/B-proved in the browser: without it 24 of 24 clipped after forced frames, with it 0 of 24. **(2) Four guards that could not fail**, all mutation-proved before and after: the zoom-trap test pinned only the POINTER half of the `--ui-scale` division — deleting it from the VIEWPORT half left the suite green while misplacing the glass on 43.8% of screen points at Scale 1.25; the `popover` profile had no outcome pin at all, so copying the card's 140/220 into it passed 45/45 while rendering every glance at 2.36 px per trading day; the profile-completeness block gated itself on `referenceBars != null`, making that field's omission the one omission it could not see (and a missing `referenceBars` sends NaN into `setVisibleLogicalRange`); and the `index.html` pre-paint parity test re-typed the twin's algorithm instead of running it, so the real script could drift or be deleted outright with everything green. All four now have real guards, plus a five-site pin on the card minimum. **Method note:** the review also caught that the ceiling-yield edit landed AFTER the evidence doc was written and nine figures across two docs were left at their pre-yield values — every one contradicted by the census those docs name as their reproduction. Corrected in place (all the 2026-09-02 rows are new in this same uncommitted change, so append-only does not bind them yet) and the census now measures the retired glance too, so no figure in the doc lacks a command that prints it. | `ScreenerMiniChart` `minWidth: 0`; `glanceMath.test.js` viewport-flip guard; `chartGeometry.test.js` popover fixtures + `BOX_KEYS` completeness gate; `uiScale.test.js` five-site pin + `node:vm` pre-paint execution; [chart_framing_2026-09.md](chart_framing_2026-09.md) |
| 2026-09-02 | **The legibility ceiling YIELDS to a base it cannot frame — monster bases zoom out.** Operator, shown the seven cards whose rest still owned 61-100% of the card width at the 220-day ceiling: *"for monster bases simply zoom out the base"*. Implemented as `ceilingYieldShare` 0.60 on every box-framed profile: if the base would STILL own more than 60% of the pane at the ceiling, the window opens as far as the base-width cap wants and the carried history allows. **Deliberately a TAIL rule, not a raised ceiling, and the difference was measured**: simply moving the ceiling to the full carried history fixes the same four cards (over-60% 7 -> 3) but drags **45 medium-base cards below 3 px per trading day** (from the ceiling's 3.12 down to 2.31-2.96, 17 of them all the way to 2.29); the yield reaches the same result at a cost of 8 cards instead of 47 (CRSP GNE REXR AM STRA TTC XRN CHD). Measured after: base over 60% of width 7 -> 3, over 45% 19 -> 17, 0 boxes cropped. **Two honest costs, recorded rather than buried.** (a) Order inversion (a longer rest drawn narrower than a shorter one) goes 0.0% -> **0.1% (37 of 25,904 pairs)** — the yield is a step, so a base just past the threshold gets a much wider window than one just under it; **all 37 involve bases of at least 96 trading days**, so the property the operator complained about (a 33-day rest vs an 82-day rest) is untouched. (b) **STRA (rest 344 days), XRN (263) and CRSP (189) cannot be fixed by framing at all** — the payload carries only 300 days, and the cap wants 872/766/555; STRA still fills 100% of the card. A per-setup history floor for the ~14 long-base setups would cost well under 1 MB (a global `DASHBOARD_CHART_DAYS` raise would cost 6.3 MB) — **not built, it is a scan-pipeline change and his call**. **Method note worth keeping:** the yield made the never-crop CLAMP redundant — swept over every base length x forward count x history depth on all three profiles, it never binds — so its fixture test would have passed with the clamp deleted, i.e. would not have been a guard at all. The clamp is KEPT as a labelled backstop (it goes load-bearing again the moment `ceilingYieldShare` is raised above 1) and the ruling is now pinned by a property SWEEP whose mutation is deliberately two edits. | `CHART_FRAMING.*.ceilingYieldShare` + `baseCappedWindow`; [chart_framing_2026-09.md](chart_framing_2026-09.md) monster-tail section; `tools/chart_framing_census.mjs` (now prints the density cost) |
| 2026-09-02 | **The hover glance is a PREVIEW, never a verdict — and it now opens beside the cursor.** Two rulings in one exchange. **(1) The glance's honesty is accepted as-is.** Asked whether to widen the 380px glass or rule it a preview, given that even under the new framing its base still owned a median 35% of the pane and 63/230 exceeded 60% (the worst surface in the app), he ruled: *"oh yeah that's fine"*. **Those are the figures that were in front of him**, measured before the monster ceiling-yield landed later the same session; the shipped glance now reads med 35.2% / p90 78.1% / 48 of 230 over 60%, because the yield was calibrated separately for that profile (see the ruling above). The ruling is unaffected: it is about what the glance is FOR, not about a number. So the glance's job is RECOGNITION — which row is this — and the card is where tightness is judged. **The standing consequence, and the reason this row exists:** the glance's 380px constraint may NEVER be used as an argument to lower the card's or the modal's span. A future session finding the glance dishonest must widen the glass (`GLANCE_WIDTH`, `glanceMath.js`) or leave it alone — never retune `CHART_FRAMING.mini`. **(2) Placement re-ruled to follow the cursor**, verbatim: *"can you make them appear near the location of my cursor instead of static?"* It was placed against the row's ticker cell, so on a wide table the chart opened far from where he was looking, in the same spot for every row. Now: down-and-right of the pointer with a 16px gap, each axis flipping independently at the viewport edge (both flips computed from the same point, so a bottom-right cursor gets the glass up-and-left and never under itself). It re-places on every row change, not on every mouse move — a 380x250 chart that jitters per frame is unreadable, and within one row the glass holding still is what lets him read it. The `--ui-scale` zoom correction is unchanged in kind and re-proved live at 1.25 (cursor 585.6 screen px -> `left: 484.8px` pre-zoom, rendering 475px wide). `data-glance-anchor` is retired with the anchor placement it existed for. | `glanceMath.glancePlacement` (cursor-based); `useHoverGlance` frame carries the pointer, `readRect` -> `anchorIsLive` liveness check; `HoverGlass`; `data-glance-anchor` removed from ArchiveTable / WatchlistZone / ScreenerWatchlistPanel; [chart_framing_2026-09.md](chart_framing_2026-09.md) glance section |
| 2026-09-02 | **The card and the modal frame by a fixed reference time scale that only a long base stretches; the vertical proportion trim is RETIRED, and the chart well grows for the first time.** Operator, with two screenshots of MSEX side by side (his card, and the same stock in TradingView at ~190 trading days): *"the mini charts are showing the consolidation much wider (horizontally) then they are making them appear much much tighter when they actually are making me confuse wide choppy setups for cute tight ones … + when looking at the close up chart, I always got to zoom out to see the real stock."* **The cause, measured** ([chart_framing_2026-09.md](chart_framing_2026-09.md), reproducible with `node tools/chart_framing_census.mjs`): each card bought VERTICAL box height with a HORIZONTAL knob — delete the oldest trading days until the box owned 40% of the visible price range (`minBaseHeightFrac`), floored at 55 days (`trimFloorBars`, shipped in the Finviz UI program and **never ruled**) — and carried an escape hatch that trimmed MAXIMALLY once the target was proved unreachable. It fired on **179/230 cards and was FUTILE on 140 of those** (burned the context, still missed its own target); it succeeded on 39. The price: the base owned a median **51.9% of card width**, 97/230 over 60%, and **2,275 of 25,904 card pairs (8.8%) drew the LONGER rest NARROWER than a shorter one** — his complaint, as a number. The modal was pinned at **81 trading days on 140/230** while the payload already carried a median 300. **Ruled wrong and REMOVED, not retuned.** New model: one `referenceBars` per surface (mini 140 ≈ 7 months, modal 252 = one trading year, popover 70), stretched only when a base would own more than a shared `baseWidthCap` 0.35, bounded by `legibilityCeilingBars` (220/300/105) — and every bound still yields to `minContextBars`, the 2026-08-09 never-crop ruling, which is the ONLY rule that survives. **Measured after: base share of width med 26.4%, over-60% 7/230, order inversion 0.0%, 0 boxes cropped, modal med 252 days (84% of the payload, the rest one scroll away).** The vertical job moves to the chart well, which had **never grown** — `clamp(180px, 11vw, 240px)` resolves to 168.96px at 1536px and sat permanently on its 180px floor at every UI-scale step (CSS `zoom` does not rescale `vw`) — now 225px, plus the mini date axis hidden, so the price band goes 123px → 180px (+46%) and the box is DRAWN at the same pixel height while the card shows 2.3× the history. Card min width 480 → 620 (two-up at 1536, 742px card) on all five sites. Not engine theory: `chartGeometry.js` decides which candles are painted, not what a good setup is. | `CHART_FRAMING` + `miniFocusLogicalRange`/`modalFocusLogicalRange` (`proportionTrimmedFrom` deleted); `--card-chart-h` in `index.css`; `uiScale.CARD_MIN_PX` + its `index.html` twin; [chart_framing_2026-09.md](chart_framing_2026-09.md) + `tools/chart_framing_census.mjs`; two Tested-DEAD rows above |
| 2026-09-02 | **The Power Play register is REMOVED from the Home tab — the display retires, the history does not.** Operator: *"home tab is full with 'powerplay' registery, i never asked for this remove it immdeadtly it bloats up the screen to infinite downscroll sizes."* He is right on both counts. It arrived as a side effect of the watchlist merge rather than as an ask, and the record that mounted it claimed it *"returns null on an empty projection, so it costs the cell no height while both species flags are dark"* — **falsified by the live artifact, which carries 79 `power_play.candidates`**, every one of them rendered as a row. Deleted: the component, its pure projection and its node battery (with the `package.json` test token; `testManifest` is a symmetric pin, so both sides moved together). **KEPT deliberately:** the engine and backend are untouched — `screener.py` still writes the block and `scan_job.py` still reads `power_play.counts` into every scan record, so suppressing the emission would blank that column on all future scans (AP-12: display retired, history did not). The closed-set status vocabulary stays in `wireVocabulary.js`, and its pin — the frontend half of a deliberate two-sided contract that `tests/test_power_play_lane.py` names in prose — was **rehomed** to `wireVocabulary.test.js` rather than dying with the deleted battery, and mutation-proved there. | `home/HomeView.jsx` (unmounted); `PowerPlayRegister.jsx` + `powerPlayRegister.js` + `powerPlayRegister.test.js` (deleted); `wireVocabulary.test.js` (pin rehomed); [power_play_program_2026-08.md](power_play_program_2026-08.md) claim corrected |
| 2026-08-30 | **The word table CLOSES and the position vocabulary grows its honesty valve — markup signed, TOUCHING_BOTH ruled a fourth value, the ceiling label is “at resistance”.** **(1) markup SIGNED verbatim** — once the term was explained in plain words (bars leaving the base upward and holding far above R, the post-breakout climb): *“ahh I see then yes we can use markup”*. The whole ONE-vocabulary word table is now operator-signed; the departure proposal is dropped. **(2) touching_both RULED its own value**, verbatim: *“yes, so we can differentiate between a base that actually has tight moves in the upper vicinity of the base (because its the correct read) rather then that being the case because the consolidation is 1 bar worth of witdh”*. A rest engaging BOTH rail areas at once reads `touching_both` — never a fabricated ceiling read; this replaces the ceiling-first tiebreak (documented and replaced the same day). Closed set is now FOUR: at_ceiling / mid_range / on_support / touching_both (model CHECK widened; EC-19). Measured impact on the 37-ticker full-output capture: **exactly ONE fire flips — CVI at_ceiling→touching_both (top −0.22 ATR vs R, bottom +0.40 ATR vs S: the mini spans nearly the whole base); zero score/tier/election drift anywhere.** **(3) The ceiling display label SIGNED: “at resistance”** — verbatim: *“lets go with 'at resistance' since the top of the base is the Resistance line”* (his earlier “at the ceiling” objection stands: that phrase is a bar STATE, not the area). All four labels now serve: at resistance / middle of the base / on support / touching both (the last styled “Touching both rails” on the chip — his phrase, one-line re-word if he prefers). **The serving landed the same sitting**: `inner_position` on the wire + four position chips (one per value, at most one fires per row) resolved ENGINE-side through the tag registry — a fire-rule addition, so `engine_config_version` rotated 92863be0→10fd83cb as the declared epoch; on the capture, exactly the 4 position-carrying fires gain exactly their one chip. **(4) Housekeeping approved and done**: the five 2026-08-29/30 ruling rows misfiled under the Tested-DEAD header were cut-pasted byte-identically into this table (pure move, verified by per-row SHA-256; append-only law respected — nothing reworded). | `inner_box.mini_consolidation_position` (the both-bands branch); `taxonomy.py` position TagSpecs + `tagCatalog`/`tagTooltips`/`wireVocabulary.POSITION_LABELS`; `dashboard.py` wire pick + the wire-key snapshot (151); reader-pin grid row 8 + seam recapture; asks.md both successor rows closed |
| 2026-08-30 | **The word table SIGNED (one word still open), and the first position label lands — the operator's rulings on the ONE-vocabulary asks.** **(1) The breach verdict is BREACHED**, verbatim: *“lets change Gave to breached because that's the official term for an instance when the price crosses a boundary (for both resistance & support) - Again we should use the same vocabulary for both resistance and support, ofc we want the resistance to hold but again the key word here is context, the resistance got breached because of what exactly? then it receives and entire new meaning.”* The verdict axis is now **held / breached / open / unreadable** — ONE word for both rails; what a breach MEANS is context’s job (Program 2), never the verdict’s. Applied to the declared dicts, tests and docs; the census instrument keeps its pre-signing internal “gave” under its frozen-conventions docstring (it must reproduce the sealed evidence byte-for-byte). **(2) touch_and_pivot SIGNED — and widened**: on his own screenshot ruling, all three red marks *“should count as Touch & Pivot, because after reaching resistance whenever the bar stopped 'at the ceiling', 'Semi-breached it', or Rests on it, they all pivoted later”*; verbatim on intent: *“the point here is not to invent these phrases so we can label each individual bar that dosen't add anything useful to us, the point here is to get accustomed that not all consolidations are pixel perfect and that we should judge by the overall shape rather then the cold knobs or whatever it is that gets rejecting normal setups.”* The engine already conforms: the wave reader’s breach line sits at `BOUNDARY_ATR_BUFFER` = 0.50 ATR = the ruled area’s outer edge, so an in-area poke that pivots back types `rejection` (→ touch_and_pivot), and only an excursion clearly BEYOND the area that then collapses types `upthrust` — his own “an UPTHRUST that recovers later into the base” context, kept. **(3) The markup word is OPEN** — verbatim: *“idk what markup is, I never used it when describing a single thing to you.”* The emitted type stays `markup` (frozen stored vocabulary, AP-12); the PROJECTED word awaits his signing; the standing proposal is **departure** — his own ruled word for bars clearly continuing up. **(4) “middle of the base” SIGNED** for `mid_range` (*“clear of both rails”*), with a caveat, verbatim: *“if you are referring to a consolidation that is so tight that it primarily consists of mostly single bars then consider it as they were touching both”* — a degenerate ultra-tight case must never read as floating mid-air. Today’s arithmetic already cannot call it middle (on a tight parent the two ±0.5-ATR bands overlap and the documented ceiling-first tiebreak wins); whether that case deserves its own touching-both NAME is his open call. “on support” he blessed as self-explanatory the sitting before; the ceiling label (proposal: “top of the base”) still awaits his word. | `event_vocabulary.py` declared dicts (the one-line-edit seam); `strategy_alpha.md` verdict axis; the program record §word table; `docs/asks.md` — both rows closed to successors |
| 2026-08-30 | **THE RAIL AREA IS RULED: ±0.50 ATR, symmetric, both rails — and “a bigger swing upwards” means the bars AFTERWARDS clearly continue up.** Closing the previous row's two opens. **(1) Symmetry CLOSED** — operator: *“Okay sure, lets do it”* on the bar-state census verdict: every harshness-by-position variant is anti-predictive, flagging HIS boxes harder than junk (any whole-bar-above outside markup 17/35 drawn vs 26/98 junk; mostly-above 27/35 vs 41/98; below-S mirror 18/35 vs 14/98; the one parity variant reaches 14%=14% only by swallowing the MSGS/NOK genuine right-edge rests). What-follows corroborates: drawn whole-bar-above breach runs resolved 8 left-UP + 3 right-edge, ZERO crash-back — in his boxes a whole bar above R is a departure/breakout signature, never a failure; the SEMI straddlers are what crash back (28/48); S-side breach runs reclaimed 79/89. So: **rail ± 0.5 ATR, one tolerance, both rails, both sides of each line, ATR units (2026-08-30 unit row), evidenced onto the incumbent `TOUCH_TOLERANCE_ATR` — no number moves.** The 2026-08-29 guard-rail stands verbatim: position vocabulary ONLY; as a gate/veto/rescue/junk filter a 0.5-ATR allowance is Tested-DEAD twice. **(2) The ‘bigger swing upwards’ exclusion DEFINED by outcome** — operator, with a hand drawing (a box whose three RED marks are pivots at/slightly above the resistance line, each turning back into the range): *“The bars after wards determine the meaning … all 3 red marking are a pivot at around the Resistance Area, 'part of a bigger swing upwards' means to a scenario where bars clearly continue to go up.”* So a poke/rest in the ceiling area that PIVOTS BACK is respect — including pivots printed from ABOVE the line — and only continuation (price clearly leaving upward) makes those bars a departure. This is the SAME discriminator he ruled 2026-06-28 for SOS-vs-upthrust (“what happens AFTER”): resolution-based typing IS his eye, and the engine's wave reader already implements it for completed waves. At the right edge ‘afterwards’ does not exist yet → the vocabulary carries an honest UNDETERMINED there (the census measured 16/20 of his true rests overlapping in_progress waves — the rest usually IS the right edge), consistent with the engine's no-lookahead law. | Bar-state census `BARSTATES.md`/`BARSTATE-VERDICT.md` (session scratchpad; committed-instrument task rides `PLAN-one-event-map.md` Task 3); `TOUCH_TOLERANCE_ATR` now carries a ruling + two measurements; rest/departure/undetermined = the Task 8 naming contract |
| 2026-08-30 | **The rail area is BAR-SCALED (ATR), the ceiling area has THREE visual STATES that are never a quality ranking, and holding below support IS respect — the operator's answers to the rail-area experiment's three open questions.** **(1) UNIT RULED — the area does not scale with the consolidation**, verbatim: *“no I believe it dosen't need to scale with the width of the consolidation, and I don't think it needs to get thicker.”* His eye and the measurement agree independently (the ATR cut moved 10% across a 1.8× box-height change while the box-fraction cut moved 3.4×). He also restated the DRAWING doctrine behind the rails, verbatim: *“I Draw In a way that I choose the most compatible anchors to 'Wrap' around the consolidation as much as possible without leaving too much empty space, so I can frame the price moves easily and give them roles that fit the narrative, meaning for an SOS to breach Resistance and then tightly rest on it in a form of LPS, I need to make sure I drew the Resistance in the right place in the first place. … I just look for the pair limbs to start and draw the consolidation from and usually its the Trends Climax and rally or the next 1 - 3 limbs afterwards where the consolidation truly begins to settle, that's there is all to it”* — with his own caveat *“don't take it literally though … it just works most of the time”*. Corroborates the dead-space rail-SELECTION rule (2026-08-23) and the root-swing cascade (2026-07-02); no new lever. **(2) The ceiling-area STATES, reframed by him**, verbatim: *“only these states exist: Resting on Resistance (Whole bar is above & not apart of a bigger swing upwards), at Resistance (Whole bar is sitting at the ceiling e.g touching resistance and then pivoting back), and Semi dwelling (Meaning Semi above and Poking through, breaching slightly) I don't think neither is more meaningful to be honest, its just a visual thing. what makes them meaningful is the Context that surrounds said breach?”* (his examples: a huge-spread bar that later crashes = a tail of sellers, vs one bar of many that breaches slightly while the consolidation continues normally). The states are DESCRIPTORS — never ranked, never gated; the judgment of a breach is context, i.e. Program 2. On SYMMETRY he leans yes but hedged it explicitly — *“I think I lean towards Symmetric? but if data suggest other wise then.... IDK man we got to dig deeper on that”* — and named the inner edge's purpose: *“we should also have a clear area below the resistance that would count as a touch so we won't drift too much”*. A bar-state census (his three states × singular-vs-run × what-follows, drawn vs junk) was commissioned the same sitting to close the symmetry hedge. **(3) Holding below support RULED as respect**, verbatim: *“we should also treat them the same as if they were inside the box because that's the whole purpose of it being an 'Area' because they are holding AT the support essentialy proving that this line is infact a support line that is being respected”*, with the stated preference *“when all/most bars stay inside the box”* — a graded preference, not a penalty. **Standing consequence:** the ±0.50-ATR measured recommendation stands uncontested on value and now RULED on unit; symmetry provisional pending the bar-state census; the 2026-08-29 guard-rail unchanged (a 0.5-ATR allowance is position vocabulary ONLY — as a gate it is Tested-DEAD twice). | Rail-area experiment `RAIL-AREA-REPORT.md` (session scratchpad; committed-instrument task in `PLAN-one-event-map.md` Task 3); `TOUCH_TOLERANCE_ATR` = the incumbent the ruling evidences; bar-state census = the dig-deeper follow-up |
| 2026-08-29 | **THE RAILS ARE AREAS — the position vocabulary is THREE values, not five, and the above-resistance treatment is a CONTEXT question that may not be ruled in the blanket.** Put the four position questions from the ONE-Event-Map council plan (`PLAN-one-event-map.md` Task 8) to the operator; he collapsed three of them and refused the fourth. **(1+2) The resistance AREA is one position**, verbatim: *“at resistance = high in the range = semi above resistance = resting on resistance. since resistance is a line that is to be treated like an area, a thin area but still.”* — so “high in the range” is NOT a separate band (*“no need, see answer 1”*), and neither is “above resistance”: a bar at the rail, a bar semi-above it, and a rest on top of it are ALL the same position. **(3) Support is the mirror**, verbatim: *“it is to be treated the same way we treat resistance, of course the Support we mark is preferably one that is respected but we also need to account for variations where bars are slightly breaching (poking below), and holding a little below.”* — the support area likewise spans a little above and a little below its line. **CONSEQUENCE: the existing three-value closed set is RIGHT and the plan's proposed five-way split was over-engineering.** `inner_box.mini_consolidation_position`'s `{at_ceiling, mid_range, on_support}` survives as THE position vocabulary; the census's finding that `at_ceiling` unions above-R with at-R and `on_support` unions on-S with below-S is a CORRECT reading of the chart, not a defect to split. The open parameter is no longer *where are the five bands* but **how thick is a rail area** — a single tolerance question, with existing candidates already in the tree (`TOUCH_TOLERANCE_ATR` 0.5 = the touch band, `BOUNDARY_ATR_BUFFER` 0.5 = the breach buffer, `MINI_POSITION_TOL_ATR` 1.0 = today's position tolerance, and the ceiling rest's 0.3-ATR razor whose drawn class measured 0.010–0.241 against junk ENIC at 0.314). **(4) The above-resistance treatment REFUSED as a blanket ruling**, verbatim: *“'Bars sitting above resistance' depends on the context (KEY WORD HERE REMEMBER THAT) are theese bars an LPS? an UPTHRUST that recovers later into the base? a mini consolidation that rests on the resistance? forming a double base? we can't be vague when making hard ruling like this.”* — the chair's question was mis-posed: the two live paths that treat this shape oppositely (`box_gates._worked_window_end` trims it as earned; `rail_qualification` disqualifies it as a departure) are NOT one-right-one-wrong. Both are right in their own context, and the real defect is that **neither consults what the bars ARE**. This is the 2026-08-29 narrative-positional ruling applied again (*“depends on where though… if we are in Phase D, we found an SOS…”*) and it is the strongest argument yet FOR the one-vocabulary program: an engine cannot dispatch on a context it never named. **Scope discipline held:** Program 1 ships zero behavior change, so it may only ensure the vocabulary CAN express which of those four things the bars are (LPS · upthrust that recovers into the base · mini-consolidation resting on resistance · a new base forming) — the context-dependent TREATMENT is Program 2's judgment split and must not be pre-empted here. | Collapses `PLAN-one-event-map.md` Task 8 questions 1-3 and reframes 4; simplifies Task 6 (one mechanism, three ruled values) and dissolves the widening half of Task 9. Substrate: `inner_box.mini_consolidation_position` (+ its test battery), `lps.py` ceiling rest, `box_gates._worked_window_end`, `rail_qualification`, `box_events.measure_resistance_events` (already types upthrust/SOS/markup) |
| 2026-08-29 | **The forgiveness doctrine CONFIRMED — the six traits ruled point by point, with the respect-form taxonomy, the fatality definition, narrative-positional judgment, and the grounding directive.** The operator answered the played-back trait list: **(1) both rails worked — yes.** **(2) Rails hug — yes, expanded**, verbatim: *“we strive to find the true trading range of the consolidation within our Rails, and not be afraid of the different types ‘Respect’ that setups may show, I'm talking about bars that Touched the rail and pivoted back, Bars that Slightly Poked above/Semi inside the box and bars that are Resting Above Resistance/ Holding Below support all of these counts … consolidations look like that and there is nothing to be afraid of.”* — the respect-form taxonomy: touch-and-pivot · slight poke / semi-inside · resting above R · holding below S — ALL are engagement, none is a defect. **(3) The fatality definition replaces ‘breaches resolve’**, verbatim: *“What's fatal is either a collapse of the structure (Meaning it never Recovers) or that we can barely decipher or find a fit for that setup that can translate into something of high quality by our measures. meaning that there isn't anything there that we can understand as an accumulation process, meaning very poor respect of a box, zero high quality traits found.”* — reject only for collapse or unreadability; everything between clean-pass and fatal is forgiveness territory. **(4) Excursion judgment is narrative-POSITIONAL**, verbatim: *“‘outside bars stay close and come back inside.’ that depends on where though..... if we are in Phase D, we found an SOS that breached support and now bars started to consolidate on a higher plane or something like that then we should be able to ‘FORGIVE’ it … a smart algorithm that can realize how a narrative is evolving in a stock.”* **(5) Terminal tightening** — *“usually that's a good trait we are hunting for, because the ‘END’ is the LPS. so yes preferably tightens”* — a hunted trait, not a demand. **(6) Freshness is TWO-BRANCH**, verbatim: *“Either price found a higher support level that gets visited often suggesting that the consolidation is accuring in a higher plane, or simply we reached a point in the consolidation that we shouldn't expect a support visit because of the current narative state that the stock is in, E.G Mini consolidation, SOS, LPS and the likes.”* — a stale original anchor is innocent when a higher support works OR the story state explains it. **(7) The GROUNDING DIRECTIVE**, verbatim: *“everything is to be based on existing code or at least be consulted in existing code because we are already doing a lot of things right but as this engine and this entire project develops I need to make sure we are not losing the Wind in our sales and shift our entire doctrine to something that I have no control over or is too bloated and messy because of conflicting orders, context and understanding.”* — the forgiveness build EXTENDS the existing machinery (election pools, the narrative state machine, the event map, the measured traits), never a parallel system. Inventory at ruling time: traits 1/3/5 fully measured today (rail episodes + sequence stats; the respect/occupancy gates ARE the fatality floor; LPS detection + contraction measures); trait 2's forms measured except ONE — ‘holding below S’ has no respect/completion form (the ceiling rest's uncovered twin; `_holding_shelf_verdict` is a HIGH-in-structure form, not this); trait 4's gap is that refusal sites judge geometry blind to story position (the event map already types the story — nothing consults it at rejection); trait 6's branch (a) exists (ascending-support measures + the rising-support-shelf rescue) while branch (b) — the story-state exemption on the descent-tail read — does not. Design goes through council-plan on the operator's go-ahead; `strategy_alpha.md` absorbs the trait doctrine when the program lands (owed, named here so it cannot drift). | This row is the doctrine anchor. Substrate: `event_map.py` episodes/stats, `box_gates.py` hang masks + respect/occupancy floors, `lps.py` forms (+ dark ceiling rest), `rail_qualification.py` hold chains, `metrics.py` equilibrium/dead-space, `tools/shape_profile.py` census |
| 2026-08-29 | **Consolidation “families” are NOT buckets — everything resolves to a zigzag at some scale, and the engine's requirement is FORGIVENESS.** After the shape-profile census (`tools/shape_profile.py`, all 35 drawn marks on the drawn rails: NO measure — raw or per-bar — splits the named zigzag set VLO/YPF/SYRE/ROIV/DLX from the named cluster set DSGN/ANRO/NGL; dead space ~0 at both rails in 35/35), the operator corrected the read on his own SYRE and ANRO screenshots (red zigzag drawn through BOTH — SYRE's strides across the box, ANRO's is the same oscillation compressed) and ruled, verbatim: *“Resting on Resistance does not indicate that the base is clustered, Only the Part that Rests is actually Resting there. it is its own separate read. … everything at the end produces some sort of what essentially is a ZigZag. so it makes me rethink that we do not need to split it into these buckets, I guess what really matters is that I need the engine to be able to 'Forgive' a setup if we can still Find the traits that we are looking for and understanding to be good, like I taught you.”* Consequences: **(1) no shape/family classifier may be built** — shape is a continuum descriptor (oscillation scale relative to the bars; `bar_overlap` orders it: ANRO 0.944 top, NOK 0.367 bottom), never a class and never a gate — a bucket lever is dead on arrival, its own census falsified it before it was built. **(2) Resting on R is a CHAPTER read**, the terminal rest (the LPS/ceiling-rest family), never a base identity — the mistake this ruling corrects. **(3) The program pivots to the FORGIVENESS architecture**: a letter-gate refusal earns a second look for the TAUGHT good-traits before final rejection; forgiveness demands POSITIVE presence of other taught traits and is bounded by the junk corpus (a forgiveness rule that admits any of the 18 must-not-fire cases is threshold-fuzzing, and fuzzing is Tested-DEAD). The existing pool escalation (strict→rescued→band→story) and the four miss-program lanes are this pattern's existing instances — the build direction is to make forgiveness first-class rather than a pile of exception flags. The trait list itself is played back for the operator's confirmation before any build. | `tools/shape_profile.py`; `output/shape_profile_2026-08-29.json`; the Shape Profile artifact page; forgiveness design = next program, gated on the operator's trait-list confirmation |
| 2026-08-29 | **NOK and MDT are WANTED catches — the miss program's second half is ruled in.** After re-marking the misses (PKE redrawn as a NEW later base @2026-04-07 — which the engine fires AT BASELINE, 2026-03-26 tier B on his rails, retiring PKE from the miss list; EGBN's rails/window nudged; NOK's LPS re-drawn to the 2-bar 02-12..02-13 rail rest), the operator ruled, verbatim: *"Okay I Remarked some of these I do would like you to take a look at Nok and MDT again, I don't want to miss them."* Consequences, both built DARK the same session (flips his): **(1) the bottoming-base lane** (`BOTTOMING_BASE_LANE_ENABLED`) — the sma200 rule opens at BOTH its layers (universe door + anchor seeding) when the 50-day is reclaimed; this resolves the MDT scope ask in the catch direction (Chrollo reads a bottoming base once its intermediate trend has turned). MDT fires 2026-07-14 tier S at his exact resistance. **(2) the ceiling-rest LPS exception** (`LPS_CEILING_REST_ENABLED`) — an INSIDE window that launched above R is no longer refused when its REST lands ON the ceiling (within 0.3 ATR under R); the bar is drawn-evidence-placed: the corpus's launched-above shelves rest at 0.010/0.087/0.148/0.241 ATR under R (DSGN/MATX/MSGS/NOK) while the nearest junk (ENIC, must-not-fire) rests at 0.314 — a stated ~0.06-ATR razor each side, pinned by the negative-corpus flag-on leg (an 0.5 bar fired ENIC tier A and the guard went red). NOK fires 2026-02-13 tier B at his exact resistance, ordinary strict pool. Guards at the build: junk corpus 18/18 with ALL FOUR miss-program lanes armed; shadow panel NO DRIFT with the ceiling rest armed; flags-on ratchet breaks on exactly EGBN/NOK/PKE(sealed old mark)/SKYT — the reseal is 28/33 → 32/33, pinned by name. | [miss_program_2026-08.md](miss_program_2026-08.md); `config/settings.py` both flags; [flag_ledger.md](flag_ledger.md) rows; `tests/test_miss_program_lanes.py` + `tests/test_negative_corpus.py` |
| 2026-08-28 | **SOS doctrine: the SHAPE is the definition — volume is out, officially.** After the Corpus Study measured all 22 drawn SOS (speed 5.5× and one-way-ness 4.5× separate marked from passed-over rallies; magnitude and volume separate nothing; 7/22 ran on below-average volume while the boxes' loudest bars are all unmarked failures), the operator ruled, verbatim: *"its basically this Immediate up surge of buyers that dosen't tie in practically with volume even though the written material suggests that it is … ITS IN THE SHAPE OF THE THING rather then the Numbery measurement, if it walks like a duck, quacks like a duck might as well be one. It can come in may variation overall but its role is always the same a sign of strong buyers before the breakout."* Consequences: an SOS detector types on price shape (span, travel, speed, efficiency, launch position, keep) and records volume as data only; the fitted thresholds are proposal INPUT for his eyeball (the 2026-08-26 decisiveness row stands); and the bar is a measure, never a classifier alone — adversarial verification found ~20 unmarked waves pass the fitted 8-clause bar on exhaustive sub-window scan, so election/story context is load-bearing. Measured at marks_fingerprint `a8543e30…` (22 drawn SOS; the same-evening re-mark pass grew the census to 27, all inside the fitted envelope). | [`corpus_study_2026-08-28.md`](corpus_study_2026-08-28.md); [`strategy_alpha.md`](strategy_alpha.md) SOS section |
| 2026-08-28 | **The Last Supper anchors to ANY up-move — never SOS-bound.** All 10 drawn specimens happen to follow an SOS; the operator ruled that reading this as definition would be a false truth, verbatim: *"Last Supper isn't always after an SOS and I don't want the algorithm to treat it like it is. Its a BU a reaction of sellers that consists of a deep correction in price usually after an UP move, be it an SOS an LPS a Breakout, it's the last trap before buyers take over and the long continues, simply one more piece of the puzzle - recorded. here to make sure you recognize the shape of it."* Consequences: any future detection anchors on the preceding up-move whatever its type, types the SHAPE (a deep fast seller reaction that holds the floor, then continuation), and must not require a typed SOS predecessor — the 10/10-after-SOS grammar statistic is sample fact, not definition. Descriptive-only and not-a-warning stand (2026-08-26); the event build itself stays reserved for the operator's go-ahead. **Supersedes the 2026-08-23 "Last Supper = the final-shakeout class" definition** (row below): the downside-break shape moves to the chain's failure branch and no longer carries the name. | [`corpus_study_2026-08-28.md`](corpus_study_2026-08-28.md); extends the 2026-08-26 row and supersedes the 2026-08-23 definition below |
| 2026-08-26 | **The Last Supper is NOT a warning, and the name collision RESOLVES — the engine's own definition was right all along.** Verbatim: *"'the final run-up that traps late buyers before the real pullback.' Its Correct also important to note that may also happen after the LPS as a TRAP so its chronological appearance ain't always the same. the Point here is that I want the algorithm to know it exists as an event and to not be afraid of it, Last suppers aren't a bad thing. many stocks popped off after said event because its essentially just a deep correction."* Three consequences. **(1) The three-way name collision dissolves:** his drawn mark IS the engine's event, so `last_supper` names ONE concept and the mark SHIPPED (the asks.md naming row closes unanswered-by-being-obsolete). His earlier *"my version of back up / BU"* and the 2026-08-23 *"final-shakeout class"* both describe the same shape — a run-up that traps, then a deep correction, then the resumption. **(2) The chip was lying about cost.** `last_supper` never took a point — it is absent from `_ta_grade_warnings`, and only `terminal_drift` / `weak_monthly` discount the grade (both neutral at 1.0) — but `warning=True` put it in the danger-toned Warning group, which contradicted engine_reference's own *"setups often break out AFTER the Last Supper has run its course"* and trained the eye to read a present Last Supper as a defect. Reclassified to the LPS group as a descriptive event chip; pinned by `test_last_supper_is_a_descriptive_chip_not_a_warning` so it cannot drift back. **(3) The chronology claim was ALREADY correct** — engine_reference has said *"can appear before OR after an LPS"* since the section was written; no doc fix was needed, which is itself the evidence that the defect was presentational, not conceptual. **STILL NOT BUILT:** *"know it exists as an event"* — the engine measures over-extension GEOMETRY around the LPS and deliberately does not label the run-up as a dated event. That is the event-typing program (`event_map` reserves the column family; BACKLOG Task 12), and it needs its own go-ahead. | `engine_alpha/scoring/taxonomy.py`, `webapp/frontend/src/components/tagCatalog.js`, [engine_reference.md](engine_reference.md) §Last Supper stretch; the mark in `marks_validity.EVENT_TYPES` |
| 2026-08-26 | **The three drawn-event marks DEFINED; two SHIPPED, Last Supper HELD on a name collision.** Shapes ruled before the build: **SOS** — two clicks, the launch low → the swing top (*"correct"*); the hold AFTER the top is NOT part of the SOS mark (that is the Mini consolidation's job). It therefore stores only the SPAN: decisiveness is ground covered over days, and both prices re-derive from the frozen frame — the same contract the rail anchors already rely on, so no new columns. **Mini consolidation** — *"like a small box, same as the regular consolidation"*: a date span PLUS a price band, drawn as two opposite CORNERS and normalized exactly like the R/S rails. It is the only event type that carries a band (`band_high`/`band_low`, required for it, well-formed-or-absent for everything else). Consistent with the 2026-08-23 ruling that the inner mini-consolidation and the ceiling shelf are ONE event whose POSITION is a graded attribute. **Last Supper** — *"its baiscally my version of back up or BU in wyckoff terminology"*, a swing of one or more bars — **DEFINED but NOT shipped**: the string `last_supper` already means two other things here (the engine's Phase-D over-extension warning geometry around the LPS, rendered as the "Last Supper" chip; and this file's own 2026-08-23 ruling *"Last Supper = the final-shakeout class… usually after price action breach of the base and a steep deep correction"* — a DOWNSIDE break, where the new meaning is an UPSIDE retest). A DB CHECK value is frozen vocabulary, so it is not being minted on an ambiguity — the naming ask is open. | `webapp/backend/models.py` (CalibrationMarkEvent), `marks_validity.EVENT_TYPES`, `services/startup.migrate_calibration_event_types`, `webapp/frontend/src/utils/calibrationMarking.js`; [asks.md](asks.md) Last-Supper naming row |
| 2026-08-26 | **Ground truth comes from the operator's DRAWN marks, never from mined candidate sheets — the standing method ruling.** Verbatim: *"How about we return to the calibration tab, add the functions to add SOS, Last Supper, Mini consolidation. and I will edit current and New Setups into that page and maybe discuss future engine change with safe setups rather the guessing random ticker that may or may not fit at all in the first place regarding of a setup we would like to see."* The mined-candidate path failed TWICE in two days, in two different programs: the story-chain sheet was ruled all-junk 2026-08-25 (a tightness screen adversely selects deal-pinned tape), and the SOS decisiveness bar could not be calibrated from engine-located waves either — **a mined ticker may not be a setup at all, so an operator verdict on it teaches nothing about the threshold** ("safe setups rather [than] guessing random ticker that may or may not fit"). The method, applied to every future threshold: extend the calibration workbench so the operator DRAWS the piece himself, then measure HIS marks (the stat-card lane) to place the bar. Mining is now a fallback that must justify itself, not the default first move. | this row (standing method); the workbench event-vocabulary build (2026-08-26); [asks.md](asks.md) SOS-decisiveness row rewritten to the drawn-marks path |
| 2026-08-26 | **Decisiveness is DEFINITIONAL for the SOS — ruled, not a grade (follow-up to the refined-definition row below).** Verbatim: *"Decisive is part of the definition, SOS can be made out of bars (plural) but its a quick that covers a lot of ground, Sharp, its a sign of strength, buyer strength."* So the label itself requires a decisive swing: multi-bar is explicitly allowed, but the swing must be QUICK and cover A LOT OF GROUND — sharpness = buyer strength. A shallow climb is not an SOS regardless of how well it holds. The measure therefore involves BOTH ground covered (launch low → wave top) and speed; the live census: 32/96 current SOS waves covered under half the box, and a grind tail covers real ground slowly (RMBI 0.38 box in 17d). The exact bar comes from the operator's eyeball of the located tails (asks.md 2026-08-26 row — dates+prices delivered in-session); the classifier change then lands via the EC-48 lane with flip counts, ratchet, and shadow measured on a branch. | this row + the refined-definition row; the located tails in the 2026-08-26 session; [asks.md](asks.md) eyeball row |
| 2026-08-26 | **The SOS definition refined — DECISIVENESS is definitional, ending location is a graded quality (operator teaching, same session as the ruled-A row below).** Verbatim: *"an SOS is a Decisive swing, not a shallow climb, dosen't even have to sit right at Resistance in case it happened lower, the important thing is that it's in Phase D, Decisive swing, and then a quality measurement of where the Price Ended (Higer Quality if its near the resistance, above it or close to prior High in case a shake out happened)."* Consistent with ruling A (no breakout required) and goes further. Three divergences from the current reader, recorded for measurement: **(1) no decisiveness requirement exists** — the classifier confirms by hold-quality alone, so a shallow climb that holds is typed SOS today (the wave already emits `strength_box` and `wave_bars`; neither is consulted); **(2) SOS material is high-zone turns only** (wave-tops at/above 0.70 box) — a decisive swing ending lower is currently invisible to SOS typing; **(3) ending location is a binary far-above-R split (markup), not a grade** — his model grades near-R / above-R / near-the-prior-high-after-a-shakeout as higher quality. Path per the standing measure-first rule: census the live SOS population's decisiveness first; any decisiveness PREcondition is an EC-48 measured change (flip counts, ruling); the ending-location quality enters, if at all, as a never-gated bonus with the raw value archived. | this row (the definition of record); census + proposals in the 2026-08-26 session; [strategy_alpha.md](strategy_alpha.md) §Sign of Strength flags the implemented-vs-recorded gap |
| 2026-08-26 | **SOS requires no breakout — the held high test IS the strength (function-sweep P2-7, ruled A).** The event reader types "SOS" on hold-quality alone (Phase D + wave-top near R + a genuine mini-consolidation) while computing but never consulting `breached` (peak > R + 0.5×ATR, the decisive-break test); its own docs and the operator-facing story claimed "break above R held". Measured on the 2026-08-26 live payload BEFORE asking: **96 SOS events, 58 never broke R decisively; 29 of 52 story-anchor (spine) SOS unbreached — 18 topped BELOW the rail entirely (up to 1.3 ATR under: EPD/XRN/TEN/BBT), 11 poked above R but inside the 0.5-ATR buffer (WOR/TRMD/PERI/OMC).** Options offered: **A** keep the read (wording fix only) / **B** require any poke above R (flips the 18) / **C** require the decisive break (flips all 29; what-if: 23 setups −1.4/−2.6 score via setup_quality — zero election flips, rankings move; 6 spines replaced by a later genuinely-breached SOS; RMBI +1.2 because its false-order SOS was breaking spring→SOS→LPS chronology). **Operator: "A."** The classifier is UNTOUCHED; the words were corrected everywhere they claimed a break (classifier docstring + comments + story trace line, strategy_alpha.md §Sign of Strength, structure_legend.md); wyckoff_canon.md §5 gains the SOS false-friend row. `breached` stays emitted per event as data. **B and C are now measured-and-declined levers — re-proposing a breach requirement costs a fresh A/B against this census.** | `engine_alpha/structure/box_events.py`; [strategy_alpha.md](strategy_alpha.md) §Sign of Strength; [wyckoff_canon.md](wyckoff_canon.md) §5; [structure_legend.md](structure_legend.md); [asks.md](asks.md) closed row |
| 2026-08-26 | **Pivot-collapse correction RULED IN (function-sweep P2-6) — the flip commit is the merge of `fix/pivots-collapse-absorb`.** Operator, on the measured evidence: *"sure go ahead."* The defect: `_collapse_swings`' sub-threshold branch popped the last COMMITTED swing and tried to absorb the small counter-pivot into `out[-2]` — an absorb that is arithmetically unreachable (the committed swing's ≥`min_amp` amplitude guarantees the counter-pivot can never be more extreme than the older anchor), so the branch's only real effect was deleting genuine swings, permanently when the counter-move was the last pivot in the window (the hard right edge, where the decision lives). The fix makes the code match the function's own documented contract: absorb the noise pivot, keep the committed extreme, let a later deeper same-type extreme extend through the merge branch. **Measured flip count, declared at this commit per EC-29/EC-48: same 32 tickers fire, ZERO election flips, ONE field moves — GF Score 96.4→96.2 (ranking slot 20→22); ratchet 28/33 PASS; doctrine PASS.** The four shadow-panel guards (all pinning `tests/baselines/shadow_baseline.json`) each failed on exactly that GF nudge and nothing else; the baseline is recaptured in this same merge commit with the drift stated. | `engine_alpha/structure/pivots.py` `_collapse_swings` + `tests/test_core_logic.py::test_collapse_swings_absorbs_never_deletes_a_committed_swing`; [asks.md](asks.md) closed row |
| 2026-08-25 | **The first story-chain candidate sheet is ALL JUNK — and the ruling teaches a new negative class.** All 40 rows, both chains, ruled junk in one stroke (an ACTIVE blanket ruling, not junk-by-default). Verbatim: *"all garbage, Most of these are Stocks are setups that consist of extremely tiny spread after a significant gap, that look nothing like regular Price action with almost no trading volume or price change for that matter these examples you chose are almost entirely stocks that are being bought out and maybe leaving the market soon I usually avoid stocks like these Because I haven't seen good trades coming out of things like these."* Two durable facts: **(1) Deal-pinned stocks are a categorical AVOID** — the signature is a significant gap followed by near-zero spread and near-zero volume (price pinned at/near an acquisition price; the stock may be leaving the market); the operator has never seen good trades come out of them. **(2) Tightness floors alone adversely select for exactly this class** — a "tightest range after the displacement" screen is a magnet for arbitraged tape, because a dead tape is the tightest structure in the market; a range/ATR ratio cannot tell quiet accumulation from a pinned deal price. The lesson lands on the candidate MINER (`tools/story_chain_candidates.py` needs a deal-pinned/aliveness exclusion before a round-2 sheet is cut), not on the engine — the engine's own read refused all 40; the crude screen surfaced them. Register rows 8–9 stay DESCRIBED; the specimens ask stays open awaiting round 2. | [story_chain_candidates_2026-08.md](story_chain_candidates_2026-08.md) Rulings received; [story_chain_program_2026-08.md](story_chain_program_2026-08.md) status log; [asks.md](asks.md) specimens row |
| 2026-08-25 | **The redeploy proved half the log fix and falsified the other half — the flood had a TWIN.** Post-redeploy measurement (the operator ran `update_dashboard.bat`; this row exists because the fix was measured in production rather than assumed): the stderr half is a complete success — **5 lines in the error log since restart**, two numpy RuntimeWarnings and three startup lines, ZERO `chrollo.request`. The error log finally means errors. But the 2026-08-24 row's claim that the fix *"removes ~99% of the volume"* was true only of the ERROR log: **uvicorn's own `uvicorn.access` logger is a twin of `chrollo.request`** — same event, less information (no request id, no duration), no quiet list, its own handler and `propagate=False` so neither the quiet list nor the stream split reaches it. The flood simply MOVED to stdout: **2,296 of the next 3,000 lines were uvicorn logging `GET /ibkr/status`**, and every real request was being written twice. Silenced to WARNING in `main.py` (it still reports problems; `chrollo.request` already logs every non-2xx at INFO and every exception with a traceback) — one access log, and it is ours. This is the same parallel-twin class the One-System council retired, hiding inside a dependency's default config. Guarded by `test_uvicorns_access_log_stays_silenced`, verified to fail when the line is removed. **Also corrected: the WAL prediction was WRONG.** I said the restart would truncate the 30 MB WAL; it did not — it survived at 30 MB against a 32 MB database. `wal_autocheckpoint` COPIES the WAL into the DB but does not shrink the file, so 30 MB is a permanent high-water mark of reused space, not a leak — checkpointing is demonstrably working (every nightly backup passes `integrity_check`). Left alone deliberately: `PRAGMA journal_size_limit` would reclaim it, but that changes the LIVE archive's connection setup for ~14 MB on no demonstrated harm. | `webapp/backend/main.py`, `tests/test_request_logging.py`; supersedes the volume claim in the 2026-08-24 row (which stands as written — append-only) |
| 2026-08-25 | **Two gates were dark, and one of them was guarding the least replaceable data in the repo.** Delegated (*"take the lead on this one"*); an engineering pass, not an operator ruling. **(1) `calibration_frames/` had NEVER been backed up.** The 2026-08-20 disposition added the frames leg to `tools/ChrolloBackup.ps1` and recorded it done — but the scheduled task runs a DEPLOYED copy at `%USERPROFILE%`, and the redeploy never ran, so the task kept executing a 2026-07-13 script with no mention of frames. It failed by ABSENCE: the leg logs either `calibration frames: N copied` OR `no calibration_frames directory yet, skipped`, and neither line appears in any run from 08-20 to 08-24 — every night logged `OK` while 110 frames (3.6 MB), each binding one of the 34 marks to the exact bars it was drawn on and **unre-derivable by design** (a live re-fetch is different bars), sat unprotected. Redeployed and VERIFIED by running it: `calibration frames: 110 copied`, all 110 present locally and on the OneDrive mirror, diffed against the repo with zero omissions. The root cause was a fix recorded without being run, so `deploy.md` §4 now lists the frames among the irreplaceable data and names the failure mode — an un-redeployed change is inert and silent — with a run-it-and-read-the-log verification step. **(2) CI had been RED on every push since 2026-08-12** (12 consecutive failures; the Windows job passed throughout, the Ubuntu job failed 2/1,685). Both were genuine Windows-vs-Linux defects, not flake. **`frame_digest` sealed the OPERATING SYSTEM**: `to_csv` defaults `lineterminator` to `os.linesep`, so the same frame hashed differently on CRLF and LF and the battery could never pass off-Windows. Pinned to LF; **the recapture is DECLARED here per EC-29** — exactly two `frame_digest` strings in `power_play_baseline.json`, zero length delta, every read baseline byte-identical (proved by diff). Corroboration that the DATA never differed: the LF digest computed on Windows (`7518f1fc…37f3c5` for FTNT) is byte-identical to the value the Ubuntu runner reported in the failure log. The operator's marks were never at risk — they bind through `frame_store.ohlcv_digest`, a separate function that always joined on an explicit newline (the same idea implemented twice; one author got it right). **The sealed-output guard was Windows-only**: `os.path.normcase` folds case on Windows and is the identity on POSIX, so `docs/Marks/…` was refused here and waved through on Ubuntu; now casefolded explicitly. Both fixes carry tests that fail on THIS machine — the first draft of the seal test passed with the fix removed (`os.path.realpath` canonicalizes the case of components that EXIST, masking the gap) and was rewritten against non-existent paths until it genuinely failed. **(3)** CI actions bumped `checkout@v4`/`setup-python@v5`/`setup-node@v4` → `@v7` (all three now native Node 24, off the forced-compat path); every input in use verified present at v7 before the bump. **Flagged, NOT changed:** the Ubuntu job tests on Python 3.12 while this machine and the service run 3.14 — a second portability surface, and a bigger call than this pass. | `tools/ChrolloBackup.ps1` (redeployed to `%USERPROFILE%`), `tools/power_play_fixture.py`, `tools/_bootstrap.py`, `tests/baselines/power_play_baseline.json` (EC-29 recapture), `.github/workflows/quality.yml`, [deploy.md](deploy.md) §4 |
| 2026-08-24 | **The service log flood had a CAUSE, not just a missing rotation cap — found and fixed.** The 2026-08-20 disposition deleted 244.7 MB of logs and got the stderr half right (`logging` writes every level to stderr, so `chrollo-service-error.log` is HTTP request lines, not errors), but left the remedy as *"the `AppRotate*` block still needs an elevated run or it recurs"*. It recurred — 167 MB in four days. Rotation was never the remedy, because the volume itself was a defect: **`/ibkr/status` was missing from the request middleware's quiet list**, while `useIBKRStatus` polls it every 3s whenever the broker is DISCONNECTED — the permanent steady state, since boot stays broker-free by house rule. Measured: **16,698 of the last 16,850 lines (99.1%)** were that one path; the 4 tracebacks buried in there were all `KeyboardInterrupt` at service restart, so nothing real was hiding. Landed together: the path joins `_NOISY_PATH_PREFIXES`, and the streams split (INFO and below → stdout, WARNING and above → stderr) so the *error* log finally means errors. The middleware had ZERO test coverage, which is how a one-tuple omission survived four months — `tests/test_request_logging.py` now pins both contracts (a successful poll is DEBUG; a failed one stays visible). No convention row was added: per this file's own *"prefer a machine-checked invariant to a paragraph"*, the test IS the rule. The elevated `AppRotate` block is still worth running as a backstop, but it is no longer load-bearing. **Not an operator ruling — an engineering fix from a maintenance pass**, recorded here because it makes the 2026-08-20 row's remedy incomplete. Same pass, same change: the README paragraph still describing the retired `setupScoreMath.js` as a live remnant corrected to the post-retirement truth, and the pointer-audit ADVISORY split so `docs/archive/` carriers stop burying the live hits (108 → 29 listed, with the suppressed count always printed — the link gate itself still covers archived docs unchanged). **Dependency hygiene, same pass:** `npm audit` went 5 vulnerabilities → **0**, moving ONLY `package-lock.json` (package.json ranges untouched) — react-router-dom 7.18.0→7.18.2 clears two HIGH advisories (GHSA-qwww-vcr4-c8h2, RSC-mode CSRF), react 19.2.5→19.2.8 and vite 6.4.2→6.4.3 are patch, and `npm audit fix` cleared five build-time-only transitives (@babel/core, brace-expansion, js-yaml, nanoid, postcss — they run at build, never in the browser). **recharts (3.8.1) and lightweight-charts (5.1.0) were deliberately LEFT** though minor upgrades exist: they draw the charts, a minor release can move rendering, and an agent cannot verify that — booting the backend is forbidden by house rule, so chart fidelity is only checkable by the operator's eye after a redeploy. Verified: eslint 0, node 290/290, `vite build` clean. | `webapp/backend/middleware/request_id.py`, `webapp/backend/main.py`, `tools/pointer_audit.py`, `tests/test_request_logging.py`, `webapp/frontend/package-lock.json`; [asks.md](asks.md) redeploy row |
| 2026-08-23 | **The Story-Chain discovery — six rulings from the scoping conversation, landed same-session (EC-50).** The operator's push, verbatim: *"i want to develop the ability to find Disjointed setups as well"* — the chart read as a chain of structures instead of one box that refuses whatever it cannot contain. Ruled in the discovery: **(1) Types are filterable lanes graded per-type** — *"categorize them into types and introduce a way to filter them and we will grade each type separately, and we won't introduce much new stuff since we are essentially looking for the same high quality traits in all setups perhaps just in a different order and with some setups some traits aren't present and that's fine."* **(2) The inner mini-consolidation and the ceiling shelf are ONE event** — *"its still the same mechanism and it is to be treated the same, no need to give it a new name just acknowledge its position which if the mini consolidation is found at the top that's a slightly higher quality"* — the dark `resistance_contraction` form and the inner-box read fold into one mini-consolidation event carrying a graded POSITION attribute; the WCC wider-box catch must survive the fold (election authority stays geometric). **(3) Shakeout depth never disqualifies** — *"a violent shake out maybe the spring and its our key to mapping out the 'Phase C' of that Setup. what happens next though is what's truly important, how does it recover? if at all (Sharply in one swing?, Stair case like with a couple of tests in between until return? a new base all together?, did it return to the original Consolidation Trading range?, is it sitting a little below or stayed at the bottom?)"* — the recovery character and destination adjudicate, then the contraction hunt (SOS, LPS, contracting lower pullbacks, mini-consolidation). **(4) The tactical-long variant is valid** — the post-shakeout pullback *"can sit lower, above the shakeout's low but below the old floor. that's a strong tactical Long Position opportunity in case the price action does recover into the base and even breach it later."* **(5) Last Supper = the final-shakeout class** — *"usually after price action breach of the base and a steep deep correction"*; separated from a valid chain by what forms (or does not form) afterwards. **(6) Dead space sharpened** — originally about Root-Swing selection bias: mark the pair of limbs whose rails wrap the zigzag most tightly so the boundaries show the real trading range where buyers and sellers touched, stopped and pivoted; a rail-SELECTION rule, never an argument for or against disjointed reads. The 10-seat planning council ran same-day (64 recommendations → 15 tasks, all chair-verified against code); the theory halves land in `strategy_alpha.md` with the program's opening task, and the TYPE NAMES await the operator's ruling before anything serializes. | [pattern_register.md](pattern_register.md) rows 8–9 (row 1 superseded into row 9); [asks.md](asks.md) (names / specimens / go-ahead rows); council record `.council/council-plan-output/2026-08-23-1130/` distills into the committed program doc when the build opens |
| 2026-08-23 | **The “One System” council executed — every parallel version is ruled and gone, on the operator's delegation.** Operator, verbatim (2026-08-22): *“Listen I need a definitive answer, remember the rule that we aren't adding more 'Versions' of the code that try to solve the same thing… I want you to Council Review + Research on the whole damn thing”*, then *“Take the lead on this one, if you think its good then push and flip.”* Ten seats, 85 raw findings → 15 shipped (all chair-verified against code). Executed under the delegation: **(1) the legacy score path RETIRED** (`e53363a`, the declared manifest seam) — the unlocking P1 was that the rollback the path was kept for had silently broken (the frozen JS cap mirror fired the demoted RS/Uptrend chips on every legacy-epoch row); the ranking now IS the grade (three seats independently; shadow guard proved ZERO drift at the seam — the repoint landed while the two orderings still agree exactly), the seed scan-back election keys on the same basis (hermetic recall held at 66.2% baseline, no winners lost), the lens LPS grade is serialized engine-side (EC-28), and the marks ratchet held 28/33 throughout. **(2) The correction lane ran end-to-end on `first_legal_look`** (row below). **(3) The intake architecture landed**: `docs/pattern_register.md` + `docs/asks.md` + conventions EC-48/EC-49/EC-50 + AP-11/AP-12 (all operator-delegated). **(4) The graveyard cleared**: `engine/ta-score-v2`, `wip/audit-tool-tweaks`, `wip/pivot-canon-2026-07` (payload verified verbatim on main — the old PARKED-UNMERGED memory claim was false), `worktree-council-p2-followups` (its three genuinely-stranded fixes re-landed first, `a2650f3`) all tagged `retired/*` and deleted; the stash (verbatim on main) dropped; 7 worktree husks removed; `wip/signal-edge-backtest` KEPT pending the operator's live-or-die (register row 6). Deliberate deviation from one finding: no “DECIDE NOW” heads inside the flag ledger — `asks.md` is the one fronting queue, and a second synchronized copy would be a fresh twin. | council record [archive/council_onesystem_2026-08-22/FINAL-REVIEW.md](archive/council_onesystem_2026-08-22/FINAL-REVIEW.md) (all 10 seat files + the brief + the execution reports archived beside it); commits `9697763..805bbc9`; `conventions.md` EC-48..50/AP-11..12; [pattern_register.md](pattern_register.md), [asks.md](asks.md) |
| 2026-08-23 | **Evidence re-derivation — the `first_legal_look` correction is MERGED and the clock-8 ruling STANDS on re-derived evidence.** The reserved-edge bias fix (council 2026-08-20 finding 3; oracle-validated) merged as `2b03d0b` under the operator's delegation, through the EC-48 Correction-lane shape: fix + census re-run + record true-up in one landing. Re-derived at clocks 8/10 on the corrected arithmetic (15/20 proven untouched; sidecar `output/power_play_census_postfix_2026-08-22.json`, cache through 2026-08-21): **clock 8's elected cohort n=85, median fwd_20 +3.8%, 58% winners — still the ONLY forward-positive cohort (clock 10: −1.7%, 46%)** — the 2026-08-18 ruling's own acceptance condition holds and sharpens (pre-fix basis +2.8%/54%/n=79; three newer cache sessions ride the re-run, stated honestly). Live register verified untouched pre-merge: NNBR/QTTB/BRKR byte-identical, MAN/FTNT immune at every clock. Also corrected on the record: the earlier “MGNX/QLYS/STRZ filed rows change state” claim was an agent recomputation over the cache, not the register — those names have zero archive rows ever (operator caught it 2026-08-22). | merge `2b03d0b`; [power_play_program_2026-08.md](power_play_program_2026-08.md) §the correction landed; `tests/test_first_legal_look.py` |
| 2026-08-20 | **The Trigger re-pin branch is RETIRED on measurement, and the seal gap it aimed at stays OPEN until the next graduation.** Branch `claude/optimistic-grothendieck-19e20e` carried a widened EC-9 fingerprint recipe (`_mark_dict` + `trigger_date`/`trigger_price`) and re-pinned `OPERATOR_APPROVED_FINGERPRINT` `b671e056…` → `c0c5c88e…`, its commit message claiming operator re-approval that no record here corroborates. **Settled by recomputing both recipes against the live marks DB rather than by argument: it now holds 34 marks (not the 33 at sign-off), all 34 carrying a Trigger; the OLD recipe yields `3cee17e0…` (≠ the `b671e056…` pin) and the branch's NEW recipe yields `a3398865…` (≠ its own `c0c5c88e…` pin).** Both fingerprints moved with the corpus (floor reseal 26→28, the story-pool conversions, a 34th mark), so the branch's pin was computed against July's DB and matches nothing today — merging it would install a pin that refuses on sight. Branch deleted; 150 commits behind main and unrebasable onto a corpus that has moved. **The DEFECT it names is real and remains: a Trigger edited after sign-off still slides through the seal unchanged**, because the recipe covers geometry and not the buy — main keeps that as its documented KNOWN COVERAGE GAP. It can only be closed **inside the next graduation event**, since widening the recipe rotates every fingerprint and the replacement pin must be approved against the DB as it stands that day. Note the export is ALREADY sealed shut (old recipe ≠ old pin) because the marks drifted past their sign-off — that is the design working, not a defect. **Standing lesson: never let a re-pin ride in on a fix commit.** In the same disposition the operator cleared the audit's four gated items: service logs **deleted** (244.7 MB — and they were not errors: `logging` writes to stderr, so `chrollo-service-error.log` was 165 MB of HTTP request lines; the `AppRotate*` block still needs an elevated run or it recurs), 12 journal snapshots **moved** off the repo (126.8 MB → `%USERPROFILE%\ChrolloBackups\journal-snapshots\`, with `trading_journal.pre_scan_date_heal_2026-08-11.db` deliberately LEFT because the append-only 2026-08-11 row cites it), `calibration_frames/` **added to the nightly backup** (the audit's most fragile finding — irreplaceable, unbacked-up; live only after the script is redeployed), and the `trend_terminal_2026-08-13` sheet **still held** for his eyeball (*"I will look at these later"*, kill-by 2026-08-31 unchanged). | branch retired (no merge); `tools/guided_list_export.py` `OPERATOR_APPROVED_FINGERPRINT` unchanged at `b671e056…`; `tools/ChrolloBackup.ps1` frames leg; **evidence: [repo_bloat_sweep_2026-08-20.md](archive/repo_bloat_sweep_2026-08-20.md)** |
| 2026-08-20 | **Repo maintenance sweep — the chart pictures left behind by closed tracks are cleared, and the pointers that would have dangled were repointed instead of broken.** The repo carried **17.9 MB of chart images, 59% of everything tracked**, because commit `44f8293` (2026-07-18) deleted 17 closed-track probe tools off the operator's signed kill list but left every one of their output folders standing. **Seven orphaned picture folders deleted here (7.39 MB):** `substrate_ab`, `box_start_extension`, `pip`, `l2`, `charts`, `ab`, `band_rails_ab` — each one's producing tool already gone, no citation anywhere in the tree, and its finding already written into prose. Resurrection stays one git command away, which is `44f8293`'s own disposal policy. **The 2026-08-04 DO-NOT-COMMIT ruling on eyeball renders is now portable:** it lived only in `.git/info/exclude`, which is machine-local — it never survived a clone and never protected CI, which is why new renders kept landing untracked AND unignored. Folded into `.gitignore` as `tools/fidelity/**/*.png`, carrying the ruling's words. `full_package` and `ar_first_reaction_2026-08-13` PNGs untracked (both regenerable by committed tools; the AR renders' purpose was fully discharged by the 2026-08-14 DO-NOT-FLIP ruling, whose verdicts survive in the sealed corpus). **`trend_terminal_2026-08-13` renders deliberately NOT untracked** — that sheet is the CURRENT eyeball for a flag whose kill-by is 2026-08-31, and the eyeball has not happened yet. **Four spent instruments archived rather than deleted, each with its citation repointed**, so the 2026-07-18 pattern (three dangling evidence pointers) is not repeated: `heal_scan_dates_2026_08` (the heal completed 2026-08-12 — and note it is cohort-grain by construction, so it is the WRONG instrument for the deferred June forming-bar era, which needs a new row-grain pass), `anchor_bar_study`, `cluster_rail_validation`, `bar_dwell_ab`. **`specs/` (13 finished implementation specs) archived under `docs/archive/specs/`; the top-level directory is gone.** Six scaffolding docs deleted, nine decision/measurement docs archived. **Nothing in the reading moved** — no engine file, no knob, no threshold. | `.gitignore`; `docs/archive/`; **evidence: [repo_bloat_sweep_2026-08-20.md](archive/repo_bloat_sweep_2026-08-20.md)** |
| 2026-08-19 | **The operator RULED both conversions real — and the flip was still REFUSED, because converting them was not all it did.** Asked to eyeball the two setups the species story form converts, he ruled: **"BOTH have setups on these days so yeah"** (EGBN 2026-01-07, PKE 2026-02-18). That ruling STANDS and is the acceptance evidence for those two names. But flipping `POWER_PLAY_STORY_FORM_ENABLED` — the half that reaches the PAYING read — does more than convert them, and the guards caught it: **`tools.shadow_diff` reports canonical drift on WCC — the species form re-elects it into a box 2.2× WIDER (`Box Width` 0.0608 → 0.1346, rails R 368.90 → 374.00 / S 347.77 → 329.62, `Base Len` 21 → 16), costing it `Score` 110.8 → 96.9, `Tier` **S → A**, and 16 places of rank (3rd → 19th)**. A wider, looser framing winning election is the prime directive inverted. Two further consequences, neither ruled: **three already-pinned hits fire EARLIER** (NKTR 2026-04-09 → 2026-03-27, MS 05-18 → 05-15, YPF 05-06 → 05-05), and the flip **violates the stated design invariant** that the paying read never consults the species form (`settings.py`: *"a passenger never touches the paying read"*; pinned by `test_paying_read_never_consults_the_species_form`, which the flip breaks by name). Twelve guards failed in total. **Verdict: the flag stays DARK and the reseal was rolled back** (baseline restored byte-identical, `STAGE_TAGS` restored, ratchet back to 28/33 PASS, shadow guard back to no-drift). **This is NOT a Tested-DEAD row for the resistance contraction** — the form is sound where it lives, inside the species twin's own scoped election, and it demonstrably reads EGBN and PKE the way the operator does. What is refused is *graduating it into the paying read by flag flip*: that is a re-election change to live picks and needs its own program with a real A/B over the firing cohort, not a same-day toggle. The operator's EGBN/PKE ruling is banked as that program's first acceptance evidence. **Method note worth keeping: the Guided-List ratchet alone said PASS at 30/33 — it was the SHADOW-OUTPUT guard that found WCC.** A ratchet measures the marked names; it cannot see a name that was never marked. Both gates, always | `settings.POWER_PLAY_STORY_FORM_ENABLED` (unchanged, dark); [flag_ledger.md](flag_ledger.md) story-form row; `tools.shadow_diff --check`; `tools.marks_corpus --check` |
| 2026-08-19 | **The Phase-A climax repair is re-keyed off the CAUSE segment, and the species lane goes LIVE at the preset half only.** Operator: *"sure lets take the fix, flip the power play thing, as long is everything works like I asked and is working by the design that i set out to work (Calibrated List, Things I listed as reading correctly, Strategy documentaion...) I believe we havent regressed so its safe to push these things"* — the condition, not the instruction, drove what shipped. **(1) The re-key.** `_enforce_climax_terminality` chose its polarity from `root.kind`, the keying **already Tested-DEAD 2026-07-27** ("the root is only a scan origin, not the box's cause"), whose row prescribes the remedy taken here: read the direction from the confirmed segment covering the box open (`trend_terminal_floor.direction`, cause-wins). Diagnosed by a 14-agent fan-out with adversarial refutation ([`climax_anchor_diagnosis_2026-08-19.md`](climax_anchor_diagnosis_2026-08-19.md)): the drawn climax is **not a chart measurement** but the extreme of a fixed 60-trading-day window ending at the box open, so `dCx = A + B` with `A ∈ [−60, 0]` (381/381 fleet structures, min exactly −60) and `B` = box placement — **`A` pinned at −60 on 5 of 6 marked names, so the split was always `B`**. The seed label contradicted the drawn pair 4/4 live; on **CNI** the raw resolver had returned the operator's exact pair (07-17 → 07-21) and the mis-polarised branch overwrote it. **Measured after: CNI resolves to his date exactly; BCPC −82 → −39; earlier-than-his-end 5 of 6 → 3 of 6, median −36 → −9. HTH REGRESSES −6 → +54** (its former closeness was two large errors cancelling, never agreement) — recorded, not hidden. Guided-List ratchet **28/33 held, same fingerprint**. **(2) The flip, and the deviation.** The ledger required both species flags to "flip together or not at all". They do not: with `POWER_PLAY_STORY_FORM_ENABLED` ON the sealed ratchet **BREAKS** — **EGBN and PKE, both recorded expected-misses of stage `rail-placement`, now FIRE** (tier A / tier B). Good news is still a break, and re-freezing the operator's sealed standard is his ruling, not an agent's. **Shipped: `POWER_PLAY_PRESET_ENABLED` = True alone** (ratchet holds; the lane is fully functional on it, since the twin enables the contraction form inside its own scoped override — the module flag only reaches the PAYING read). **`POWER_PLAY_STORY_FORM_ENABLED` stays dark pending the operator's EGBN/PKE ruling.** Also corrected: the ScanTimer cost bound the ledger names as the flip gate **cannot be measured while the lane is dark** — the cost instrument only reports with the preset on; the bound arrives with the first live nightly scan. ⚠ Recorded honestly: **MAN's "exact pair at clock 10" (2026-08-14 row below) could NOT be reproduced** — `read_structure` returns None for MAN under the supported override; do not cite it un-re-derived | `bricks._cause_is_up` + `_enforce_climax_terminality`; `settings.POWER_PLAY_PRESET_ENABLED`; [`climax_anchor_diagnosis_2026-08-19.md`](climax_anchor_diagnosis_2026-08-19.md); [strategy_alpha.md](strategy_alpha.md) "Climax terminality"; [engine_reference.md](engine_reference.md) "Polarity re-keyed"; `tests/test_bricks.py` polarity + fallback pins |
| 2026-08-18 | **The species breakout wall is RULED: the DEPARTURE form** — an episode counts resolved only when a close clears the pole peak by **1×ATR10 of the ten bars BEFORE the crossing** (the base's own volatility; an explosive breakout bar never raises its own wall); a close hugging the peak is base-building, not a resolution. Operator: *"Lets go with your recommendation"*, on this evidence: the S1 rulings (the departure form keeps all four named-anchor episodes — MAN, FTNT, HELE ×2 — unresolved at their looks and files all four junks; the old close-above-peak wall kept 0/6), and the full-cache clock-8 A/B under the landed arithmetic (same-cache legs, tripwire OK, `output/power_play_census_wall.json` vs `_legacywall.json`): **MAN's breakout re-dates to 2026-08-13 — the operator's own date, to the day — and MAN ELECTS**; 855 of 2,207 filings regain their looks and land honestly (464 refused by the baseline filters, 359 watched-not-elected, 22 elect an older base, 10 newly elect — the newly-elected cohort at fwd_20 median **+2.6%, 67% winners**); the whole elected cohort holds quality while catching more (88 @ +2.7%/56% vs 79 @ +2.8%/54%). Landed as `POWER_PLAY_BREAKOUT_DEPARTURE_ATR = 1.0` (species lane + instruments only — `ticker_episodes` has no paying consumer; the 0.0 legacy branch is retained byte-identically); the ratchet baseline recaptured in the same declared change — **the new frozen fact: MAN @ 2026-08-12, the day before his breakout, is `admitted_dark` at clock 8** (his exact climax/AR dates, 19-bar span, 1/19 lower-third bars): the dark lane now reads the program's specimen end-to-end. Instrument defects caught and fixed in the same session: the first A/B was a silent no-op (the census `--override` lever wrapped elections while the wall is consumed at SCREEN time — run overrides now scope the whole read, pinned), and the first landed yardstick included the crossing bar's own range (self-defeating on gaps — corrected to the shifted window, pinned by the knife-edge batteries). Both species flags REMAIN DARK; open = the ScanTimer cost bound at clock 8 → the operator's both-flags flip. | `settings.POWER_PLAY_BREAKOUT_DEPARTURE_ATR`; `engine_alpha/structure/power_play.py` `ticker_episodes`; baseline `tests/baselines/power_play_baseline.json` (recaptured); sealed verdicts + program doc "The breakout-wall calibration" |
| 2026-08-18 | **The species reading clock is RULED: 8 trading days** — species lane only, everything else unchanged. Operator, verbatim: *"sure I guess we can start measuring from 8 days, But other then that everything stays the same no?"* — confirmed: the preset moves exactly the two window keys inside the species lane's scoped override; the paying scan, the 5-bar edge reserve, and every other law stay as they are, and BOTH species flags stay dark until the ScanTimer cost bound at clock 8 + his flip. Evidence: the species census (48-min re-run 2026-08-18, tripwire OK — clock 8's elected cohort the ONLY forward-positive: median fwd_20 +2.8%, 54% winners, n=79, vs negative at 10/15/20) + his 40 sealed sheet rulings (S2 8/10 keep on the 8-day marginal catch; S1 6/10 keep incl. all four named anchors). In the same session he approved the **species-scoped occupancy/traversal relaxation as the next MEASURED experiment** (*"I guess we can do that, the main point of these is that we understand and read correctly, its less about the raw numbers and more about the shape of things"* — the ABVX autopsy showed the refusal wall is `EQ_MIN_HALF_DWELL`/`EQ_MIN_COVERAGE`/`EQ_MIN_TOUCHES_PER_RAIL` + the traversal floors, the same wall that killed MAN; measure-only, no floor moves live), and **declined the edge-reserve sweep** (*"I don't see why we should?"* — the reserve stays 5). The value change rotates the manifest and the ratchet baseline is recaptured in this same declared change (EC-29; the frozen MAN fact moves BY DESIGN: at clock 8 the species read watches MAN pre-breakout). | `settings.POWER_PLAY_WINDOWS`; ledger rows `POWER_PLAY_PRESET_ENABLED`/`POWER_PLAY_STORY_FORM_ENABLED`; sealed verdicts `docs/power_play_verdicts_2026-08-18.json`; baseline `tests/baselines/power_play_baseline.json` (recaptured) |
| 2026-08-18 | **An admission record names the BEHAVIOR it measured — "holding shelf" is retired as invented jargon.** Operator, verbatim: *"Instead of 'Holding Shelf' we should specify, is it price action that contracts above Resistance after breaking out? then say that, is it price action that contracts on support after it recovered post shake out? we can't just throw around the word shelf around when the behaviour already has a name."* The species story form is renamed the **resistance contraction**; its admitting sentence derives from the measured terminal posture — `contracting above resistance` (last bar closes above the rail — the post-breakout stance) or `contracting at resistance` (engaged at the rail's zone, close below it) — via the ONE naming implementation (`event_map.resistance_contraction_label`). The support-side sibling (contracting ON support after a shakeout recovery) is the **LPS** and keeps its own name and path. NOTE the untangled collision: the LPS layer's own live July vocabulary (`LPS_HOLDING_SHELF_ENABLED`, swing types `holding_shelf`/`buec_shelf`/`rising_support_shelf` — manifest-listed, archived) had already taken the same words for a different thing; renaming THOSE is a declared migration awaiting its own ruling, not part of this change. | `event_map.resistance_contraction_admission` + `resistance_contraction_label`; `strategy_alpha.md` "The rail-episode read"; battery `tests/test_resistance_contraction.py` |
| 2026-08-14 | **Power Plays are WANTED setups — "Acceptable Misses" is re-ruled.** Operator: *"Power Plays are a wanted setups, amend acceptable misses"*; the species carries Minervini's own term and his examples are MAN, FTNT (*"a powerplay that the engine finds today! and we should model after it"*), MRVL and ARM; in the same message he asked to *"incorporate more of William O'Neil's work too into our engine as principle, same with Minervini."* Four-specimen diagnosis (same day, all read-only): **FTNT fires** (strict pool, tier S) because its root cleared the reading clock — AR 2026-07-09 was exactly 20 bars old on the edge-trimmed frame at fire — and its 26-bar shelf worked BOTH rails (4R/6S touches, spring undercut 0.98 ATR on 07-29 with volume z **3.4**, LPS, trigger 165.75); it is the model specimen. **MAN read NOTHING**: a fresh root becomes walkable only **25 bars after its AR** (`MIN_BASE_DAYS` enforced at anchor seeding AND again by `validate_equilibrium`'s `len(eq_df)` guard, plus `STRUCTURE_EDGE_SKIP_BARS` = 5), and his whole setup — climax 07-16/17 off a +109% leg, AR low 07-24, 20-bar upper-half shelf, LPS 08-10..12, breakout 08-13 — lived and died inside that window (first legal look ≈ 08-28, eleven sessions post-breakout). Counterfactually seeded (clock 10, in-process probe), the engine finds **his exact pair — climax 2026-07-17 → AR 2026-07-24, to the day** — and the box then dies at occupancy (`lower_dwell 0.00<0.15`, `coverage 0.5<0.8`, `r_touches 1<3`: a drift-up shelf's virtue IS its crime under worked-equilibrium floors) and at story admission (*"ruled form not read: S+ R^"* — the ruled form is S-test-hungry, and `R^` requires a close above R, only true post-breakout). Seeding refusals are invisible to BOTH the election trace and the near-miss lane (it attaches at the pair-election seam). **MRVL and ARM never reach chart reading at all**: refused at the `sma50` baseline leg mid-correction (close −7.3% / −11.2% under the 50-day, with YoY +180% / +97%) — and behind that gate their geometry is genuinely unfinished (−50.6% / −51.5% corrections off the June peaks; newest roots die at respect/width), so the engine and Minervini agree the entry is not NOW; the difference is the engine is not even watching. **Nothing flips in this change** — the species enters as an open measure-first program: census over the seeding clock (the species' shelf-duration distribution vs the 25-bar clock is THE question — FTNT's shelf lasted long enough, MAN's resolved on bar 20), operator marks on clean Power-Play charts, species-scoped admission only after measurement. Graveyard checked: **no Tested-DEAD row covers the maturity clock or a second reading lane**; rulings that BIND any build: LIVN "way too young" at 13 bars (2026-07-27), BBVA 2×`MIN_BASE_DAYS` rescue floors, occupancy floors sealed where junk begins (2026-07-25), overshoot-magnitude DEAD ×3. **Recorded as direction, unimplemented:** O'Neil + Minervini enter as principle sources alongside Wyckoff; the dark `FUNDAMENTALS_ENABLED` layer (earnings history + filing-lag discipline + in-house RS rating + RS line — built, 0/262 payload rows populated) is the natural landing zone. | `engine_reference.md` "Acceptable Misses" (re-ruled in the same change); no flag — no behavior moved |
| 2026-08-14 | **`AR_FIRST_REACTION_ENABLED` stays DARK — its span is right and its anchor is wrong.** The operator read the twelve renders and wrote a verdict on nine, supplying dated trend-end/AR marks with them. Measured against those marks on the six names where both reads compute, **flag-OFF is closer on four of six: |off| = 133 bars total vs |on| = 266** (median 18.5 vs 48) — and this is the cohort deliberately selected to be where the flag moves MOST (the twelve renders are the twelve largest tighteners in the universe), so a cohort chosen to flatter the proposal still loses. **The mechanism is the ruling, not the arithmetic.** The raw fallback pins `ar_bar` to `box.start_bar` — measured exactly equal on 9 of 9 firing names — and the operator's AR **is** the box open by his own repeated definition (*"AR at 17/06 which also serves as the root swing for the consolidation"*, stated on IRMD, IART and CYRX). So flag-OFF satisfies his identity BY CONSTRUCTION: it is right for a reason that has nothing to do with reading a reaction. The retarget breaks that identity by re-attaching the AR to a short reaction off `climax_bar` — the correct Wyckoff move, and the right answer if `climax_bar` were the trend end. It is not: the engine's climax is EARLIER than his trend end on **6 of 6** measurable names, never once later, median −50.5 bars. The retarget's SHAPE is vindicated (spans 1–7 bars against his 1–5; on HTH its AR lands on his trend end to the bar, which is precisely what he said — *"the green circle is correct but not for the AR"*). Flipping would trade an accidentally-correct drawing for a principled incorrect one. **The blocking decision moves from the eyeball to the CLIMAX ANCHOR**; kill-by 2026-09-15 unchanged, and if the anchor is still unaddressed then, delete the flag. **Recorded as theory, unimplemented:** the operator marks a transition ZONE, not two events — *"the point of this is finding the Last Trending Before the Consolidation starts and that's why they go hand in hand"* — the last trend peak and the low 1–5 bars later where the base opens, the AR low being the root swing being the consolidation start. | flag `AR_FIRST_REACTION_ENABLED`; **evidence: [anchor_marks_ruling_2026-08-14.md](anchor_marks_ruling_2026-08-14.md)**, marks [trend_end_marks_2026-08.json](trend_end_marks_2026-08.json); [flag_ledger.md](flag_ledger.md) |
| 2026-08-14 | **The 2026-07-28 DO-NOT-FLIP ruling on `TREND_TERMINAL_BOX_GATE_ENABLED` is corroborated — and the box-blind defect splits in two.** That ruling (the gate is *"not wrong, early"*; `segment_trends` is box-blind) survived only in an agent session memory with no row in this repo, which is how a later session came to tell the operator his gate was still unruled. He re-marked IRMD by hand, independently, three weeks later and put the trend end at **2026-06-10** — the same date the memory recorded. This row exists so that cannot happen again; it is dated when the corroborating evidence landed, not backdated to the ruling. **The defect is two defects, and only one is a trend-reader problem.** (a) *The pivot isn't found:* `segment_trends` holds a terminal within 3 bars of his trend end on only **2 of 9** marks — on IRMD/IART/CYRX/CATO/CNI/EC/HTH the peak he marks is not in the pivot set at all, because `terminal_bar` is a segment's extreme pivot and a later upthrust to the box's own R is a higher high, so it swallows the true trend end and drags the terminal into the base. (b) *The pivot IS found and Phase A cannot reach it:* on BCPC `segment_trends` holds his EXACT pair (2026-02-17 up, 2026-02-20 down) and on LZB his trend end to the bar (2026-06-17), and Phase A anchors months earlier regardless — the elected box opens BEFORE his trend end and the chronological invariant `ar_bar <= box.start_bar` forbids Phase A from reaching forward into it. No work on the trend reader fixes (b); there the BOX is the binding constraint. ⚠ **Nine marks is a direction of travel, not an acceptance set** — the operator explicitly declined to certify them (*"there isn't one concrete answer Im 100% sure is correct, their just very ugly structures (most of them) so i cant be sure"*), and his own consolation stands: *"on most of these the engine knows how to read the consolidation starting formation box correctly."* Do not seal a gate against that file; the 15–20 labels the unblock asks for are still owed, and marks on CLEAN structures are worth more than more marks on this cohort. | flag `TREND_TERMINAL_BOX_GATE_ENABLED`; **evidence: [anchor_marks_ruling_2026-08-14.md](anchor_marks_ruling_2026-08-14.md)**, marks [trend_end_marks_2026-08.json](trend_end_marks_2026-08.json); [flag_ledger.md](flag_ledger.md) |
| 2026-08-13 | **An LPS's zone class is read against the rails of the range that OWNS it — the gate was comparing it to a rail the detector never saw.** The doctrine gate went red on TTC (`D5 lps-above-R`: low 97.02, R 101.08). Not a classification bug and not an election bug — a **gate** bug, and the read is correct: TTC's LPS is elected on the inner tightening range (iR 96.90, iS 91.49, opening bar 483 inside a parent box opening at 363), so `detect_lps` typed it `OVERSHOOT_R` against the INNER resistance it genuinely broke out over and backed up onto, while `Structure.R/S` always publish the PARENT's rails (B2/B3/B6 assert exactly that pairing). D5 read `s.R` and so compared an inner-measured class to a parent rail. **Latent since the gate was authored 2026-07-20** — it needed an inner box sitting *entirely below* the parent's R, the case the gate's own header did not contemplate ("inner boxes above parent R"), which nesting-is-temporal has always permitted. Fleet census at the fix (271 payload setups): every zone class is coherent against its **active** rails — 48 `OVERSHOOT_R`, 12 `UNDERCUT_S`, 211 `INSIDE`, **0 violations**; against the parent rail, 2 break (TTC, HUBB — the only inner-elected above-R reads whose inner R sits below the parent's). **Engine untouched**: no flag, no threshold, no archive seam, no scoring change — the repair is in the gate and the docs. D5 also re-keys from the `buec_shelf` swing form to the `OVERSHOOT_R` **zone class**, which is what doctrine names "LPS above R"; the four other swing forms outrank `buec_shelf` in the label cascade, so keying on the form left 37 of the 48 above-R reads unasserted (HUBB among them) — coverage 11 → 48, still 0 violations. **Measured but NOT added** (available if wanted): the two mirror invariants against active rails, `UNDERCUT_S ⇒ low < S` (12/12 clean) and `INSIDE ⇒ S ≤ low ≤ R` (211/211 clean). | `tools/doctrine_audit.py` D5 + module header; `strategy_alpha.md` "Which R is 'the old resistance'?"; `engine_reference.md` Phase 3 gate 6 |
| 2026-08-12 | **Consolidation is ONE grade, and Phase C is a MARK, not a grade — the partition moved, the number did not.** Operator, on the fused panel he had just approved: *"I think I just want to fuse 'Phase B' grading into 'Cause' and call it 'Consolidation Grade', since a two-sided zigzag price action can be folded into one of the quality traits we look for in a consolidation as a whole. Phase C also shouldn't be graded because there is no telling whether a setup that has one will win or not — a setup is more complete if a clear Phase C occurs and then recovers nicely into a Phase D, but that's about it. It's more important for the engine to FIND Phase C (spring) or the 'V' tip structure just to put a mark on where the right-most side of the consolidation is, to understand the order of how the setup played out. Same with shakeouts."* **`CHAPTER_ORDER` is now `(consolidation, phase_d, trend)`.** Two mechanical moves carry it, and neither touches the grade's arithmetic — same terms, same caps, same fixed divisor 171, byte-identical `ta_grade` and tier: (1) every `cause` + `phase_b` term re-chapters to `consolidation`; (2) `ascending_support` **moves to `consolidation` rather than being demoted** — it measures the whole base's valley lows stair-stepping up (`measure_support_slope` over the box), never the shakeout, and it sat under `phase_c` only because that chapter's blurb said "the rising support". It was the ONLY reason a Phase C column ever showed points. Its 8-point cap is deliberately untouched: demoting a term is an edge-read decision (the `rs`/`uptrend` precedent, 1977 episodes), and no evidence was offered against a rising floor — only against grading the shakeout EVENT. **`spring` left the `ta` layer for a new third layer, `marker`**: measured, archived (`score_spring`) and drawn, excluded from `ta_layer_terms()`, so it cannot reach a chapter, the divisor or the tier even if `SCORE_SPRING` were raised. That closes the "shape-only until the A/B" door the 2026-08-09 flip left open for this term — there is no A/B coming for it — and turns the ruling into a machine-checked invariant rather than a paragraph (`test_a_marker_event_is_found_and_never_graded`). The completeness a clean turn genuinely buys stays graded where completeness belongs, in the consolidation's story terms. **Display half:** the lens panel now carries two kinds of thing that must not look alike — three graded chapters with points and width (~365px each at 1536), and MARKS (Phase A, Phase C) as 128px rails whose phase chip stands where their neighbours print a number. A mark appears only when its band resolved; its absence is simply no mark, which retires the "No Phase C on this chart" column that 212 of 302 live setups wore. Also at the operator's word: the total reads **Grade**, not "TA grade", and its 32px figure no longer overprints that label (measured against the live font: the digits' baseline now lands exactly on the chapter figures' number line with 6px of air below). Archive seam: `engine_config_version` rotates, chapter membership being hashed engine identity. **Recorded as theory, unmeasured:** the operator's read that the smaller contracting seller pullback AFTER a shakeout is often a very good setup, and a different shape from a Last Supper. | `engine_alpha/scoring/taxonomy.py` `CHAPTER_ORDER` + layer `marker`; `strategy_alpha.md` Spring entry + "Where the graded reads land"; `engine_reference.md` Phase 4; `setupStoryRows.js` / `SetupStoryPanel.jsx` / `wireVocabulary.CHAPTER_LABELS` |
| 2026-08-12 | **One panel tells the setup's story, and the LPS carries a grade of its own — display only, the grade's arithmetic is untouched.** Operator, on the lens after the TA-grade flip: *"the 'Why it stands out' part should be on top, we can fuse 'Technical Structure Analysis' into it since they both show the same thing… also we should add an LPS grade as well to completely fold them together."* The two panels genuinely duplicated each other — one listed Phase B/C/D as hoverable SPANS, the other as graded CHAPTERS — and neither was whole: the spans had no grades and the chapter strip had no LPS, the phase read last and hardest. Fused into one row per phase (A · Cause · B · C · D · └LPS · Trend, chart order), each carrying its span, its words and its grade. **The LPS grade is the existing `lps_tightness` term as a fraction of `SCORE_LPS_TIGHTNESS`** — the engine's own graded opinion of the last support test, not a new signal, computed from the cap mirror `setupScoreMath.js` has always owned. **It is deliberately NOT a sixth chapter and shows a PERCENTAGE, never points of the 100: those points are already counted inside Phase D**, and printing them again in the points column would stop the column summing to the grade (a new chapter would also re-weight `ta_grade`, hence the tier ladder — a scoring change nobody asked for, and measure-first forbids setting a cap at add time). Chapter→region hover became IDENTITY in the same change (`phase_c`→C, `phase_d`→D, the LPS row claiming `lps`): the old routing pointed each chapter at where its terms live, which contradicts a row that now names its own phase. Nothing engine-side moved — no term, cap, chapter membership, or manifest key. | `webapp/frontend/src/components/setupStoryRows.js` (+ `SetupStoryPanel.jsx`, `chapterStrip.CHAPTER_REGION`) |
| 2026-08-11 | **scan_date is the session the scan evaluated, and the archive was purged & healed to match — operator: "purge & heal".** The nightly scan runs ~18:19 ET = 01:19 local on an ET+7 box, so the local-clock stamp (`date.today()`) filed every nightly cohort under the day AFTER its data session, and weekend re-runs of Friday's panel became NEW Saturday/Sunday rows — 2,906 rows (28.0%) on non-trading dates, plus a uniform +1 mislabel on weekday cohorts. **Fix (306c34c, live from the 2026-08-12 scan):** the ONE threaded scan_date now copies the panel's own last bar via `_last_bar_date` (EC-37 — from the artifact, never the clock; clock fallback only for an empty panel), closing the long-standing asymmetry with the near-miss lane (which always stamped `df.index[-1]`). **Heal (evidence-first, executed 2026-08-12 00:2x):** every legacy cohort was proven against the price cache (`current_price` vs close at stamp vs previous session; 97.9% decisive-vote agreement) rather than blanket-shifted — which is what caught the 26 seed/manual cohorts whose backdated stamps are TRUE, and the stamp-true 2026-07-06 evening-rescan cohort. Purged 2,905; shifted 4,807 across 39 cohorts; merged-out 124 same-session duplicates (incumbent wins); re-pointed 1 read_verdict + 22 setup_reviews; 7,353 rows after, zero screener rows on non-trading dates, zero duplicate keys; forward returns re-matured in full against the corrected dates (`--min-age 0 --force`). **Seed/manual rows are exempt from the heal by design**; the lone seed row on a Sunday (ST, 2026-02-01) is an operator hand-dating slip left for the operator. **DEFERRED, awaiting operator ruling: the June forming-bar era** (9 AMBIGUOUS cohorts + 8 cascade-blocked neighbors, 2,299 rows, 2026-06-01..06-30 + 07-02) — mixed write-events (partial forming bars upserted over prior sessions) that cannot be cohort-shifted; per-row evidence is preserved for a row-grain pass if ever wanted. Backup: `trading_journal.pre_scan_date_heal_2026-08-11.db`. | `core/pipeline/scan_job.py` scan_date derivation; `tools/heal_scan_dates_2026_08.py`; **evidence: [scan_date_heal_2026-08.md](scan_date_heal_2026-08.md)**; `engine_reference.md` scan_identity |
| 2026-08-10 | **Zero-by-geometry never masquerades as zero-by-drift — the episode read carries a zone-coverage companion, and the measurement itself stays untouched.** Discovered on LEVI, the 2026-08-10 scan's TOP row (grade 76.3, Tier S): the operator eyeballed a clean consolidation with an undercut-rebound LPS while the Engine's Read panel claimed "0 completed support tests · ends drifting on support". Mechanism confirmed scan-wide: the episode zones are ATR-fixed (`2 × TOUCH_TOLERANCE_ATR` of box height between them), so on a box shorter than ~2 ATR the neutral middle is thinner than one average bar and distinct tests merge into ONE unresolved visit — LEVI's 1.45-ATR box read as a single R episode spanning the whole base; the sub-2-ATR cohort averages 2.57 completed events vs 4.72+ above, monotone. **Chosen (operator: "Sure lets go with B"): measure the blindness, don't repair it** — every substrate row now archives `event_map_zone_coverage` (`2·tol/(R−S)`, raw, unclamped, NULL = read refused); the story-input law gains its fourth state (all-zero counts at coverage ≥ `STORY_UNREADABLE_ZONE_COVERAGE` = 0.5 read ABSENT, stance included — behavior-neutral today with every `SCORE_STORY_*` at 0, dormant law so the future story A/B cannot punish the tightest boxes); the lens + chapter strip caveat the same rows ("touch zones cover N% of this box — a quiet read here is geometry, not evidence"). Nonzero counts stay evidence at any coverage (FXNC: 5 completed at 0.66). **Option A — re-basing the zones box-relative — DEFERRED, not rejected**: the reader feeds the story-rescue pool (election-affecting) and the ruled admission form + census fingerprint are pinned on exactly these bases, so that change is a re-measurement (new archive seam + census re-run + re-pin) reserved for its own program if story pricing ever needs it; the EGBN precedent (basis artifact confirmed, direct gate-form fix tested-DEAD) argues the same. Also per the naming doctrine: the warning chip drops the never-approved coinage "Terminal drift" for the panel's own words, "Ends drifting on support" (wire id frozen). | `event_map_zone_coverage` + `settings.STORY_UNREADABLE_ZONE_COVERAGE` (manifest seam); `strategy_alpha.md` "The stated geometric limit"; `engine_reference.md` narrative fact block; `narrativeRead.readCaveats` |
| 2026-08-09 | **The TA grade is the read's number, and the tier is derived from it — `TA_SCORE_V2` FLIPPED LIVE at cuts 62 / 52 / 42 / 32.** Operator, on the A/B of the 2026-08-09 scan: *"Sure lets do 62 /52 /42"*, then *"okay the lets do it"*. `Tier` now comes from `ta_grade` against `TIER_*_STRUCT` (manifest-registered, resolved once in `compose_ta_grade` as `structure_tier`) instead of the raw ~122-point sum; `TIER_C_STRUCT = 32` continues the operator's own 10-point spacing (D was empty under every candidate ladder, so nothing rode on it). **62 over the count-preserving 61.5 deliberately**: 61.5 reproduced the old S population exactly but sat 0.19 points above the next grade — a knife edge that reshuffles on any scan. The `S_MAX_BOX_WIDTH` cap is UNCHANGED and still applies on top of the new ladder: it is the operator's rule that a wide base is never elite, and it is what holds 22 of 111 A-tier names out of S (AAP, 4th-highest grade on the scan, at 0.151). **Basis discipline recorded:** the 2026-08-08 A/B was REJECTED as a basis — its rows predate the grade columns so `setup_quality` graded absence-neutral 0 while the stored score kept its points, and the entire ±31 movers list decomposed to exactly that differential (6/6 vs the wire); only a scan archived by the merged code is a legal basis, and the tool now banners it. **Weights deliberately NOT set at the flip** (`SCORE_SPRING` + the four `SCORE_STORY_*` stay 0, both `TA_WARN_*` stay 1.0): those sub-scores are flag-gated, so no live archive exists to calibrate against — setting a cap now would be calibrating at add time, which measure-first forbids. The flip STARTS that archive; costs get set later against it. Consequence stated so it is not misread as a null result: **the flip re-expresses the read on a 0-100 scale with chapters and re-bases the ladder — it does not change the judgment** (within-date movement median 0 / max ±2). Measured drift is exactly two fields, `FOF.Tier` and `HVT.Tier`, both B→A, with no score/rail/ranking movement across all five flag-on shadow guards. Atomic re-bless inside the flip commit (EC-29): shadow re-captured + PASS, seed recall re-captured at 66.2% (its old baseline was ALREADY stale and failing, with recall *improved* 56.9→66.2) + PASS, marks ratchet held 28/33, fold-parity captured, Quick-Reference regenerated. Cost median 0.45 ms/fire, payload +1,019 B/setup. Still owed: the operator's 1536×864 lens eyeball after `update_dashboard.bat`, the grade-verdict UI (API-only, named deferral), and the staged retirement of the legacy path — which IS the rollback until the flip is declared good. | `settings.TA_SCORE_V2` + `TIER_*_STRUCT`; **evidence: [ta_grade_flip_2026-08-09.md](ta_grade_flip_2026-08-09.md)**; [flag_ledger.md](flag_ledger.md) Retired; `strategy_alpha.md` "Where the graded reads land"; `engine_reference.md` Phase 4 |
| 2026-08-09 | **Cause before effect, Phase C → Phase D: the LPS may not predate its spring.** Operator (KYMR eyeball, same sitting): "by Chronological order the Actual LPS comes in Phase D which is found in the Right side of the stock 'V' after either a dedicated Phase C or just natural price action progression that leads to LPS." Doctrine invariant C6 (`spring.tip <= lps.start`) tripped on KYMR (live payload 2026-08-08/09, first appeared with the 08-07→08-08 read shift): the LPS elector — latest window with an overhead trigger, no chronology floor — reached 8 bars back past the elected spring and painted a support level that had already BROKEN (rest at 104.32 on 07-30; spring low 99.52 on 08-03) as the last point of support. Encoded as the LPS window floor `start >= spring.tip` (opening ON the tip stays legal — the sanctioned undercut-rebound form), passed by the narrative walk only; a spring story whose only LPS predates the tip is refused on ORDER (trace outcome `lps_before_spring`, read by the doctrine gate as an expected non-election like `cause_absent`). **Live from birth, not dark-first**: the standing doctrine gate was RED and the blast radius is provably the violator set (the floor only removes candidates, so a C6-clean election cannot move; fleet census 256 setups / 69 springs: KYMR −8 was the sole negative, nearest legal read +2, median +21). A/B evidence riding the change: doctrine 255/255 PASS + KYMR vetoed, shadow byte-identical, marks ratchet 28/33 held. KYMR itself now honestly abstains — its only legal post-spring LPS (08-05..08-06) is already through its trigger; a fresh rest re-elects normally. | flag `LPS_AFTER_SPRING_ENABLED` (manifest key = the archive seam); `strategy_alpha.md` Spring entry; `engine_reference.md` Phase 3 gate 4b |
| 2026-08-09 | **One LPS per setup — the prior-test staircase is no longer drawn.** Operator, same sitting: "the engine is in a strong enough place that we don't need all of these other prior markings and just settle on a single LPS", alongside the other events that may appear. The brown/gold prior LPS/TEST staircase (a proving instrument he originally requested, to see multi-test behavior) is retired from the chart AND the wire: payload field `lps_tests` removed, `phase_d.drawn_support_tests` deleted, the frontend draws only the active `_lps_zone_*` quad in the LPS gold. Display-only — the RAW staircase (`detect_lps_tests`) still feeds Phase-D boundary evidence and the LPS-shrink measurement; `LPS_DRAW_MIN_DESCENT_FRAC` survives as the active-zone drawn trim. | `strategy_alpha.md` LPS entry; `engine_reference.md` Outputs; `chartPhaseOverlay.js` |
| 2026-08-09 | **"Puzzle" is retired from the vocabulary — the term is Setup quality.** Operator: "were grading setups not puzzles, the puzzle is the setups so just refer to it as it is." Applied as the mechanical word substitution while nothing was archived under the old names (the columns were born 2026-08-08 and are still all-NULL): term `setup_quality` / column `score_setup_quality` / cap `SCORE_SETUP_QUALITY` (+ `SETUP_QUALITY_W_*` / `SETUP_QUALITY_CHRONO_PARTIAL` knobs); the three narrative grades `setup_completeness` / `setup_chronology` / `setup_upthrust_terminal`; the L2 metaphor in living docs reads "Wyckoff **story**" (TermSpec kind `story`). Declared no-behavior rename seam — scores byte-identical, manifest rotates. Historical names stay verbatim (retired flag `PUZZLE_SCORE_ENABLED`, old specs/evidence, earlier ruling rows). The live DB keeps four orphaned all-NULL `puzzle_*` columns until a post-update cleanup; **run `update_dashboard.bat` BEFORE the next scan** so nothing ever archives under the old names. | `engine_alpha/scoring/taxonomy.py`; `config/settings.py` |
| 2026-08-09 | **The archive stays pure — measured history rides a sidecar, never a backfill.** Rows that predate the grade columns (everything before 2026-08-08: 9,525 rows / 82 scan dates / 15 engine epochs) keep their NULLs **forever**; the flip checklist's no-backfill contract is now an operator ruling, not just expected churn. When historical puzzle/grade values are wanted, they are MEASURED by point-in-time replay (`tools.ta_grade_archive_replay` — today's engine on the cache, joined to a row only when the replayed rails match the archived rails within 0.5%) and written to a read-only sidecar report (`output/ta_grade_archive_replay_*.json`), never into `setup_archive`. Operator: "lets go with ur recommendation" after the alternative (backfilling measured values into rows) was named. First run's smoke: latest-date fires 100% concordant; with puzzle measured, v1→v2 rank movement at current weights is median 0 / max 1 — the 2026-08-08 A/B's ±31 movers were entirely the missing-puzzle differential. | `tools/ta_grade_archive_replay.py`; [`ta_grade_flip_checklist_2026-08.md`](ta_grade_flip_checklist_2026-08.md) step 0 |
| 2026-08-08 | **The chapter vocabulary is the operator's phase-overlay language: Cause → Phase B → Phase C → Phase D → Trend.** "Work / Turn / Finish" were invented labels the operator never used ("never words I ever said when describing my methods") — retired per the naming doctrine before anything serialized them. Cause = the consolidation, the re/accumulation process; Trend = "hopefully the result of said cause." Chapter MEMBERSHIP is unchanged (the 2026-08-06 partition stands); keys + labels renamed while the flag is dark — a declared no-behavior manifest seam (`TA_GRADE_CHAPTER_ORDER`/`_MAP` rotate), nothing archived under the old names. | `engine_alpha/scoring/taxonomy.py` `CHAPTER_ORDER`; `webapp/frontend/src/components/wireVocabulary.js` |
| 2026-08-06 | **The Event Map is the grade's frame: ONE 0-100 Technical Analysis Grade per setup**, decomposed into story chapters — Cause → Work → Turn → Finish → Trend context — with the letter tier derived from the 0-100. "Event Map shouldn't be a chip but rather a base feature… we will grade the setups by their Event Maps aka their story" (2026-08-05). Chapters are a display partition of ONE fixed-divisor affine sum, never per-chapter normalization; chapter membership is engine identity, hashed into `engine_config_version`, so a re-chaptering is a visible archive seam. The 2026-08-08 registration of this taxonomy (+ the batched v2 settings names) is the build's first deliberate no-behavior seam — scores byte-identical, hash rotated. | `engine_alpha/scoring/taxonomy.py`; flag `TA_SCORE_V2` |
| 2026-08-06 | **HTF enters the GRADE as a visual trait.** The grade's master is visual quality (the operator's charter): the Minervini base count within the current confirmed up-segment and the inter-base width comparison are graded traits — entering measure-first, weighted only at the operator's A/B eyeball. **Supersedes the 2026-07-04 "don't score HTF" edge verdict FOR THE GRADE only**; edge reads remain the separate predictive yardstick, and the demoted weekly/monthly trend-state score stays dead. | this file (supersedes the 2026-07-04 Tested-DEAD row for the grade); measurement lands via the TA-grade build |
| 2026-08-06 | **RS and uptrend stay demoted at 0** — registered, visible, zero-weight display terms (`rs_bonus`, `uptrend_bonus`); never re-inflated outside an operator A/B. | `config/settings.py` |
| 2026-07-27 | **A complete panel that is one session behind is readable, not broken.** "One bad provider day costs me nothing is better for sure." Evaluation proceeds on the cache's own last session while **archiving stays shut**; the tolerance is bounded by `MARKET_DATA_EVALUATE_MAX_LAG_SESSIONS` and requires completeness through that session, so a panel that is both behind and sparse still blocks. Prompted by 2026-07-24, where the static NYSE rule calendar called it a session and Yahoo carried **no bar for it at all**. | `engine_reference.md` "When the provider loses a whole session"; state `session_lag` |
| 2026-07-27 | **Correction, same day.** The 2026-07-24 stall was first diagnosed as "the provider published the session with OHLV and a null Close". That was **wrong**, and the way it was wrong is worth keeping: the row Yahoo served labelled `2026-07-24` (Open 738.510010, Volume 41,480,799, Close null) was the **forming 2026-07-27 bar under the wrong date** — proven when the market opened and 07-27 appeared carrying that identical Open to six decimals. An explicit-window fetch returns `07-20, 07-21, 07-22, 07-23, 07-27` for every symbol probed: 07-24 does not exist upstream. **Lesson: never conclude a bar exists from its index alone — read the values.** The first probe printed only dates and produced a confident, wrong answer that survived into four files before the market open falsified it. | this file |
| 2026-07-27 | **RESOLVED: 2026-07-24 was a normal trading day and Yahoo lost it.** S&P 500 settled 7,411.98, Nasdaq 24,975.82, Dow 51,947.25 — the NYSE calendar was correct, and the second hypothesis (an unscheduled closure a static rule calendar cannot represent) was also wrong. The real failure class is **the sole provider silently dropping a full session across the whole universe**, which no retry can fix. Leaves a permanent one-day hole in the panel (07-23 → 07-27) and no archive row for 07-24. Strongest argument yet for the parked second-provider work: one source that can lose a day has no cross-check. | `engine_reference.md` "When the provider loses a whole session" |
| 2026-07-27 | **A base may not open inside a live trend.** "We can't start the anchor from the opposite direction of the trend if we are still inside that trend" + LIVN's correction was "way too young". Encoded as: box open ≥ covering **confirmed** trend segment's terminal, and when that terminal lands inside the box, ≥ `MIN_BASE_DAYS` bars must have printed since. | `engine_reference.md` "Phase B — trend-terminal box gate"; flag `TREND_TERMINAL_BOX_GATE_ENABLED` |
| 2026-07-26 | **Wyckoff is a source of ideas, not a specification.** Textbook fidelity is not a success criterion. Never open a change whose whole justification is "canon has this and we don't." | [`wyckoff_canon.md`](wyckoff_canon.md) |
| 2026-07-26 | Retire the jargon the operator does not use — *creek*, *ice*, *BU/BUEC*, *Mini-BC*. Describe the event plainly. Frozen wire keys (e.g. `buec_shelf`) stay. | [`wyckoff_canon.md`](wyckoff_canon.md), [`structure_legend.md`](structure_legend.md) |
| 2026-07-25 | Event-Map ORDER separates the twins: drift junk shows zero completed rail-events; a mark has multiple legal forms. | [`event_map_program_2026-07.md`](event_map_program_2026-07.md) |
| 2026-07-24 | **The Guided List (33 marks) is THE engine-test standard** — the must-fire gate, checked with `python -m tools.marks_corpus --check`. | [`guided_list_read_2026-07-24.md`](guided_list_read_2026-07-24.md) |
| 2026-07-20 | Cause-before-effect: a box may not be elected over a live trend that never matured a cause. | flag `CAUSE_BEFORE_EFFECT_VETO_ENABLED` |
| 2026-07-20 | A box opening ON the trend extreme collapses to the sanctioned one-bar boundary form (climax == AR == box open). | `engine_reference.md` "Phase A — Locality Resolution" |
| 2026-07-10 | Rails anchor from the chronological swings, **wick to wick**; a deep below-rail event is Phase C inside ONE box. | flag `BAND_RAILS_ENABLED` |
| — | **Bull regime is not a validity caveat.** The bull-only corpus IS the relevant population; the surviving concern is sample SIZE, not regime. | standing |
| — | **Calibration success = concordance**, not 1:1 rail replication: the engine shares the operator's idea and his picks surface live. | standing |

---

## How a change to this engine should go

The order below is what actually caught mistakes, in the order it caught them.

1. **Read the theory first** ([`strategy_alpha.md`](strategy_alpha.md)), then this file's
   Tested-DEAD table. Most bad proposals die here for free.
2. **Measure before building.** State the discriminator you expect, then test it against the
   corpus *and* against the operator's marks. Four of this engine's most plausible ideas
   were nulls; finding that out costs minutes, and finding it out after implementation costs
   a day.
3. **Build it behind a default-off flag**, with a row in [`flag_ledger.md`](flag_ledger.md)
   in the same change. Flag-off must be byte-identical.
4. **A/B against the sealed marks ratchet** (`python -m tools.marks_corpus --check`) with the
   flag on. A regressed pinned hit is a design falsification, not a threshold to tune —
   diagnose the specific name before touching a constant.
5. **Run the invariant gate** (`python -m tools.doctrine_audit --check`) and the full suite.
6. **Land the docs in the same change.** Theory → `strategy_alpha.md`; implementation and
   its reasoning → `engine_reference.md`; the ruling and anything falsified → here.
7. **The flip is the operator's**, on eyeball evidence. Renders go to `tools/fidelity/`.

**Put the gotcha where the hands are.** The facts that unblocked the 2026-07-27 session came
from *docstrings* at the point of use, not from any document. If an invariant can be stated
next to the code that depends on it, state it there and let these files carry the model.

**Prefer a machine-checked invariant to a paragraph.** `tools/doctrine_audit.py` is the
highest-leverage artifact in the repo: prose tells the next session the rule, the gate proves
they did not break it. When a ruling is expressible as an invariant, add it.
