# Edge read — 2026-06-30 (20-day forward-return calibration)

**Branch:** `engine/calibration-20d` · **Author:** engine lane · **DB:** `webapp/backend/trading_journal.db` (canonical live archive, `source='screener'`)

Pre-flight gates (clean tree, commit `4285c05`): `pytest -q` **609 passed** · `shadow_diff --check` **PASS** (31 firing, no drift) · `seed_recall --check` (fresh, network) **PASS** (recall 54.5% = baseline, no winners lost).

---

## TL;DR

**No engine/scoring change this session. Baselines untouched.** The matured 20-day
sample is nominally large (146 episode-deduped setups) but **collapses to a single
entry cohort** — every matured episode was first seen on **2026-05-31** and shares
one overlapping forward window. The only "harmful" sub-score signal (`score_rs_bonus`)
is a **continuation-inflation artifact** that evaporates under episode-dedup, so
nothing survives the task's bar for action. The LPS markup gate is **validated**
(drops zero matured winners); ADR-tightness is **surfaced for a chart eyeball**;
HTF is **un-testable** (no matured HTF data yet).

---

## Per-lever verdict table

| Lever | Prior expectation | Fresh evidence (matured, episode-deduped) | **Verdict** |
|---|---|---|---|
| Sub-score re-weight — `score_rs_bonus` | −0.23 harmful (over-rewards extended names) | raw rows −0.20 harmful **BUT** episode-deduped **−0.044 INERT** (n=49) | **INSUFFICIENT-SAMPLE / no action** — refuted by dedup |
| Sub-score — `score_base_age` | +0.37 beneficial | deduped **+0.232 beneficial** (already a positive contributor) | Confirmed direction; **no change** |
| Other sub-scores | inert | mostly inert; deduped harmful candidates (`high_proximity` −0.267, `box_tightness` −0.182) are single-cohort & unstable | **No action** |
| ADR-tightness flip `TIGHTNESS_ADR_AWARE` | held, needs eyeball / 20d data | `box_tightness` evidence **mixed & sign-flipping** (−0.182 vs durable-win, **+0.265** vs R); one cohort only | **SURFACE-FOR-EYEBALL — leave OFF** |
| LPS markup gate `LPS_RESCUE_MAX_ADVANCE_BOX=0.21` | live; validate no winner dropped | ON(0.21) vs OFF(None) on 145 matured tickers: both fire 80, **0 dropped, 0 winners dropped** | **VALIDATED — keep 0.21** |
| HTF edge-test (`htf_w_reaccum`/`daily_nested`) | measure-only | **all-null** on matured cohort (HTF shipped ~06-21, after the 05-31 scan) | **INSUFFICIENT-SAMPLE (no data) — defer** |

---

## 1. Sample assessment — the crux

| metric | value |
|---|---|
| raw `source='screener'` rows | 2748 (2026-05-31 → 06-29) |
| episodes (gap-and-islands, `max_gap=4` trading days) | 959 across **15** distinct entry days |
| **matured episodes** (canonical first-seen row has `fwd_return_20d`) | **146** |
| matured with `r_multiple_20d` | 144 |
| matured with a triple-barrier label (`win`/`loss`) | **49** (26 win / 23 loss) |
| matured with `fwd_return_60d` | 0 (60-bar horizon not reached) |
| **distinct entry days among the 146 matured episodes** | **1 — all `2026-05-31`** |

**Why one cohort.** The live archive began 2026-05-31. `fwd_return_20d` only fills
once 20 forward bars exist (~06-27 for a 05-31 entry). The next entry cohorts are
queued but not yet matured — 06-01 (63 episodes), 06-03 (163), 06-05 (114), … 06-27
(20). So today's matured set is exactly the first day's setups, all sharing the
**same** 20-bar forward window.

**Implication.** 146 (or the 49 labelled) is **not** 146 independent observations of
"does sub-score X predict edge." It is **one** cross-sectional snapshot of **one**
regime (the late-May→June tape). The naïve adequacy gate in `analyze.py` (which counts
rows, not cohorts) reports *"Sample looks adequate"* and even the episode-deduped
`signal_edge` marks its verdicts `trustworthy=True` (n=49 ≥ 25, minority 23 ≥ 8) — but
**neither tool checks temporal/regime independence**, which is the binding constraint
here. Per the task's guard ("if it's still thin … do NOT re-weight on noise"), this is
a stop: nominal n clears 30, but the *effective* independent sample is ≈1 cohort.

> The market context makes this concrete: the cohort's mean raw `fwd_return_20d` is
> **+8.0%** (86% up); SPY-subtracted **abnormal** return is **+9.8%** (88% beat SPY).
> A +8% / 86%-up snapshot is a single bull-tape realization — exactly the kind of
> outcome distribution that yields spurious cross-sectional correlations.

---

## 2. Prior-signal confirm / refute (raw rows vs episode-deduped)

The prior signals were measured on **raw** archive rows. The screener re-flags a
persisting base every day, so a base that sits 15 days and then loses contributes its
sub-scores **15×** against one shared outcome. Collapsing to one row per logical setup
(`core/archive/episodes.py`) removes that autocorrelation. The two views disagree, and
the disagreement is the finding:

| sub-score | prior | raw rows (n=769 durable) | **episode-deduped (n=49 durable)** | survives dedup? |
|---|---|---|---|---|
| `score_rs_bonus` | −0.23 **harmful** | −0.20 **harmful** | **−0.044 INERT** | **NO — refuted** |
| `score_base_age` | +0.37 beneficial | +0.30 beneficial | **+0.232 beneficial** | yes (already positive) |
| `score_high_proximity` | — | +0.11 inert | −0.267 harmful* | new, single-cohort |
| `score_box_tightness` | — | −0.06 inert | −0.182 harmful* / **+0.265 vs R** | sign-flips by target |

\* The "harmful" label **moves between sub-scores depending on raw-vs-dedup**
(rs_bonus in raw; high_proximity + box_tightness in deduped) and, for box_tightness,
**flips sign across outcome targets**. That instability — across aggregation method,
across targets, and vs the prior read (rs_bonus −0.23 → −0.044) — is itself decisive
evidence that none of these are robust. They are noise on one regime.

**Conclusion: nothing survives episode-dedup as an actionable harmful signal with a
real (multi-cohort) sample.** `score_rs_bonus`'s apparent harm is a measurement
artifact, not an engine defect.

---

## 3. Sub-score re-weighting — **NO ACTION**

No weight in `config/settings.py` is changed. Therefore `shadow_diff` and `seed_recall`
baselines are **not** re-captured (they were confirmed green at pre-flight and the
engine is byte-identical). Re-weighting is the only step that moves the baselines; it
is not justified on a single-cohort sample whose one harmful candidate is a dedup
artifact.

Watch-list for the next read (do **not** act now): if `score_high_proximity` and/or
`score_box_tightness` read persistently harmful **across distinct-day cohorts** (not
just within 05-31), they become re-weight candidates — `high_proximity` would be the
first to trim, and `box_tightness` is entangled with the ADR-tightness question below.

---

## 4. ADR-tightness flip (`TIGHTNESS_ADR_AWARE`) — **SURFACE FOR EYEBALL, LEAVE OFF**

The dissection rationale (corr(box_tightness, ADR) = −0.73; GBTG-class flat low-ADR
drifts scoring as "coils") is unchanged and on-philosophy. But the matured data does
**not** give a clean confirmation:

- `score_box_tightness` rank-corr is **−0.182 vs durable-win but +0.265 vs R-multiple**
  (deduped). On raw `fwd_return_20d`, the tight box-width tertile **underperforms** the
  loose tertile by −2.9% (Section 5 "prime-directive" test), but `tightness_ratio` and
  `atr_ratio` tertiles mildly **support** the thesis. Mixed.
- All of this is one cohort / one regime, and ranking-only by construction (firing is
  unchanged), so it is recall-safe to flip whenever — but the *evidence to justify the
  flip is not here yet*.

**Recommendation (needs a human chart eyeball — I cannot eyeball charts autonomously):**
hold `TIGHTNESS_ADR_AWARE = False`. Flip only after **(a)** a side-by-side chart look
(does GBTG read as dead drift vs TECX a real coil?) **and** **(b)** a multi-cohort read
confirms low-ADR tight-box names underperform. The flip mechanics (set `True` → re-capture
shadow baseline) are unchanged; threshold `MAX_BOX_WIDTH_ADR=4.5` was stable 3.5–5.0.

---

## 5. LPS markup gate (`LPS_RESCUE_MAX_ADVANCE_BOX = 0.21`) — **VALIDATED, KEEP 0.21**

Validation method (network-free, cache-based): re-evaluate each matured ticker as-of its
2026-05-31 entry on the **same** 5y cache under gate **ON (0.21)** vs **OFF (None)**.
Comparing ON vs OFF on identical data isolates the gate's marginal firing effect.

```
matured tickers as-of 2026-05-31: 146 (145 evaluable; SSSS has no cache history)
fire under gate OFF(None): 80     fire under gate ON(0.21): 80
DROPPED BY GATE (fires OFF, not ON): 0
  → matured barrier/durable winners dropped: 0
  → matured fwd20-up tickers dropped:        0
```

The 0.21 gate drops **zero** matured tickers and therefore **zero** winners on the only
matured cohort. Combined with the prior seed-recall validation (0/52 winners dropped at
the **tighter** 0.15), the gate is confirmed recall-safe. **Keep 0.21.**

> Note: the archive's `lps_swing_type` column is null for the 05-31 cohort (it began
> being written ~06-26, and the gate was committed 06-27 — both after this scan), so the
> gate could not be validated by reading archived swing-types; the cache re-eval above is
> the correct substitute and gives a direct ON-vs-OFF answer.

---

## 6. HTF edge-test (`htf_w_reaccum` / `htf_w_daily_nested`) — **DEFER (no data)**

All HTF fields (`htf_w_reaccum`, `htf_w_daily_nested`, `htf_w_stage2`, `htf_w_in_consol`,
`htf_m_*`) are **null** for every matured episode. HTF context began being computed only
on scans after the feature shipped (~2026-06-21); none of those scans have reached 20
forward bars. There is nothing to correlate yet. As instructed, HTF is **not** wired into
scoring. Re-test when post-06-21 scans mature (~mid-July for a first 20d read).

---

## 7. Engine standalone edge (encouraging side-note, not a calibration lever)

Though this single cohort can't calibrate sub-scores, it is a first read on the engine's
**standalone edge** (per the engine-validation north star): the 05-31 picks returned a
mean **+9.8% abnormal (SPY-subtracted) 20-day return**, with **88% beating SPY**.
Tier monotonicity also holds on the deduped matured set: S (n=113) +9.0% / 91% win /
+1.75 expR ≥ A (n=31) +5.2% / 71% / +1.60 ≥ B (n=2) −5.1%. Encouraging, but it is one
cohort — read it as a promising first data point, not validated edge.

---

## 8. Re-check schedule

| when | what matures | use |
|---|---|---|
| ~rolling, daily | 06-01 → 06-27 cohorts cross 20 bars through July | sample grows fast, but all still early-June overlapping tape |
| **~2026-07-15** | ~10 entry-day cohorts (05-31 → ~06-18) matured to 20d (~700 episodes) | **first genuinely multi-cohort 20d read** — re-run this analysis |
| **~2026-08-25** | 05-31 cohort reaches the 60-bar horizon | **first 60d read** — the more decisive horizon |

Re-weighting needs cohorts from **distinct regimes**, not merely more early-June days.
The earliest defensible re-weight decision is the 60d read (late Aug) or a multi-cohort
20d read that spans a visibly different tape.

---

## Appendix A — official `python -m core.archive.analyze --source screener` (raw rows)

The standard instrument, unmodified. Reads raw (continuation-inflated, single-cohort)
rows; its Section 1 "adequate" verdict and Section 6 `score_rs_bonus = harmful` are the
raw-row view that §2 above refutes via episode-dedup. Section 1 also flags a separate,
known data-quality issue (10 base_length > 250 anchor outliers: CACC/MSCI/BMRN/CWT) —
unrelated to this calibration, tracked elsewhere.

```
========================================================================
  3. SEGMENTED PERFORMANCE  (raw rows; single 05-31 cohort; beta-laden)
========================================================================
-- By tier -------------------------------------------------------------
tier               n   med20d   avg20d   win%  trig%   avgR   expR
S               1829    +8.4%    +9.0%  +91.2% +88.7%   1.75   1.75
A                741    +6.3%    +5.2%  +71.0% +84.6%   1.60   1.60
B                172    -5.1%    -5.1%   +0.0% +72.9%   0.29   0.29
-- By setup_type -------------------------------------------------------
LPS             2590    +7.8%    +7.9%  +85.9% +87.3%   1.48   1.48
REBOUND          158    +8.8%    +9.3%  +81.8% +78.9%   4.60   4.60

========================================================================
  4. PREDICTOR CORRELATIONS (raw rows)
========================================================================
-- vs fwd_return_20d (top |corr|) --
score_adr +0.218 · s_touches +0.216 · score_uptrend_bonus +0.209 · adr_pct +0.199
score_high_proximity -0.187 · touches +0.181 · dist_52w_high_pct -0.164
contraction_count +0.163 · score_touch_density +0.131 · score_lps_tightness +0.126
-- vs r_multiple_20d (top |corr|) --
rs_vs_sector_pct -0.276 · box_width -0.143 · score_box_tightness +0.143
s_touches +0.132 · score_ascending_support -0.120 · base_length -0.111

========================================================================
  5. PRIME-DIRECTIVE (tightness) — raw fwd_return_20d tertiles
========================================================================
box_width:        tight +6.8% vs loose +9.6%  → CONTRADICTS (-2.9%)
atr_ratio:        tight +7.6% vs loose +6.6%  → supports (+1.0%)
tightness_ratio:  tight +8.8% vs loose +7.3%  → supports (+1.5%)
lps_descent_frac: tight +8.1% vs loose +9.3%  → CONTRADICTS (-1.2%)
contraction_qual: tight +8.1% vs loose +7.1%  → supports (+1.0%)

========================================================================
  6. SIGNAL EDGE (raw rows, durable_win primary, n=769, minority=335)
========================================================================
score_rs_bonus          769   -0.20  harmful   <-- refuted by dedup (-0.044, §2)
score_uptrend_bonus     769   -0.14  inert
score_adr               769   -0.11  inert
score_box_tightness     769   -0.06  inert
score_touch_density     769   +0.01  inert
score_lps_tightness     769   +0.08  inert
score_breadth_bonus     769   +0.09  inert
score_ascending_support 769   +0.10  inert
score_high_proximity    769   +0.11  inert
score_base_age          769   +0.30  beneficial
  HARMFUL (raw): score_rs_bonus
```

## Appendix B — episode-deduped signal edge (the decision basis)

One row per logical setup (`build_episodes`, `max_gap=4` trading days); matured = canonical
first-seen row has `fwd_return_20d`. Primary `durable_win` n=49 (26 win / 23 loss), all
entry date 2026-05-31.

```
sub-score                  n   durable_r  verdict      | fwd20   R20
score_high_proximity      49    -0.267    harmful*     | -0.116  -0.049
score_box_tightness       49    -0.182    harmful*     | -0.051  +0.265   <-- sign-flips
score_atr_squeeze         49    -0.064    inert        | +0.001  +0.094
score_adr                 49    -0.045    inert        | +0.131  -0.005
score_rs_bonus            49    -0.044    INERT        | +0.072  -0.015   <-- refutes prior -0.23
score_uptrend_bonus       49    +0.011    inert        | +0.171  +0.079
score_lps_tightness       49    +0.112    inert        | +0.084  +0.103
score_vol_contraction     49    +0.132    inert        | +0.029  +0.062
score_touch_density       49    +0.153    beneficial   | +0.230  +0.281
score_base_age            49    +0.232    beneficial   | +0.048  -0.030
score_ascending_support   49    +0.324    beneficial   | +0.100  -0.025
* single-cohort, unstable across targets/aggregation — NOT acted on.

TIER EXPECTANCY-R (deduped matured): S n=113 +9.0%/91%/+1.75 · A n=31 +5.2%/71%/+1.60 · B n=2 -5.1%/0%/+0.29
HTF columns: all null (feature post-dates the matured cohort).
```
